Convert the active document to DocBook:
* do not use <programlisting> but <screen> instead, do not include its language specification
* do not use <sidebar> - only keep its content
* modify xml:id's to briefly describe the content of the related block; prefix them with 'rke2-'
* enclose all file names in <filename/> if they are not inside <screen/>
* if variables - usually written in uppercase - are enclosed in <literal>, enclose them in <replaceable> instead
* if variables are enclosed in &lt; and &gt;, enclose them in <replaceable> instead
* system services should be enclosed with <systemitem class="daemon">, not unenclosed or enclosed with <literal>
* titles have only the first character od the first word capitalized