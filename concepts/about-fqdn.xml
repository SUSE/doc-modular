<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="about-fqdn"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>About the FQDN</title>
    <meta name="maintainer" content="amrita.sakthivel@suse.com" its:translate="no"/>
    <abstract>
      <para>
FQDN is a domain name that specifies its exact location in the tree hierarchy of the DNS (Domain Name System).
It is absolute, which means it provides every label from the specific host up to the DNS root zone.
</para>
    </abstract>
  </info>
<para>An FQDN consists of two main parts; the host name and the domain name. It is structured from the most specific label on the left, to the most general, which is on the right.</para>
<para>For example, <literal>mail.google.com</literal>:</para>

<variablelist>
  <varlistentry>
    <term>Host name</term>
    <listitem>
      <para>
        <literal>mail</literal> identifies a specific server or service; in this case, it is the server handling e-mail.</para>
     </listitem>
  </varlistentry>
  <varlistentry>
    <term>Subdomain</term>
    <listitem>
      <para>
        <literal>google</literal> is the specific organization or network.</para>
     </listitem>
  </varlistentry>
  <varlistentry>
    <term>Top-level domain</term>
    <listitem>
      <para>
        <literal>com</literal> is the general category (commercial).</para>
     </listitem>
  </varlistentry>
  <varlistentry>
    <term>Trailing dot</term>
    <listitem>
      <para>
        <literal>.</literal> is the root zone. While we usually leave it off when typing in a browser, a true FQDN technically ends with a dot. This signals that the name is absolute and not relative to another domain.</para>
     </listitem>
  </varlistentry>
  </variablelist>
<para>A host name and an FQDN are distinctly different because they serve different functions in a networked environment.
A host name is a simple human readable label assigned to a device on a local network. It is relative because it only identifies the machine within its immediate subnetwork.
An FQDN is the absolute and unambiguous address of that host on the Internet. It combines the host name with the full domain hierarchy.
</para>
<section xml:id="imp-fqdn">
<title>Importance of FQDNs</title>
<para>FQDNs serve a number of important functions:</para>
<itemizedlist>
<listitem><para>By mapping a distinct host name to its full parent domain hierarchy, FQDNs eliminate the ambiguity inherent in local naming conventions, ensuring that every asset can be precisely located and accessed across the global Internet.</para></listitem>
<listitem><para>In an e-mail address, the portion following the <literal>@</literal> symbol represents the FQDN (or a domain that resolves to one) of the destination mail server.</para></listitem>
<listitem><para>In Web hosting, FQDNs serve as the definitive addresses that map user requests to specific Web servers, enabling browsers to locate and retrieve precise Web pages within the global DNS structure.</para></listitem>
<listitem><para>DNS serves as the Internet's directory by translating human-readable names into machine-accessible IP addresses to facilitate cross-network communication. FQDNs are the essential components of this process, providing the absolute, unambiguous paths required for DNS queries and responses to accurately resolve host names to their specific numerical destinations.</para></listitem>
</itemizedlist>
</section>
<section xml:id="certs-fqdn">
    <title>About FQDNs and SSL/TLS certificates</title>
    <para>An FQDN is a fundamental requirement for obtaining SSL/TLS certificates, which safeguard online communications by encrypting data transmitted between servers and clients.
      By providing a specific FQDN to a Certificate Authority (CA), you ensure the resulting certificate is mathematically bound to your exact domain, authenticating your site's identity and preventing unauthorized third parties from intercepting or spoofing your traffic.</para>
<para>Without a valid FQDN, browsers cannot validate the site’s security credentials, making it impossible to establish the trusted, encrypted tunnel necessary for modern Web safety and data privacy.</para>
    </section>
</topic>