<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<topic xml:id="agama-automated-installation"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <title>Automated installation using &dinstaller;</title>
  <section xml:id="concept-what-is-agama-automated-installation">
    <title>What is an automated installation?</title>
    <para>
      As an addition to the interactive installation described in
      <xref linkend="task-deploy-alp-dinstaller"/>, &dinstaller; supports an
      unattended automated installation.
    </para>
  </section>
  <section xml:id="concept-agama-automated-installation-how-it-works">
    <title>How does an automated installation work?</title>
    <para>
      &dinstaller; supports the following types of unattended installation:
    </para>
    <variablelist>
      <varlistentry>
        <term>Profile-based installation</term>
        <listitem>
          <para>
            This type of installation uses a customized file called
            <emphasis>profile</emphasis> that includes a description of the
            system to install.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Script-based installation</term>
        <listitem>
          <para>
            This type of installation uses a plain shell script that enables
            custom pre-installation workflows.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>
    <para>
      The actual automated installation is started by passing the following
      parameter on the kernel command line during the &dinstaller; boot
      process:
    </para>
<screen>dinstaller.auto=<replaceable>PROFILE_OR_SCRIPT_URL</replaceable></screen>
    <tip>
      <para>
        When booting from the &dinstaller; live media ISO, you need to edit and
        modify the &grub; boot loader command line and specify the path to the
        profile configuration file. When booting via PXE, the file can be
        reached via HTTP, for example:
      </para>
<screen>dinstaller.auto=http://example.net/profile1.jsonnet</screen>
    </tip>
    <important>
      <title>Use correct file suffix</title>
      <para>
        Using the correct suffix of the file name is important:
      </para>
      <variablelist>
        <varlistentry>
          <term>.jsonnet</term>
          <listitem>
            <para>
              Enables dynamic content through Jsonnet.
            </para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term>.json</term>
          <listitem>
            <para>
              Assumes that the profile is just a JSON file with no dynamic
              content.
            </para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term>.sh</term>
          <listitem>
            <para>
              Is interpreted as a shell script.
            </para>
          </listitem>
        </varlistentry>
      </variablelist>
    </important>
  </section>
  <section xml:id="concept-agama-automated-installation-benefits">
    <title>Benefits of an automated installation</title>
    <itemizedlist>
      <listitem>
        <para>
          You can prepare installation setup in advance and modify it easily
          for future deployments.
        </para>
      </listitem>
      <listitem>
        <para>
          By running an unattended installation, you can save the time that you
          would normally spend in the interactive installation process.
        </para>
      </listitem>
      <listitem>
        <para>
          After fine-tuning the installation profile, you can deploy multiple
          hosts at the same time to meet datacenter requirements.
        </para>
      </listitem>
    </itemizedlist>
  </section>
</topic>
