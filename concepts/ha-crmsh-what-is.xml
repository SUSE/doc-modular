<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_config_cli.xml -->

<topic xml:id="ha-crmsh-what-is"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>What is the &crmshell;?</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        The <emphasis>&crmshell;</emphasis> (&crmsh;) is a command-line interface for installing,
        configuring and managing &ha; clusters. You can run most &crmsh; commands from any node
        in the cluster.
      </para>
      <para>
        This section introduces the command-line tool <command>crm</command>. The <command>crm</command> command has several subcommands which manage resources, CIBs, nodes, resource agents, and others. It offers a thorough help system with embedded examples. Events are logged to <filename>/var/log/crmsh/crmsh.log</filename>.
      </para>
    </abstract>
  </info>

  <section xml:id="ha-crmsh-what-is-using">
    <title>Using the &crmshell;</title>
    <para>
    The <command>crm</command> command itself can be used in the following ways:
    </para>
    <variablelist>
      <varlistentry>
        <term>Directly</term>
        <listitem>
          <para>
            Concatenate all subcommands to <command>crm</command>, press <keycap function="enter"/> and you see the output immediately. For example, enter <command>crm help ra</command> to get  about the <command>ra</command> subcommand (resource agents).
          </para>
          <para>
            It is possible to abbreviate subcommands if they are unique. For example, you can shorten <command>status</command> as <command>st</command> and &crmsh; knows what you mean.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Interactive mode</term>
        <listitem>
          <para>
            Type <command>crm</command> to enter the internal shell. The prompt changes to <literal>crm(live)</literal>. With <command>help</command> you can get an overview of the available subcommands. As the internal shell has different levels of subcommands, you can <quote>enter</quote> one by typing this subcommand and press <keycap function="enter"/>.
          </para>
          <para>
            For example, if you type <command>resource</command> you enter the resource management level. Your prompt changes to <literal>crm(live)resource#</literal>. To leave the internal shell, use the command <command>quit</command>. If you need to go one level back, use <command>back</command>, <command>up</command>, <command>end</command>, or <command>cd</command>.
          </para>
          <para>
            You can enter the level directly by typing <command>crm</command> and the respective subcommand without any options and press <keycap function="enter"/>.
          </para>
          <para>
            The internal shell supports also tab completion for subcommands and resources. Type the beginning of a command, press <keycap function="tab"/> and <command>crm</command> completes the respective object.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Shell scripts</term>
        <listitem>
          <para>
            &crmshell; scripts contain subcommands of <command>crm</command>. For more information, see xref linkend="sec-ha-manual-config-crmshellscripts"/.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Cluster scripts</term>
        <listitem>
          <para>
            These are a collection of metadata, references to RPM packages, configuration files, and &crmsh; subcommands bundled under a single, yet descriptive name. They are managed through the <command>crm script</command> command.
          </para>
          <para>Do not confuse them with &crmsh; shell scripts: although both share some common objectives, the &crmshell; scripts only contain subcommands whereas cluster scripts incorporate much more than a simple enumeration of commands. For more information, see xref linkend="sec-ha-manual-config-clusterscripts"/.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>
    <para>
      &crmsh; also supports synchronous command execution. Use the <option>-w</option> option to activate it. If you have started <command>crm</command> without <option>-w</option>, you can enable it later with the user preference's <command>wait</command> set to <literal>yes</literal> (<command>options wait yes</command>). If this option is enabled, <command>crm</command> waits until the transition is finished. Whenever a transaction is started, dots are printed to indicate progress. Synchronous command execution is only applicable for commands like <command>resource start</command>.
    </para>
  </section>

  <section xml:id="ha-crmsh-what-is-authentication">
    <title>User privileges and authentication</title>
    <para>
      Managing a cluster requires sufficient privileges. The following users can run the
      <command>crm</command> command and its subcommands:
    </para>
    <itemizedlist>
      <listitem>
        <para>
          The &rootuser; user or a user with <command>sudo</command> privileges. These users have
          full privileges on all cluster nodes by using SSH for &crmsh; operations such as
          <command>crm cluster init</command>, <command>crm cluster join</command>, and
          <command>crm report</command>. They can also make changes to the CIB,
          such as adding resources and constraints.
        </para>
      </listitem>
      <listitem>
        <para>
          The CRM owner user (typically the user <systemitem class="username">hacluster</systemitem>,
          created by default during the cluster installation). This user can make changes to the CIB,
          but has limited privileges for operations that use SSH, such as <command>crm report</command>.
        </para>
        <tip>
          <title>Unprivileged users</title>
          <para>
            The <option>user</option> option allows you to run <command>crm</command> and its
            subcommands as a regular (unprivileged) user, and to change its ID using
            <command>sudo</command> when necessary. For example, the following command sets
            <systemitem class="username">hacluster</systemitem> as the privileged user ID:
          </para>
          <screen>&prompt.root;<command>crm options user hacluster</command></screen>
          <para>
            You must set up <filename>/etc/sudoers</filename> so that <command>sudo</command>
            does not ask for a password.
          </para>
        </tip>
      </listitem>
    </itemizedlist>
    <para>
      For operations that use SSH, the cluster uses passwordless SSH access for communication
      between the nodes. If you set up the cluster with <command>crm cluster init</command>, the
      script checks for SSH keys and generates them if they do not already exist. If you set up
      the cluster with the &yast; cluster module, you must configure the SSH keys yourself.
    </para>
    <para>
      In most cases, the &rootuser; or <command>sudo</command> user's SSH keys must exist (or be
      generated) on the node. Alternatively, a <command>sudo</command> user's SSH keys can exist
      on a local machine and be passed to the node via SSH agent forwarding. This can be useful
      if you need to avoid storing SSH keys on the nodes, but requires additional configuration.
    </para>
  </section>

  <section xml:id="ha-crmsh-what-is-more-info">
    <title>For more information</title>
    <itemizedlist>
      <listitem>
        <para>
https://crmsh.github.io/
        </para>
      </listitem>
      <listitem>
        <para>
https://crmsh.github.io/documentation
        </para>
      </listitem>
      <listitem>
        <para>
crm help overview
        </para>
      </listitem>
      <listitem>
        <para>
https://crmsh.github.io/man-5.0/#topics.Introduction.Interface
        </para>
      </listitem>
    </itemizedlist>
  </section>
</topic>
