<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_config_hawk2.xml -->

<topic xml:id="ha-hawk-what-is"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>What is &hawk;?</title>
    <meta name="maintainer" content="" its:translate="no"/>
    <abstract>
      <para>
        &hawk; (&haweb;) is a Web-based graphical interface for configuring and managing &sleha;
        clusters. You can use &hawk; on any Linux or non-Linux machine (the <emphasis>client
        machine</emphasis>) with a Web browser and network access to the cluster nodes.
      </para>
      <para>
        You can access Hawk using the IP address of any node in the cluster that has the &hawk;
        service running. Alternatively, you can create a floating, virtual IP address so that you
        can access &hawk; with a consistent address. If you install the cluster with
        <command>crm cluster init</command>, the setup script can create the virtual IP address
        for you.
      </para>
      <para>
        The &hawk; Web interface uses the HTTPS protocol and port 7630.
      </para>
    </abstract>
  </info>

  <section xml:id="ha-hawk-what-is-requirements">
    <title>Requirements</title>
    <para>
      Before users can log in to &hawk;, certain requirements must be met on the client machine
      and on the cluster nodes:
    </para>
    <variablelist>
      <varlistentry>
        <term>Client requirements</term>
        <listitem>
          <itemizedlist>
            <listitem>
              <para>
                To connect to &hawk;, the client machine must have network access to the cluster
                nodes.
              </para>
            </listitem>
            <listitem>
              <para>
                To use &hawk;, the client machine must have a graphical Web browser with JavaScript
                and cookies enabled.
              </para>
            </listitem>
          </itemizedlist>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Cluster node requirements</term>
        <listitem>
          <itemizedlist>
            <listitem>
              <para>
                The <package>hawk2</package> package must be installed on the node that you want to
                connect to. This is a recommended package in the <package>ha_sles</package> pattern,
                so is typically installed on all nodes as part of the cluster installation.
              </para>
            </listitem>
            <listitem>
              <para>
                The <literal>hawk</literal> service must be started on the node that you want to
                connect to. If you installed the cluster with <command>crm cluster init</command>,
                the <literal>hawk</literal> service is already enabled.
              </para>
            </listitem>
            <listitem>
              <para>
                &hawk; users must be members of the <systemitem>haclient</systemitem> group.
                If you installed the cluster with <command>crm cluster init</command>, the user
                <systemitem>hacluster</systemitem> is created by default. You can also create a
                new user in the <systemitem>haclient</systemitem> group.
              </para>
            </listitem>
            <listitem>
              <para>
                &hawk; users must have secure passwords. If you installed the cluster with
                <command>crm cluster init</command>, a default password is set for the
                <systemitem>hacluster</systemitem> user. Before starting &hawk;, change the
                default password to a secure password.
              </para>
            </listitem>
            <listitem>
              <para>
                By default, Hawk generates and presents a self-signed certificate. We recommend
                replacing the generated certificate with your own certificate, or a certificate
                that was signed by an official Certificate Authority.
              </para>
            </listitem>
          </itemizedlist>
        </listitem>
      </varlistentry>
    </variablelist>
  </section>

  <section xml:id="ha-hawk-what-is-top-level-row">
    <title>Top-level row</title>
    <para>
      &hawk;'s top-level row shows the following options:
    </para>
    <!-- Add screen shot -->
    <itemizedlist>
      <listitem>
        <para>
          <guimenu>View Cluster Details</guimenu>: View details such as the cluster's status,
          which node is the Designated Coordinator, and the &pace; version, for example. Certain
          items are underlined, which means you can hover the cursor over them for more information.
        </para>
      </listitem>
      <listitem>
      <para>
        <guimenu>Batch</guimenu>: Switch to batch mode. This allows you to simulate and stage
        changes and to apply them as a single transaction.
      </para>
      </listitem>
      <listitem>
      <para>
        <guimenu><replaceable>USERNAME</replaceable></guimenu>: Set preferences for &hawk;, such as
        the language for the Web interface, or whether to display a warning if fencing is disabled.
      </para>
      </listitem>
      <listitem>
      <para>
        <guimenu>Help</guimenu>: Access the &productname; documentation, read the release notes
        or report a bug.
      </para>
      </listitem>
      <listitem>
      <para>
        <guimenu>Logout</guimenu>: Log out of &hawk;.
      </para>
      </listitem>
    </itemizedlist>
  </section>

  <section xml:id="ha-hawk-what-is-left-navigation-bar">
    <title>Left navigation bar</title>
    <para>
      &hawk;'s left navigation bar shows the following options. Most screens refresh in near real time, so changes are visible almost immediately.
    </para>
    <!-- Add screen shot -->
    <variablelist>
      <varlistentry>
        <term><guimenu>Monitoring</guimenu></term>
        <listitem>
          <itemizedlist>
            <listitem>
              <para>
                <guimenu>Status</guimenu>: Displays the current cluster status at a glance.
              </para>
            </listitem>
            <listitem>
              <para>
              <guimenu>Dashboard</guimenu>: Allows you to monitor multiple clusters.
              </para>
            </listitem>
          </itemizedlist>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><guimenu>Troubleshooting</guimenu></term>
        <listitem>
          <itemizedlist>
          <listitem>
            <para>
              <guimenu>History</guimenu>: Opens the <guimenu>History Explorer</guimenu>, from
              which you can generate cluster reports.
            </para>
          </listitem>
          <listitem>
            <para>
              <guimenu>Command Log</guimenu>: Lists the <command>crm</command> commands recently
              run by &hawk;.
            </para>
          </listitem>
          </itemizedlist>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><guimenu>Configuration</guimenu></term>
        <listitem>
          <itemizedlist>
            <listitem>
              <para>
                <guimenu>Add Resource</guimenu>: Opens the resource configuration screen.
            </para>
            </listitem>
            <listitem>
              <para>
                <guimenu>Add Constraint</guimenu>: Opens the constraint configuration screen.
              </para>
            </listitem>
            <listitem>
              <para>
                <guimenu>Wizards</guimenu>: Allows you to select from several wizards that guide
                you through the creation of resources for a certain workload, for example, a
                DRBD block device.
              </para>
            </listitem>
            <listitem>
              <para>
                <guimenu>Edit Configuration</guimenu>: Allows you to edit resources, constraints,
                node names and attributes, tags, alerts, and fencing topologies.
              </para>
            </listitem>
            <listitem>
              <para>
                <guimenu>Cluster Configuration</guimenu>: Allows you to modify global cluster
                options and defaults.
            </para>
            </listitem>
            <listitem>
              <para>
                <menuchoice><guimenu>Access Control</guimenu><guimenu>Roles</guimenu></menuchoice>:
                Allows you to create roles (sets of rules describing access rights to the CIB) for
                access control lists (ACLs).
            </para>
            </listitem>
            <listitem>
              <para>
                <menuchoice><guimenu>Access Control</guimenu><guimenu>Targets</guimenu></menuchoice>:
                Allows you to create ACL targets (users) and assign roles to them.
              </para>
            </listitem>
          </itemizedlist>
        </listitem>
      </varlistentry>
    </variablelist>
  </section>

  <section xml:id="ha-hawk-what-is-more-info">
    <title>For more information</title>
    <itemizedlist>
      <listitem>
        <para><!-- Not sure if this is even worth including as it's very outdated -->
          <link xlink:href="https://hawk-ui.github.io/"/>
        </para>
      </listitem>
    </itemizedlist>
  </section>
</topic>
