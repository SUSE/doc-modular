<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_resource_constraints.xml -->

<topic xml:id="ha-resources-constraints-what-is"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>What are resource constraints?</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        Resource constraints let you specify which nodes resources can run on, what order
        resources should start in, and which resources are dependent on one another.
      </para>
    </abstract>
  </info>

<section xml:id="ha-resources-constraints-what-is-types">
  <title>Types of constraints</title>
  <variablelist>
    <varlistentry>
      <term>Order</term>
      <listitem>
        <xi:include href="../snippets/ha-resources-order-constraints.xml"/>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>Location</term>
      <listitem>
        <xi:include href="../snippets/ha-resources-location-constraints.xml"/>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>Colocation</term>
      <listitem>
        <xi:include href="../snippets/ha-resources-colocation-constraints.xml"/>
      </listitem>
    </varlistentry>
  </variablelist>
  <important>
    <title>Restrictions for constraints and primitive resources</title>
    <itemizedlist>
      <listitem>
        <para>
          Don't add colocation constraints to primitives in a resource group.
          Add a colocation constraint to the group instead.
        </para>
      </listitem>
      <listitem>
        <para>
          Don't add any constraints to a primitive that has been cloned.
          Add constraints to the clone instead.
        </para>
      </listitem>
    </itemizedlist>
  </important>
</section>

  <section xml:id="ha-resources-constraints-what-is-scores">
    <title>Scores and kinds</title>
    <para>
      Location and colocation constraints must have a <emphasis>score</emphasis> to indicate how
      important the constraint is. A score of <literal>INFINITY</literal> (positive or negative)
      means that the constraint <emphasis>must</emphasis> be applied. The score can also be a
      non-infinite number (positive or negative), which means the constraint is only a preference.
      In this case, the constraint might not be applied if its score is outweighed by other factors,
      such as other constraints, resource stickiness, failure thresholds, etc.
    </para>
    <para>
      For location constraints, a positive score means the resource should (or must) run on the
      specified node. A negative score means the resource should <emphasis>not</emphasis> (or must
      not) run on the specified node. You can also add multiple location constraints with different
      scores to create an order of node preference.
    </para>
    <para>
      For colocation constraints, a positive score means the resources should (or must) run together
      on the same node. A negative score means the resources should <emphasis>not</emphasis> (or
      must not) run together on the same node.
    </para>
    <para>
      Instead of a score, order constraints have a <emphasis>kind</emphasis>, which defines whether
      the constraint is mandatory or optional.
    </para>
  </section>

  <section xml:id="ha-resources-constraints-what-is-sets">
   <title>Resource sets</title>
   <para>
     TODO As an alternative format for defining location, colocation or order constraints, you can use <emphasis>resource sets</emphasis>, where primitives are grouped together in one set. Resource sets allow multiple resources to be affected by a single constraint.
   </para>
   <para>
    They do different things for different constraints
   </para>
   <para>
     Sets can be either ordered (<literal>sequential=true</literal>) or unordered (<literal>sequential=false</literal>). Furthermore, the <literal>require-all</literal> attribute can be used to switch between <literal>AND</literal> and <literal>OR</literal> logic.
    </para>
    <para>
      Location constraints use curly {} brackets, order and colocation constraints use regular () brackets or sometimes square [] brackets. Why...?
    </para>
<!-- trichardson 2026-04-01: Maybe add diagrams later to show the flow of sets vs no sets -->
  </section>
  <section xml:id="ha-resources-constraints-what-is-more-info">
    <title>For more information</title>
    <itemizedlist>
      <listitem>
        <para>
          Constraints: <link xlink:href="https://clusterlabs.org/projects/pacemaker/doc/3.0/Pacemaker_Explained/html/constraints.html"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Constraints for clones: <link xlink:href="https://clusterlabs.org/projects/pacemaker/doc/3.0/Pacemaker_Explained/html/collective.html#clone-constraints"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Scores: <link xlink:href="https://clusterlabs.org/projects/pacemaker/doc/3.0/Pacemaker_Explained/html/local-options.html#scores"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Resource sets: <link xlink:href="https://clusterlabs.org/projects/pacemaker/doc/3.0/Pacemaker_Explained/html/constraints.html#resource-sets"/>
        </para>
      </listitem>
    </itemizedlist>
  </section>
</topic>
