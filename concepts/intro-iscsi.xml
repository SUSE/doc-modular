<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="about-iscsi"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>What is iSCSI and how does it work?</title>
    <meta name="maintainer" content="amrita.sakthivel@suse.com" its:translate="no"/>
    <abstract>
      <para>
 iSCSI is a networking protocol that enables the transmission of SCSI commands over 
 standard IP networks. It enables a host system (the initiator) to access remote 
 block-level storage on a server (the target) using standard Ethernet cables and switches. 
 The client operating system formats and manages the assigned remote disk space&mdash;known 
as a Logical Unit Number (LUN)&mdash;using native file systems like Btrfs, XFS or ext4. 
      </para>
    </abstract>
  </info>
    <para>
   By wrapping storage-level data blocks into standard TCP/IP packets, it bridges 
   the gap between local storage performance and network flexibility, using existing 
   Ethernet infrastructure like switches and cables rather than expensive, specialized hardware. 
   In an iSCSI setup, the initiator connects to a target to claim a portion of disk space known 
   as a LUN (Logical Unit Number), which the client can then format and manage with its own 
   native file system.
   Note that LU (Logical Unit) is the data container or virtual disk and LUN (Logical Unit Number) 
   is the numerical address of the container.
   Linux iSCSI provides iSCSI initiator and iSCSI LIO (Linux-IO) target software for connecting 
   Linux servers to central storage systems.
  </para>
  <section xml:id="what-is">
    <title>iSCSI core architecture and components</title>
    <para>The iSCSI architecture relies on two primary entities: the initiator (the client 
      requesting storage) and the target (the server serving storage). Storage capacity 
      is presented as LUNs.</para>
    <para>In a physical iSCSI environment, the initiator is the server’s software that 
      requests data, the LIO target is the physical storage appliance that hosts the 
      data and the LUN (Logical Unit Number) represents the specific physical disk 
      capacity assigned to the server.</para>
    <itemizedlist>
  <listitem><para><emphasis role="bold">The iSCSI initiator:</emphasis> The initiator is the physical server that needs to consume the storage. It initiates the conversation with the target.</para>
  <itemizedlist>
    <listitem><para><emphasis role="bold">HBA (host bus adapter):</emphasis> While you can use a standard network card, a physical iSCSI HBA is a dedicated expansion card installed in the server's PCIe slot. It contains its own processor to handle storage traffic and prevents the server’s main CPU from becoming overwhelmed.</para></listitem>
    <listitem><para><emphasis role="bold">Physical ports:</emphasis> These are the dedicated connection points on the server used exclusively for storage traffic to ensure low latency and high throughput.</para></listitem>
  </itemizedlist>
    </listitem>
    <listitem><para><emphasis role="bold">The iSCSI LIO target:</emphasis> The target is the physical hardware device that houses the data. LIO (Linux-IO) is the standard, open-source storage enterprise-grade target for the Linux kernel.</para>
     </listitem>
    <listitem><para><emphasis role="bold">The LUN:</emphasis> While a LUN is a logical identifier, it directly corresponds to the physical disk resources allocated to a server.</para>
    <itemizedlist>
      <listitem><para><emphasis role="bold">Physical disk groups:</emphasis> A LUN is often a physical collection of disks bound together in a RAID configuration.</para></listitem>
      <listitem><para><emphasis role="bold">The drive to the OS:</emphasis> From the server's perspective, the LUN is treated as a physical local hard drive. You can format, partition and install files on it just like a drive plugged directly into the motherboard.</para></listitem>
      <listitem><para><emphasis role="bold">Physical capacity:</emphasis> The size of the LUN is limited by the number of physical disks installed in the target array.</para></listitem>
    </itemizedlist>
    </listitem>
    </itemizedlist>
    </section>
    <section xml:id="features-iscsi">
      <title>Key technical characteristics of remote iSCSI storage</title>
      <para>The following are some of the common characteristics of iSCSI:</para>
      <itemizedlist>
      <listitem><para><emphasis role="bold">Block-level access:</emphasis> Unlike a network share where you see files, with iSCSI, your system sees raw sectors. This allows you to format the remote drive with any file system such as NTFS, ReFS, or ext4.</para></listitem>
      <listitem><para><emphasis role="bold">Distance and flexibility:</emphasis> The storage can be in a server rack across the room or in a data center across the city. Because iSCSI uses block-level storage, the system treats this network-attached storage exactly like a local hard drive.
        When traversing these distances, security is maintained by enforcing CHAP for secure authentication between the system and the storage target, alongside IPsec to encrypt the data packets in transit.</para></listitem>
      <listitem><para><emphasis role="bold">Low latency requirement:</emphasis> Because the OS thinks the drive is local, it expects fast responses. This is why remote iSCSI usually requires a high-speed network ( preferably 10Gbps or higher).</para></listitem>
    </itemizedlist>
        </section>
        <section xml:id="usecase-iscsi">
          <title>What are the main use cases for iSCSI storage?</title>
          <para>The common use cases for iSCSI include:</para>
          <itemizedlist>
          <listitem><para><emphasis role="bold">Virtualization:</emphasis> Servers running VMware or Hyper-V store virtual machines on remote iSCSI targets so that if one physical server dies, another can immediately take over the disk over the network.</para></listitem>
          <listitem><para><emphasis role="bold">Database hosting:</emphasis> High-performance databases like SQL Server or Oracle require block-level access to manage data placement and logging. Since iSCSI presents itself as a local physical disk rather than a shared folder, it satisfies the strict technical requirements of database software while keeping the data on a secure and centralized server.</para></listitem>
          <listitem><para><emphasis role="bold">Consolidated backup and disaster recovery:</emphasis> iSCSI allows organizations to centralize backups from various machines onto one large and manageable storage array. It also supports diskless booting, where a server with no internal hard drive can load its entire operating system directly from a remote iSCSI target, making hardware replacement much faster.</para></listitem>
          <listitem><para><emphasis role="bold">External storage array integration:</emphasis> SLES 16 connects to high-performance external storage systems, like NetApp, to centralize data management over iSCSI. Instead of using internal drives, administrators provision network-based virtual disks (LUNs) that &sles; 16 handles via the <literal>open-iscsi</literal> framework. The OS leverages kernel multi-queueing for low-latency throughput and uses <literal>multipath-tools</literal> to combine multiple network paths for redundancy. This setup eliminates single points of failure, allowing the remote storage to behave like a local disk that can be formatted with Btrfs or XFS and scaled dynamically without downtime.</para></listitem>
            </itemizedlist>
            <figure xml:id="iscsi-workflow">
              <title>iCSI topology</title>
              <mediaobject>
                <imageobject role="html">
                  <imagedata fileref="iscsi-workflow.svg" width="50%"/>
                </imageobject>
                <imageobject role="fo">
                  <imagedata fileref="iscsi-workflow.svg" width="50%"/>
                </imageobject>
                <textobject role="description">
                  <phrase> iSCSI Target and Initiator Architecture Topology</phrase>
                </textobject>
              </mediaobject>
            </figure>
            </section>
      </topic>