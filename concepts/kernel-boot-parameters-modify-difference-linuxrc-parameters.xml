<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="kernel-boot-parameters-modify-difference-linuxrc-parameters"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Difference between kernel boot parameters and linuxrc parameters</title>
    <!-- can be changed via merge in the assembly -->
    <!--add author's email address-->
    <meta name="maintainer" content="ssarkar@suse.com" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        Older &suse; documentation distinguishes kernel boot parameters from
        <literal>linuxrc</literal> parameters. On &productname; 16, that distinction no longer applies to
        an installed system: <literal>linuxrc</literal> is gone, and the parameters that used to
        be associated with it are ordinary kernel boot parameters handled by dracut.
      </para>
    </abstract>
  </info>
  <section xml:id="kernel-boot-parameters-modify-linuxrc-parameter-introduction">
    <title>What happened to linuxrc on &productnameshort; 16?</title>
    <para>
      <literal>linuxrc</literal> was the boot-time parameter parser used by the installer on
      earlier &productnameshort; releases. It ran only during installation, before the target
      system's root file system existed, so it played no part in an already-installed system. It
      is not present on &productnameshort; 16 at all: &productnameshort; 16 installs exclusively with &agama;, which does not use
      <literal>linuxrc</literal>. &agama; provides a compatibility layer for a handful of
      <literal>linuxrc</literal> option names, for administrators migrating scripts or profiles
      built around the old parser. For the installer-time options and the compatibility mapping,
      see <xref linkend="kernel-boot-parameters-modify-more-info"/>.
    </para>
    <para>
      What remains is dracut, the framework that builds the
      initial RAM disk (initramfs). It brings up the environment the kernel needs before the real
      root file system is mounted, such as loading storage drivers or unlocking encrypted
      devices. Unlike <literal>linuxrc</literal>, dracut is not specific to installation. It also
      runs on every boot of an installed system.
    </para>
    <para>
      The options that configure dracut are not a separate parameter category with their own
      syntax. They are ordinary kernel boot parameters, entered on the same &grub; command line
      and following the same rules described in this article. Among these, dracut simply acts on
      the ones it recognizes before handing off to the rest of the boot process.
    </para>
  </section>
  <section xml:id="kernel-boot-parameters-modify-linuxrc-parameter-examples">
    <title>Examples of dracut parameters</title>
    <para>
      The following kernel boot parameters are handled by dracut during the initrd stage, before
      the root file system is mounted:
    </para>
    <itemizedlist>
      <listitem>
        <para>
          <literal>rd.driver.blacklist=</literal>: Specifies drivers to be blocked in the initrd.
          For example, <literal>rd.driver.blacklist=nouveau</literal> prevents the
          <literal>nouveau</literal> graphics driver from loading during the early stages of boot.
          This can be useful when troubleshooting driver-related boot issues.
        </para>
      </listitem>
      <listitem>
        <para>
          <literal>rd.break</literal>: Interrupts the boot process and drops the system to a shell
          within the initrd for debugging purposes. This parameter is helpful for diagnosing issues
          related to the early boot process, such as driver loading or file system mounting errors.
        </para>
      </listitem>
      <listitem>
        <para>
          <literal>rd.retry=</literal>: Specifies how long, in seconds, dracut retries configuring
          devices before giving up. For example, <literal>rd.retry=240</literal> extends the
          default 180-second timeout, which can help on hardware that takes longer than usual to
          initialize storage devices.
        </para>
      </listitem>
      <listitem>
        <para>
          <literal>rd.luks=0</literal>: Disables LUKS-encrypted device detection in the initrd.
          Detection is enabled by default on a system with an encrypted root file system. Use
          this parameter only if you need to turn it off, for example while troubleshooting an
          unrelated storage issue.
        </para>
      </listitem>
    </itemizedlist>
    <para>
      Because these are kernel boot parameters like any other, the temporary and permanent
      modification methods described elsewhere in this article apply to them. They work the same
      way as for <parameter>quiet</parameter> or <parameter>nomodeset</parameter>.
    </para>
  </section>
</topic>
