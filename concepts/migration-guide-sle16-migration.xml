<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
<!ENTITY % entities SYSTEM "../common/generic-entities.ent">
%entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="migration-guide-sle16-migration"
       role="concept" xml:lang="en"
       xmlns="http://docbook.org/ns/docbook" version="5.2"
       xmlns:its="http://www.w3.org/2005/11/its"
       xmlns:xi="http://www.w3.org/2001/XInclude"
       xmlns:xlink="http://www.w3.org/1999/xlink"
       xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>How to migrate from &slea;&nbsp;15 to &suselinux;&nbsp;16</title>
    <!-- can be changed via merge in the assembly -->
    <!--add author's email address-->
    <meta name="maintainer" content="daria.vladykina@suse.com" its:translate="no"/>
    <!--abstract>
      <para>
      Let us dive into the foundational changes affecting system services, core libraries, and essential utilities.
      </para>
      </abstract-->
  </info>
  <para>
    Migration from &slea;&nbsp;15 to &suselinux;&nbsp;16 uses the Distribution Migration System that
    replaces the previous Unified Installer approach. The migration is performed over the network 
    using the &zypper; migration workflow. The Distribution Migration System provides a bootable 
    live migration image and a start-up utility, <literal>run_migration</literal>, which reboots 
    the running system into the migration environment and executes the migration in stages:
  </para>
  <itemizedlist>
    <listitem>
      <para>
        Pre-migration: The migration image boots and analyses the existing installation. 
        It detects the system to be upgraded, mounts the required file systems, configures the network 
        to match the existing system and prepared SSH access. Settings that cannot be 
        converted automatically (such as &aa; profiles) are flagged. If an error occurs before 
        the migration begins, the system reverts to its original state.
      </para>
    </listitem>
    <listitem>
      <para>
        Migration: The migration environment prepares the package manager and upgrades the system using &zypper;.
      </para>
    </listitem>
    <listitem>
      <para>
        Post-migration: The workflow updates the boot loader configuration, unmounts all file systems 
        used during migration, and reboots into the newly upgraded operating system. 
        After the first boot, the migrated system can run optional post-migration finalization tasks.
      </para>
    </listitem>
  </itemizedlist>
  <para>While &suselinux;&nbsp;16.0 featured a limited migration scope, &suselinux;&nbsp;&productnumber;
  provides full migration support, including additional source releases. This enables migrations from 
    &slea;&nbsp;15&nbsp;SP5 and later.</para>
  <para>Upgrading to a new major version requires the system to be offline. Migration of a running 
    system is no longer supported.
</para>
  
</topic>
