<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
<!ENTITY % entities SYSTEM "../common/generic-entities.ent">
%entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="migration-guide-sle16-security"
       role="concept" xml:lang="en"
       xmlns="http://docbook.org/ns/docbook" version="5.2"
       xmlns:its="http://www.w3.org/2005/11/its"
       xmlns:xi="http://www.w3.org/2001/XInclude"
       xmlns:xlink="http://www.w3.org/1999/xlink"
       xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>What are the new security features and sudo policies?</title>
    <!-- can be changed via merge in the assembly -->
    <!--add author's email address-->
    <meta name="maintainer" content="daria.vladykina@suse.com" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        &suselinux;&nbsp;16 tightens security at every level&nbsp;replacing &aa; with a fully
        enforced &selnx; policy, restricting remote root access, and hardening privilege escalation
        defaults.
      </para>
    </abstract>
  </info>
  <section xml:id="migration-security-selinux">
    <title>&selnx; replaces &aa;</title>
    <para>
      &slea;&nbsp;15 used &aa; by default and shipped &selnx; without policies. &suselinux;&nbsp;16
      makes a definitive switch:
    </para>
    <itemizedlist>
      <listitem>
        <para>
          &aa; is removed.
        </para>
      </listitem>
      <listitem>
        <para>
          &selnx; is enabled and set to enforcing mode by default, with policies covering more than
          400 modules&nbsp;confining nearly the entire system.
        </para>
      </listitem>
      <listitem>
        <para>
          Parts of the system can run unconfined if required.
        </para>
      </listitem>
      <listitem>
        <para>
          On &sles4sap;&nbsp;16, &selnx; provides workload isolation for data, data flows and
          containers. When configuring &suselinux; or &s4s; to run &sap;, &selnx; is automatically
          set to permissive mode.
        </para>
      </listitem>
    </itemizedlist>
    <para>
      For full details, see
      <link xlink:href="https://documentation.suse.com/sles/16.0/html/SLES-SELinux/">Understanding
      SELinux Basics</link>.
    </para>
  </section>
  <section xml:id="migration-security-ssh">
    <title>How to manage remote root login via SSH</title>
    <para>
      Physical console login as root is unchanged. For SSH access, password-based root login is
      disabled by default. To re-enable it, install the
      <literal>openssh-server-config-rootlogin</literal> package. Key-based root login (via
      <filename>/root/.ssh/authorized_keys</filename>) continues to work.
    </para>
  </section>
  <section xml:id="migration-security-sudo">
    <title>What are the new sudo password policies?</title>
    <para>
      In &slea;&nbsp;15, &sudo; prompted for the target user's password. &suselinux;&nbsp;16
      tightens this:
    </para>
    <itemizedlist>
      <listitem>
        <para>
          The first user created during installation is added to the wheel group.
        </para>
      </listitem>
      <listitem>
        <para>
          Users in the wheel group are prompted for their own password when using <command>sudo
          -i</command>, <command>kexec</command> or <command>polkit</command>.
        </para>
      </listitem>
      <listitem>
        <para>
          Users not in the wheel group are prompted for the &rootuser; password.
        </para>
      </listitem>
    </itemizedlist>
    <para>
      This policy is implemented by the <literal>sudo-policy-wheel-auth-self</literal> package,
      which is installed by default on new &suselinux;&nbsp;16 systems.
    </para>
  </section>
  <section xml:id="migration-security-yama">
    <title>Yama security enhancements</title>
    <para>
      &suselinux;&nbsp;16 now supports the Yama security module, which enhances 
      system-wide security by restricting the use of the <literal>ptrace</literal> system call. 
      By introducing configurable scopes that control which processes can observe 
      or manipulate others, Yama addresses a fundamental weakness in the traditional 
      Linux process model and prevents lateral movement by attackers. This ensures 
      that compromised low-privilege applications cannot easily steal sensitive data 
      from other processes running in the same session. For more details on how to enable, 
      configure and troubleshoot Yama, see 
      <link xlink:href="https://documentation.suse.com/sles/16.0/html/SLES-lsm/">Hardening Linux 
      with Linux Security Module Framework and Yama</link>.
    </para>
  </section>
</topic>