<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<topic xml:id="minimal-vm-highlights"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>&minvm; highlights</title>
    <meta name="maintainer" content="dpopov@suse.com" its:translate="no"/>
  </info>
  <itemizedlist>
    <listitem>
     <para>
      &minvm; is available as preconfigured and ready-to-run virtual
      machine images. 
     </para>
    </listitem>
    <listitem>
      <para>
       While you can deploy the provided images as-is, it is also possible to
       create customized &minvm; images using  the supplied &kiwi; templates.
      </para>
     </listitem>
    <listitem>
     <para>
      The supplied <systemitem>jeos-firstboot</systemitem> wizard runs on the
      first boot and allows you to configure basic settings, such as system
      locales and the &rootuser; password.
     </para>
     <para>
      Instead of <systemitem>jeos-firstboot</systemitem>, &minvm; for &ostack;
      uses <systemitem>cloud-init</systemitem> for configuring &ostack; instances.
     </para>
    </listitem>
    <listitem>
     <para>
      You can establish an SSH connection to the running &minvm; instance
      without any further configuration.
     </para>
    </listitem>
    <listitem>
     <para>
      The default network configuration is set to use DHCP.
     </para>
    </listitem>
    <listitem>
      <para>
       The system automatically creates a Btrfs snapshot after the initial configuration has been completed. This means that you can easily revert all subsequent
       changes, if necessary.
      </para>
     </listitem>
    <listitem>
     <para>
      &minvm; images use the
      <systemitem>kernel-default-base</systemitem> as the default kernel, which
      results in a smaller footprint.
     </para>
    </listitem>
    <listitem>
    <para>
    As &opensuse; Leap &minvm; and &minvm; are similar, it is easy to migrate from the former to the latter.
    </para>
    </listitem>
   </itemizedlist>
</topic>
