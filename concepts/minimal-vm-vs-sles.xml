<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<topic xml:id="minimal-vm-vs-sles"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Differences between &sleminvm; and &productname;</title>
    <meta name="maintainer" content="eliska.romanova@suse.com" its:translate="no"/>
    <abstract>
      <para>
        This section outlines the primary configuration, kernel and storage differences between
        &minvm; images and standard &productnameshort; deployments
      </para>
    </abstract>
  </info>
  <para>
    &sleminvm; is built for a small footprint and unattended deployment, whereas &productname; is
    installed interactively and ships a complete package set. The following differences affect how
    you deploy and operate the system.
  </para>
  <variablelist>
    <varlistentry>
      <term>No installation step</term>
      <listitem>
        <para>
          There is no installer to run. The images are ready to boot, and basic settings such as
          keyboard layout, time zone and &rootuser; password are configured on first boot by the
          &jeosfirstboot; tool, or by <literal>cloud-init</literal> on the cloud image.
        </para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>No repositories by default</term>
      <listitem>
        <para>
          The &minvm; images contain no repositories. Register the system to access online
          repositories. Registration requires a paid subscription.
        </para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>Reduced kernel</term>
      <listitem>
        <para>
          To keep the image small, &minvm; uses <package>kernel-default-base</package> as the
          default kernel. This kernel does not contain drivers for bare metal. If your scenario
          requires additional drivers, install the <package>kernel-default</package> package to get
          the full functionality. This is the kernel used by a regular installation.
        </para>
      </listitem>
    </varlistentry>
    <varlistentry os="sles4sap">
      <term>Preinstalled &sap; patterns</term>
      <listitem>
        <para>
          The images come with the &sap; base pattern preinstalled, so the system is already
          prepared to receive an &sap; workload. To list the installed patterns, run the following
          command:
        </para>
<screen>&prompt.sudo;<command>zypper search --type pattern --installed-only</command></screen>
        <para>
          The output includes <literal>sles_sap_base_sap_server</literal> and
          <literal>sles_sap_minimal_sap</literal>, alongside the base and &selnx; patterns.
        </para>
      </listitem>
    </varlistentry>
    <varlistentry os="sles4sap">
      <term>Automatic base tuning</term>
      <listitem>
        <para>
          Because the &sap; pattern pulls in &saptune;, base tuning is applied automatically. On
          installation, &saptune; applies the <literal>SAP_Base</literal> solution, which
          implements the recommendations of <link xlink:href="https://me.sap.com/notes/1275776">
          &sapnote; 1275776</link>.
        </para>
        <note>
          <title>Workload-specific tuning solutions</title>
          <para>
            Base tuning is a starting point only. Apply the solution that matches your workload,
            for example <literal>HANA</literal>, <literal>NETWEAVER</literal>,
            <literal>S4HANA-APPSERVER</literal> or <literal>S4HANA-APP+DB</literal>. Only one
            solution can be active at a time. For more information, see
            <link xlink:href="https://documentation.suse.com/sles-sap/16.0/html/SAP-saptune/"> How
            to Tune Systems with &saptune;</link>.
          </para>
        </note>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>&zypper; configuration</term>
      <listitem>
        <para>
          Part of the size reduction comes from two options in
          <filename>/etc/zypp/zypp.conf</filename>:
        </para>
        <itemizedlist>
          <listitem>
            <para>
              <literal>rpm.install.excludedocs = yes</literal> excludes all files marked as
              documentation from installation.
            </para>
          </listitem>
          <listitem>
            <para>
              <literal>solver.onlyRequires = true</literal> disables installation of suggested and
              recommended packages.
            </para>
          </listitem>
        </itemizedlist>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>Base packages</term>
      <listitem>
        <para>
          The images are built from &kiwi; templates that list the packages to include. Keep in
          mind that packages listed in a template can pull in dependencies during the build, so an
          image usually contains more packages than the template names.
        </para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>Predefined disk layout</term>
      <listitem>
        <para>
          Unlike &productname;, which calculates an adjustable disk layout proposal during
          installation, &sleminvm; uses a predefined virtual image partitioning scheme:
        </para>
        <itemizedlist>
          <listitem>
            <para>
              2&nbsp;MB BIOS boot
            </para>
          </listitem>
          <listitem>
            <para>
              512&nbsp;MB EFI system
            </para>
          </listitem>
          <listitem>
            <para>
              24&nbsp;GB &rootuser; file system in non-cloud deployments
            </para>
          </listitem>
          <listitem>
            <para>
              A dynamically sized &rootuser; file system in cloud deployments
            </para>
          </listitem>
        </itemizedlist>
        <para os="sles4sap">
          The &rootuser; file system holds the operating system and is not sized for &sap; data.
          Before you deploy an &sap; workload, add storage for the &sap; file systems, for example,
          <literal>/usr/sap</literal>, <literal>/hana/data</literal>, <literal>/hana/log</literal>
          and <literal>/hana/shared</literal>, and configure swap according to the &sap; sizing
          recommendations for your workload.
        </para>
        <para>
          To add space, either extend the existing disk from the virtual host, or add a secondary
          disk to the virtual machine. If you add a secondary disk, you must partition and format
          it manually.
        </para>
      </listitem>
    </varlistentry>
  </variablelist>
</topic>
