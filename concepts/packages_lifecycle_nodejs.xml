<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="packages-lifecycle-nodejs"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title><literal>node.js</literal> update strategy</title><!-- can be changed via merge in the assembly -->
    <!--add author's email address-->
    <meta name="maintainer" content="jsindelarova@suse.com" its:translate="no"/>
    <abstract><!-- can be changed via merge in the assembly -->
      <para>
       <literal>Node.js</literal> is an open source, cross-platform JavaScript runtime environment that allows developers to build and run server-side applications outside of a web browser. It is built on Google Chrome's V8 engine and uses an event-driven, non-blocking I/O model, making it highly efficient for creating scalable network applications.
      </para>
    </abstract>
  </info>
  <para>
   On &suselinux; 16, <literal>node.js</literal> is updated and supported according to the following rules:
  </para>
  <itemizedlist>
    <listitem>
      <para>
        The <literal>node.js</literal> version is kept in sync with the upstream LTS version.
      </para>
    </listitem>
    <listitem>
      <para>
        Each &suselinux; 16 minor release has the <link
        xlink:href="https://nodejs.org/en/about/previous-releases">latest Active LTS version</link>,
        starting with <literal>nodejs 22</literal>, and comes with a default version of <literal>node.js</literal>.
      </para>
    </listitem>
    <listitem>
      <para>
All <literal>node.js</literal> minor releases are available generally after security fixes or on request.
      </para>
    </listitem>
    <listitem>
      <para>
       Different <literal>node.js</literal> versions can be installed in parallel.
      </para>
    </listitem>
    <listitem>
      <para>
        Versions in a specific &suselinux; minor release are supported until the end of LTS.
      </para>
    </listitem>
    <listitem>
      <para>
        To always use the most recent recommended version available in the repository, applications should use <literal>BuildRequires: nodejs &gt;= 22</literal>.
      </para>
    </listitem>
  </itemizedlist>
</topic>
