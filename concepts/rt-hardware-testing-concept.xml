<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
<!ENTITY % entities SYSTEM "../common/generic-entities.ent">
%entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="rt-hardware-testing-concept"
       role="concept" xml:lang="en"
       xmlns="http://docbook.org/ns/docbook" version="5.2"
       xmlns:its="http://www.w3.org/2005/11/its"
       xmlns:xi="http://www.w3.org/2001/XInclude"
       xmlns:xlink="http://www.w3.org/1999/xlink"
       xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Understanding real-time hardware latency testing</title>
    <meta name="maintainer" content="daria.vladykina@suse.com" its:translate="no"/>
    <abstract>
      <para>
        Real-time hardware capabilities require predictable response times within a specified
        margin. This section explains fundamental real-time concepts and the sources of platform
        interference. It details how to distinguish hardware-induced delays from the scheduling
        overhead of the operating system using latency tools. Understanding these concepts helps
        you isolate low-level firmware bottlenecks before deploying production workloads.
      </para>
    </abstract>
  </info>
  <para>
    You must distinguish between hardware-induced and operating-system-induced latencies. The
    hardware latency detector runs independently of the Linux kernel scheduler. It polls the CPU
    Time Stamp Counter (TSC) to find gaps. These gaps indicate when the hardware or firmware
    disturbed the CPU.
  </para>
  <para>
    Conversely, <systemitem>cyclictest</systemitem> measures operating system latencies. It runs
    closer to a real computing situation by including operating system overhead in its results.
  </para>
  <para>
    <systemitem>cyclictest</systemitem> measures a superset of what the hardware latency detector
    measures, plus the scheduling and interrupt handling overhead added by the operating system. On
    a correctly tuned system, expect <systemitem>cyclictest</systemitem> to report somewhat higher
    latencies than the hardware latency detector, because it includes this additional software
    overhead. If <systemitem>cyclictest</systemitem> reports latencies that are much higher, for
    example, hundreds of microseconds or more where the hardware latency detector reports only a
    few microseconds, this points to a source of latency in the operating system rather than in the
    hardware. In that case, contact &suse; for support.
  </para>
</topic>
