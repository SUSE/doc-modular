<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="zypper-immutable-modifying-system"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Modifying the read-only system</title><!-- can be changed via merge
in the assembly -->
    <!-- add author's e-mail -->
    <meta name="maintainer" content="jsindelarova@suse.com" its:translate="no"/>
    <abstract><!-- can be changed via merge in the assembly -->
      <para>
      Without the transactional wrapper, the system modification commands like <command>zypper install</command>, <command>zypper patch</command>, or <command>zypper update</command> fail and the user is instructed to use the &tr-up; command instead.
      </para>
    </abstract>
  </info> 
  <para>
  When the transactional wrapper is enabled, <command>zypper</command> detects the immutable mode and spawns the transactional wrapper as a child process to execute the requested write operations. The transactional wrapper retrieves the exact command-line arguments of its parent process directly from the <filename>/proc</filename> file system. This ensures the command is replicated perfectly without requiring any syntax changes from the user.
  </para>
  <para>
  The transactional wrapper is usually installed and enabled by default. If it is not present on your system, see the following section for installation instructions. </para>
</topic>
