<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="zypper-subcommands"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>&zypper; subcommands</title>
    <!--add author's email address-->
    <meta name="maintainer" content="cwickert@suse.com" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        &zypper; can be extended with subcommands.
      </para>
    </abstract>
  </info>
  <section xml:id="what-are-zypper-subcommands">
    <title>What are &zypper; subcommands?</title>
    <para>
      &zypper; subcommands are executables that are stored in the directory
      <filename>/usr/lib/zypper/commands</filename>. If a subcommand is not found in this directory,
      &zypper; automatically searches the rest of your <envar>$PATH</envar> for it. This enables
      writing custom local extensions and storing them in userspace.
    </para>
  </section>
  <section xml:id="how-do-zypper-subcommands-work">
    <title>How do &zypper; subcommands work?</title>
    <para>
      &zypper; subcommands work like regular zypper commands. The only exception is that executing
      subcommands in the &zypper; shell, and using global &zypper; options are not supported.
    </para>
  </section>
  <section xml:id="using-zypper-subcommands">
    <title>Using Zypper subcommands</title>
    <para>
      To list available subcommands, run:
    </para>
<screen>&prompt.user;zypper subcommand help
&lt;subcommand&gt; [--command-options] [arguments]

Zypper subcommands are standalone executables that live in the
zypper_execdir ('/usr/lib/zypper/commands').

For subcommands zypper provides a wrapper that knows where the
subcommands live, and runs them by passing command-line arguments
to them.

If a subcommand is not found in the zypper_execdir, the wrapper
will look in the rest of your $PATH for it. Thus, it's possible
to write local zypper extensions that don't live in system space.

Using zypper global-options together with subcommands, as well as
executing subcommands in 'zypper shell' is currently not supported.

Available zypper subcommands in '/usr/lib/zypper/commands':

      lifecycle             products and packages lifecycle information


Zypper subcommands available from elsewhere on your $PATH:

      &lt;none&gt;


Type 'zypper help &lt;subcommand&gt;' to get subcommand-specific help if available.
</screen>
    <para>
      View the help screen for a subcommand:
    </para>
<screen>&prompt.user;zypper help appstream-cache</screen>
  </section>
</topic>
