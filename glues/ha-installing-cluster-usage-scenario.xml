<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/article_installation.xml -->

<topic xml:id="ha-installing-cluster-usage-scenario"
 role="glue" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Usage scenario</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
  </info>
  <para>
    This guide describes the setup of a minimal &ha; cluster with the
    following properties:
  </para>
  <itemizedlist>
    <listitem>
      <para>
        Three cluster nodes. Three nodes are required for this setup so that diskless &sbd; can
        handle split-brain scenarios without the help of &qdevice;.
      </para>
    </listitem>
    <listitem>
      <para>
        A floating, virtual IP address that allows clients to connect to a service no matter which
        node the service is running on. This IP address is used to connect to the graphical
        management tool &hawk;.
      </para>
    </listitem>
    <listitem>
      <para>
        Diskless &sbd; (&sbd.long;) and a software watchdog used as the node fencing mechanism
        to avoid split-brain scenarios.
      </para>
    </listitem>
    <listitem>
      <para>
        Failover of resources from one node to another if the active host breaks down
        (<emphasis>active/passive</emphasis> setup).
      </para>
    </listitem>
  </itemizedlist>
  <para>
    This is the simplest supported cluster setup with the fewest external requirements.
    You can use this cluster for testing purposes or as a minimal cluster configuration that you
    can extend later. Before using the cluster in a production environment, modify the cluster
    according to your requirements.
  </para>
  <orderedlist>
    <title>Overview of procedures</title>
    <listitem>
      <para>
        Review <xref linkend="ha-requirements"/> to make sure you have everything you need to
        install a &sleha; cluster.
      </para>
    </listitem>
    <listitem>
      <para>
        Enable and register &sleha; with <xref linkend="ha-enabling-extension"/>.
        <!--If you still need to install &sles;, see this link that doesn't exist yet instead.-->
      </para>
    </listitem>
    <listitem>
      <para>
        Initialize the cluster on the first node with <xref linkend="ha-setting-up-first-node"/>.
      </para>
    </listitem>
    <listitem>
      <para>
        Add more nodes to the cluster with <xref linkend="ha-adding-nodes"/>.
      </para>
    </listitem>
    <listitem>
      <para>
        Log in to the &hawk; Web interface to monitor the cluster with <xref linkend="ha-logging-in-hawk"/>.
      </para>
    </listitem>
    <listitem>
      <para>
        Perform basic tests to make sure the cluster works as expected with <xref linkend="ha-testing-the-cluster-basic"/>.
      </para>
    </listitem>
        <listitem>
      <para>
        Review <xref linkend="ha-installing-cluster-next-steps"/> for advice on expanding the cluster for a production environment.
      </para>
    </listitem>
  </orderedlist>
</topic>
