   conky.config = {
       -- Configuration options
       ...
   }

    conky.text = [[
    ${color white}CPU Temperature:${color} ${execi 10 sensors | grep 'Package\|Core 0' | awk '{print \$3}'}
   ]]
