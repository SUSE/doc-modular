<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="agama-boot-options"
 role="reference" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>&agama; boot options</title>
    <!-- can be changed via merge in the assembly -->
    <meta name="maintainer" content="souvik.sarkar@suse.com" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        The behavior of the &agama; Live ISO can be altered using the kernel command line at boot
        time. On architectures that support &grub;, modify the <literal>agama-installer</literal>
        boot entry, adding the options below at the end of its <literal>linux</literal> line.
      </para>
    </abstract>
  </info>
  <section xml:id="agama-boot-options-profile">
    <title>Specifying and starting an unattended installation</title>
    <variablelist>
      <varlistentry>
        <term><literal>inst.auto</literal></term>
        <listitem>
          <para>
            Tells the installer to use the profile at the given URL to start an unattended
            installation.
          </para>
<screen>inst.auto=<replaceable>http://mydomain.org/profile.jsonnet</replaceable></screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.auto_insecure</literal></term>
        <listitem>
          <para>
            Ignores SSL/TLS problems, such as a self-signed certificate, when downloading the
            profile.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.info</literal></term>
        <listitem>
          <para>
            Points to an info file containing additional &agama; settings. Useful when you need to
            provide more options than are practical to type at the boot prompt. Only parameters
            documented in this section are supported. Kernel parameters in the file are ignored.
          </para>
<screen>inst.info=<replaceable>ftp://myserver/devel.info</replaceable></screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.install</literal></term>
        <listitem>
          <para>
            In unattended mode, &agama; automatically starts the installation after reading the
            profile. Set this to <literal>0</literal> to stop after reading the profile instead,
            giving you a chance to review the installation options before proceeding.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.finish</literal></term>
        <listitem>
          <para>
            Changes &agama;'s behavior when the installation finishes:
          </para>
          <itemizedlist>
            <listitem>
              <para>
                <literal>stop</literal>: do nothing (default for interactive installation). The web
                interface shows a button to reboot and a chance to download the logs.
              </para>
            </listitem>
            <listitem>
              <para>
                <literal>reboot</literal>: reboot the system (default for unattended installation).
              </para>
            </listitem>
            <listitem>
              <para>
                <literal>halt</literal>: halt the system.
              </para>
            </listitem>
            <listitem>
              <para>
                <literal>poweroff</literal>: power off the system.
              </para>
            </listitem>
          </itemizedlist>
<screen>inst.finish=poweroff</screen>
        </listitem>
      </varlistentry>
    </variablelist>
  </section>
  <section xml:id="agama-boot-options-scripts-driver-updates">
    <title>Pre-boot scripts and driver updates</title>
    <variablelist>
      <varlistentry>
        <term><literal>inst.script</literal></term>
        <listitem>
          <para>
            URL of an installation script that runs during boot, before <literal>inst.auto</literal>
            (if given) is processed. Can perform preparation work or tweak the installation
            workflow using &agama;'s command-line interface.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.script_insecure</literal></term>
        <listitem>
          <para>
            Ignores SSL/TLS problems, such as a self-signed certificate, when downloading the
            installation script.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.dud</literal></term>
        <listitem>
          <para>
            Applies a Driver Update (DUD) to the installation environment, to patch or extend the
            installer. Supported formats include RPM packages and DUD archives created with
            <literal>mkdud</literal>. Can be specified multiple times, with each entry processed
            independently. This option performs no dependency checks or signature validation on
            the provided update.
          </para>
<screen>inst.dud=<replaceable>https://download.opensuse.org/some-project/some.rpm</replaceable>
inst.dud=<replaceable>label://UPDATES/package.rpm</replaceable></screen>
          <para>
            Besides patching the live image or adding kernel modules, a DUD can also carry a
            partial &agama; profile: place an <filename>autoinst.jsonnet</filename>,
            <filename>autoinst.json</filename>, or <filename>autoinst.xml</filename> file in the
            DUD's <filename>inst-sys</filename> directory, and &agama; copies and processes it
            automatically, in addition to any profile given via <literal>inst.auto</literal>. This
            is useful for bundling installation scripts or settings together with a live-image
            patch or replacement package, without a separate profile download.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.dud_insecure</literal></term>
        <listitem>
          <warning>
            <title>Security risk</title>
            <para>
              Disabling SSL/TLS checks is a security risk. An attacker could tamper with the
              downloaded DUD image and inject malicious code into the system, or cause vulnerable
              packages to be installed.
            </para>
          </warning>
          <para>
            Ignores SSL/TLS problems, such as a self-signed certificate, when downloading the DUD
            image from an HTTPS server.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.dud_packages.gpg</literal></term>
        <listitem>
          <warning>
            <title>Security risk</title>
            <para>
              Installing unknown packages is a security risk. Malicious code could be installed on
              your system.
            </para>
          </warning>
          <para>
            Set to <literal>0</literal> to ignore package GPG signature problems when installing
            DUD packages to the target system. Applies only to DUD packages; GPG problems for other
            packages are still detected and reported.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>
  </section>
  <section xml:id="agama-boot-options-network-registration">
    <title>Network, registration, and time</title>
    <variablelist>
      <varlistentry>
        <term><literal>inst.copy_network</literal></term>
        <listitem>
          <para>
            Whether the persistent &nm; system connections should be copied to the
            target system at the end of installation. Enabled by default.
          </para>
<screen>inst.copy_network=0</screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.register_url</literal></term>
        <listitem>
          <para>
            Sets the URL of the RMT or &scc; proxy server to register the product with.
          </para>
<screen>inst.register_url=<replaceable>http://rmt.example.net</replaceable></screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.install_url</literal></term>
        <listitem>
          <warning>
            <title>Affects all products</title>
            <para>
              Setting this variable affects all products.
            </para>
          </warning>
          <para>
            Overrides the default <literal>installation_url</literal> set in the product files.
            Particularly useful for pre-production testing (for example, with openQA).
          </para>
<screen>inst.install_url=<replaceable>https://myrepo,https://myrepo2</replaceable></screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>proxy</literal></term>
        <listitem>
          <para>
            Sets up a network proxy, in the form
            <literal>protocol://[user[:password]@]host[:port]</literal>.
          </para>
<screen>proxy=http://192.168.122.1:3128</screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>rd.ntp</literal></term>
        <listitem>
          <para>
            Sets the time sources to synchronize the date and time before &agama; itself starts;
            specify multiple times for multiple sources. The installation medium synchronizes
            during boot, and &agama; copies the resulting configuration to the installed system.
          </para>
<screen>rd.ntp=pool:1.opensuse.pool.ntp.org:iburst</screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.self_update</literal></term>
        <listitem>
          <para>
            Available since &suselinux; 16.0/QU0. Set
            <literal>inst.self_update=0</literal> to disable
            the installer self-update. Related options:
            <literal>inst.self_update_ssl=0</literal>,
            <literal>inst.self_update_unsigned_repo=1</literal>,
            <literal>inst.self_update_import_key=1</literal>, and
            <literal>inst.self_update_gpg=0</literal>.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>
  </section>
  <section xml:id="agama-boot-options-live-system">
    <title>Live system access</title>
    <variablelist>
      <varlistentry>
        <term><literal>live.password</literal> / <literal>live.password_hash</literal></term>
        <listitem>
          <para>
            Sets the &rootuser; password of the live system. <literal>live.password</literal>
            accepts a plain text password; <literal>live.password_hash</literal> expects a hashed
            password, which is more secure but longer and harder to type at the boot prompt. The
            hashed form is more practical when the boot parameters are prepared in advance, for
            example for PXE boot or virtual machines. If you do not set a password with either
            option, &agama; generates a random one and prints it to the console.
          </para>
<screen>live.password=<replaceable>nots3cr3t</replaceable></screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>live.password_dialog</literal></term>
        <listitem>
          <para>
            Starts an interactive full-screen dialog during boot for entering and confirming the
            password. If the dialog does not render correctly in some environments, try
            <keycombo><keycap function="control"/><keycap>L</keycap></keycombo> to refresh the
            screen, or use <literal>live.password_systemd</literal> instead.
          </para>
<screen>live.password_dialog
live.password_dialog=1</screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>live.password_systemd</literal></term>
        <listitem>
          <para>
            Asks for the password using a single-line prompt instead of a full-screen dialog,
            which works better in constrained environments such as a serial console.
          </para>
<screen>live.password_systemd
live.password_systemd=1</screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.remote</literal></term>
        <listitem>
          <para>
            Available since &suselinux; 16.1. By default, the &agama; installer can be
            accessed remotely from other machines or mobile devices. Set
            <literal>inst.remote=0</literal> to disable remote access for increased security,
            restricting access to the local machine only. To disable other network services, such
            as SSH, use the standard <literal>systemd.mask</literal> boot option instead.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>
  </section>
  <section xml:id="agama-boot-options-advanced">
    <title>Advanced and diagnostic options</title>
    <variablelist>
      <varlistentry>
        <term><literal>fips</literal></term>
        <listitem>
          <para>
            Standard Linux kernel parameter. Set to <literal>1</literal> to start the installer in
            FIPS-enabled mode; &agama; also uses this as a trigger to propose a FIPS-compliant
            installed system.
          </para>
<screen>fips=1</screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.solver_testcase</literal></term>
        <listitem>
          <para>
            Available since &suselinux; 16.1. By default, the dependency solver test case is
            saved only on failure. Set <literal>inst.solver_testcase=1</literal> to also save it
            on success, which is useful for debugging package dependency problems. Saved to
            <filename>/run/agama/testcase</filename>, and included in the logs created by
            <command>agama logs store</command>. If the <literal>ZYPP_FULLLOG=1</literal> boot
            option is set, the solver test case is instead created automatically by
            <literal>libzypp</literal> (the library behind &zypper; and &agama;'s own package
            resolution) under
            <filename>/var/log/YaST2/autoTestcase</filename>, and this option is ignored.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>inst.systemd_boot_preview</literal></term>
        <listitem>
          <note>
            <title>Preview feature</title>
            <para>
              This is a preview feature for testing <literal>systemd-boot</literal> support. It
              automatically falls back to &grub; if the system does not meet the Boot Loader
              Specification (BLS) requirements, and may be removed after the testing period for
              <literal>systemd-boot</literal> support ends.
            </para>
          </note>
          <para>
            Available since &suselinux; 16.1. Set to <literal>1</literal> to enable the
            <literal>systemd-boot</literal> boot loader as a preview feature.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>systemd.unit</literal></term>
        <listitem>
          <para>
            Standard way to change the &systemd; boot target. Useful for headless mode, when neither
            a graphical interface nor a browser needs to start locally.
          </para>
<screen>systemd.unit=multi-user.target</screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>systemd.mask</literal></term>
        <listitem>
          <para>
            Standard way to disable a &systemd; unit during boot, for example to disable a default
            service started on the Live ISO:
          </para>
<screen>systemd.mask=sshd.service</screen>
        </listitem>
      </varlistentry>
    </variablelist>
  </section>
  <section xml:id="agama-boot-options-linuxrc-compatibility">
    <title>Linuxrc compatibility</title>
    <para>
      &agama; provides a few options that ease the transition from <command>linuxrc</command>, the
      boot-time parameter parser used by earlier &suse; installers. They are not identical and work
      slightly differently, but should be sufficient for regular use. Unlike
      <command>linuxrc</command>, &agama; is case-sensitive and requires the exact parameter name,
      without the additional dots, dashes, and underscores <command>linuxrc</command> tolerated.
    </para>
    <variablelist>
      <varlistentry>
        <term>Info file support</term>
        <listitem>
          <para>
            The <literal>inst.info</literal> option (see
            <xref linkend="agama-boot-options-profile"/>) acts similarly to
            <command>linuxrc</command>'s <literal>info=</literal> option. However, it supports
            only URI schemes recognized by <literal>curl</literal>, and the info file can contain
            only &agama;-specific options.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Network configuration</term>
        <listitem>
          <para>
            &agama; translates a subset of <command>linuxrc</command>'s <literal>ifcfg</literal>
            option into the <literal>ip</literal>/<literal>vlan</literal> syntax used by
            <command>dracut</command>; for most cases, prefer writing <literal>ip</literal>
            directly instead. For a complete list of <command>dracut</command> options, refer to
            <command>man 7 dracut.cmdline</command>.
          </para>
<screen># ifcfg=*=dhcp
ip=dhcp

# ifcfg=eth0=dhcp
ip=eth0:dhcp

# ifcfg=eth0.10=192.168.0.100/24,192.168.0.1
vlan=eth0.10:eth0 ip=192.168.0.100::192.168.0.1:24::eth0.10

# ifcfg="eth0=192.168.0.33/24 10.0.0.100/24,192.168.0.1,192.168.0.1 10.0.0.1,example.com"
ip=192.168.0.33::192.168.0.1:24::eth0 nameserver=192.168.0.1 nameserver=10.0.0.1 ip=10.0.0.100:::24::eth0</screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>hostname</literal> and <literal>SetHostname</literal></term>
        <listitem>
          <para>
            Available with almost the same behavior as the equivalent <command>linuxrc</command>
            options.
          </para>
          <itemizedlist>
            <listitem>
              <para>
                <literal>hostname</literal>: sets the system's static host name in
                <command>dracut</command> and persists it for the installer. If set, the transient
                host name is not used, so network configuration (for example, DHCP) does not
                modify it. Distinct from the profile's own <literal>hostname</literal> section
                (see
                <xref linkend="hostname-configuration-agama-installation-profile"/>), which
                applies to the installed system rather than the live installation environment.
              </para>
            </listitem>
            <listitem>
              <para>
                <literal>SetHostname</literal>: controls whether the host name can be set by DHCP.
                Setting it to <literal>0</literal> sets &nm;'s <literal>hostname-mode</literal> to
                <literal>none</literal>. If a static host name is already set, it is not modified
                either way; this differs from the behavior under Wicked.
              </para>
<screen># Default, keeps the default NetworkManager hostname-mode
SetHostname=1

# Sets the NetworkManager hostname-mode to 'none'
SetHostname=0</screen>
            </listitem>
          </itemizedlist>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Environment variables</term>
        <listitem>
          <para>
            Some options were not handled by <command>linuxrc</command> directly, but passed
            through to &yast; using the <filename>install.inf</filename> file, available to the
            installer as environment variables. &agama; recognizes the same variable name
            patterns: <literal>LIBSTORAGE*</literal>, <literal>YAST*</literal>,
            <literal>Y2*</literal>, and <literal>ZYPP_FULLLOG</literal>, making them available to
            the &agama; systemd services. For example, <literal>LIBSTORAGE_MULTIPATH_AUTOSTART</literal>
            (see <xref linkend="forced-multipath-activation-agama-installation"/>) and
            <literal>ZYPP_FULLLOG</literal> (see
            <xref linkend="agama-boot-options-advanced"/>) are already documented individually
            elsewhere in this reference.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>
  </section>
</topic>
