<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<topic xml:id="cpu-check-temperature-troubleshoot"
 role="reference" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Troubleshooting</title>
    <meta name="maintainer" content="daria.vladykina@suse.com" its:translate="no"/>
    <abstract>
      <para>
        This part describes potential problems when monitoring CPU temperatures
        and their solutions.
      </para>
    </abstract>
  </info>
  <section xml:id="cpu-check-temperature-troubleshoot-unrealistic-temperatures-shown">
    <title>The displayed temperatures are unrealistic</title>
    <para>
      If you suspect that the displayed temperature is too low or too high, you
      can try the following:
    </para>
    <itemizedlist>
      <listitem>
        <para>
          Compare the temperatures of the ACPI thermal zones with the
          <literal>acpitz</literal> entries in the output of the
          <command>sensors</command> command. Both show the same zones, so the
          values should agree. For the commands, see
          <xref linkend="cpu-check-temperature-get-data"/>.
        </para>
      </listitem>
      <listitem>
        <para>
          Use a different tool that reads the same kernel sensors, for example,
          <command>s-tui</command>, a terminal utility that shows the
          temperatures that the system reports together with the CPU frequency
          and utilization. It is provided by SUSE Package Hub, whose packages
          are community-maintained and delivered without L3 support. If Package
          Hub is not enabled, run
          <command>SUSEConnect --list-extensions</command>, which prints the
          command to enable it.
        </para>
        <para>On the mutable system:</para>
<screen>&prompt.sudo;<command>zypper install s-tui</command></screen>
        <para>On the immutable system:</para>
<screen>&prompt.sudo;<command>transactional-update pkg install s-tui</command></screen>
        <para>Reboot the system to activate the change.</para>
        <para>
          Then run <command>s-tui</command> from the terminal.
        </para>
      </listitem>
    </itemizedlist>
    <para>
      If none of these recommendations solves the issue, the problem might be
      due to unsupported or faulty hardware. In this case, you need to seek
      help from your hardware manufacturer.
    </para>
  </section>
  <section xml:id="cpu-check-temperature-troubleshoot-wrong-temperature">
    <title>The displayed temperature is too high</title>
    <para>
      If the CPU temperature is too high, here are actions you can take:
    </para>
    <itemizedlist>
      <listitem>
        <para>
          Verify that the CPU cooling system, such as the fan or heat sink, 
          works correctly. Ensure that the fan is spinning properly and that
          the heat sink is making proper contact with the CPU. If necessary,
          you may need to replace the thermal paste between the CPU and the
          heat sink to improve heat transfer.
        </para>
      </listitem>
      <listitem>
        <para>
          Adjust the power settings on your system to reduce heat generation.
          Lowering the CPU frequency or enabling power-saving features can help
          keep the temperature in check.
        </para>
      </listitem>
      <listitem>
        <para>
          Monitor the system load and CPU usage. High CPU usage for extended
          periods can lead to increased temperatures. Identify
          resource-intensive processes with tools such as
          <command>top</command> or <command>ps</command>, then consider
          optimizing or limiting their usage. For more information, refer to
          <link xlink:href="https://www.suse.com/support/kb/doc/?id=000016916"/>.
        </para>
      </listitem>
    </itemizedlist>
    <remark>ssarkar: removed the dead cpupower-tool link
    (.../SBP-performance-tuning/index.html#sec-cpupower-tool). No SLE 16
    System Analysis and Tuning Guide exists yet, nothing to point to
    instead.</remark>
  </section>
</topic>
