<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="compose-yaml-file"
 role="reference" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
   <title>The <filename>compose.yml</filename> file</title><!-- can be changed via merge in the assembly -->
    <!--add author's email address-->
    <meta name="maintainer" content="jsindelarova@suse.com" its:translate="no"/>
    <abstract><!-- can be changed via merge in the assembly -->
      <para>
       Introductory text
      </para>
    </abstract>
  </info>
  <para>
    The <filename>compose.yml</filename> is preferably placed in the working directory. It can be
    either a single file or you can use more granular approach by utilizing fragments and
    extensions. Multiple &compose; files can be also merged together to define the whole application model.
  </para>
  <section xml:id="compose-yaml-file-service-definition">
    <title>Service definition</title>
    <para>
      Each service is defined as a container image to use and a set of runtime arguments. Start
      the services definition by adding <literal>services</literal> as the top-level element to
      the <filename>compose.yml</filename> file. Other elements are described below:
    </para>
    <variablelist>
      <title>Service definition elements/title>
      <varlistentry>
        <term><literal>build</literal></term>
        <listitem>
          <para>
           Specifies how to create a container image that is later used to start the service.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><literal>image</literal></term>
        <listitem>
          <para>
           Defines the container image to use for starting the service.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>    
  </section>
  <section xml:id="compose-yaml-file-network-definition">
    <title>Networks definition</title>


  </section>
  <section xml:id="compose-yaml-file-volume-definition">
<title>Volumes definition</title>
  </section>
</topic>
