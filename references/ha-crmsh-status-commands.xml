<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/edit/main/xml/ha_config_cli.xml -->

<topic xml:id="ha-crmsh-showing-cluster-status"
 role="reference" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>&crmshell; status commands</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        The <command>crm</command> command has multiple options for showing information about the cluster:
      </para>
    </abstract>
  </info>

  <itemizedlist>
    <listitem>
      <para>
        Show the status of the cluster services:
      </para>
<screen>&prompt.user;<command>sudo crm cluster status</command></screen>
    </listitem>
    <listitem>
      <para>
        Show the status of the cluster, including a summary of the cluster details, a list and
        status of all the nodes, and a list and status of all the resources:
      </para>
<screen>&prompt.user;<command>sudo crm status</command></screen>
    </listitem>
    <listitem>
      <para>
        Show the status of the cluster, but with the resources listed under the nodes they're
        currently running on:
      </para>
<screen>&prompt.user;<command>sudo crm status bynode</command></screen>
    </listitem>
    <listitem>
      <para>
        Show the status of the cluster, but expanded to also list the primitive resources that
        are included in resource groups and clone sets:
      </para>
<screen>&prompt.user;<command>sudo crm status detail</command></screen>
    </listitem>
    <listitem>
      <para>
        Show the status of the cluster, a summary of the resource operations on each node,
        and the migration threshold of each resource:
      </para>
<screen>&prompt.user;<command>sudo crm status ops</command></screen>
    </listitem>
    <listitem>
      <para>
        Show the status of the cluster with more information, such as details about the resource
        operations and a list of inactive resources:
      </para>
<screen>&prompt.user;<command>sudo crm status full</command></screen>
    </listitem>
    <listitem>
      <para>
        Show additional options:
      </para>
<screen>&prompt.user;<command>sudo crm help status</command></screen>
    </listitem>
  </itemizedlist>
  <para>
    If the output is longer than the terminal window, these commands open a text viewer.
    The text viewer allows you to scroll up or down and read the output more comfortably.
    To leave the text viewer, press the <keycap>Q</keycap> key.
  </para>
</topic>
