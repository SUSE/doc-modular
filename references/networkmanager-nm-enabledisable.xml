<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="networkmanager-nm-enabledisable"
 role="reference" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Establishing and terminating network connections</title>
    <!-- can be changed via merge in the assembly -->
    <!--add author's email address-->
    <meta name="maintainer" content="shalaka.harne@suse.com" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
       <para>Enable network connection to access and connect devices and resources.</para>
      </abstract>
  </info>
<section xml:id="sec-networkmanager-enable-connections">
    <title>Enabling network connection</title>
    <para>Activate network connections using the <command>nmcli</command> command.</para> <variablelist>
      <varlistentry>
        <term>View the list of existing connections:</term>
        <listitem>
          <para>
           <command>nmcli connection show</command></para>
           <para>The active network connections appear with the names and UUIDs.</para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Enable a connection using the name or UUID:</term>
        <listitem>
          <para>
          <command>nmcli connection up uuid <replaceable>CONNECTION-UUID</replaceable></command></para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>If networking is disabled using <command>nmcli networking off</command> command, enable networking:</term>
        <listitem>
          <para>
          <command>nmcli networking on</command></para>
           </listitem>
      </varlistentry>
      <varlistentry>
        <term>View the connection status:</term>
        <listitem>
          <para>
           <command>nmcli connection show --active</command>
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Enable a Wi-Fi network connection:</term>
        <listitem>
          <para>
          <command>nmcli device wifi connect <replaceable>SSID</replaceable> password <replaceable>PASSWORD</replaceable></command></para>       </listitem>
      </varlistentry>
    </variablelist>
</section>
<section xml:id="sec-networkmanager-disable-connections">
    <title>Disabling network connection</title>
     <para>You can temporarily disconnect your system from external
  networks using the <command>nmcli</command> command to
  disable your network connection.</para>
  <variablelist>
     <varlistentry>
        <term>View the list of active connections:</term>
        <listitem>
          <para>
           <command>nmcli connection show --active</command></para>
           <para>The active network connections appear with the names and UUIDs.</para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Terminate a specific connection:</term>
        <listitem>
          <para>
          <command>nmcli connection down <replaceable>CONNECTION-NAME/CONNECTION-UUID</replaceable></command></para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Disable network connection:</term>
        <listitem>
          <para>
          <command>nmcli networking off</command></para>
           </listitem>
      </varlistentry>
    </variablelist>
</section>
</topic>
