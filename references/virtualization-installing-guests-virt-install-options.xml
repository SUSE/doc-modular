<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="reference-example"
 role="reference" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
   <title>Using the <command>virt-install</command> command</title><!-- can be changed via merge in the assembly -->
    <!--add author's email address-->
    <meta name="maintainer" content="tbazant@suse.com" its:translate="no"/>
    <abstract><!-- can be changed via merge in the assembly -->
      <para>
       The <command>virt-install</command> command provides many options to deploy a VM according to your needs.
      </para>
    </abstract>
  </info>
  <para>
    The <command>virt-install</command> accepts many options. The list of required options follows.
  </para>
  <variablelist>
    <varlistentry>
      <term><option>--name <replaceable>VM_GUEST_NAME</replaceable></option></term>
      <listitem>
        <para>Specify the name of the new virtual machine. The name must be unique across all guests known to the hypervisor on the same connection. It is used to create and name the guest’s configuration file and you can access the guest with this name from <command>virsh</command>. Alphanumeric and <literal>_-.:+</literal> characters are allowed.</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term><option>--memory
                <replaceable>REQUIRED_MEMORY</replaceable></option></term>
                <listitem>
                  <para>
                    Specify the
                number of virtual CPUs. For best performance, the number of
                virtual processors should be less than or equal to the number
                of physical processors.
                  </para>
                </listitem>
    </varlistentry>
    <varlistentry>
      <term><option>--vcpus
                <replaceable>NUMBER_OF_CPUS</replaceable></option></term>
                <listitem>
                  <para>
                    Specify the
                number of virtual CPUs. For best performance, the number of
                virtual processors should be less than or equal to the number
                of physical processors.
                  </para>
                </listitem>
    </varlistentry>
    <para>
      The following options define the virtualization type.
    </para>
    <varlistentry>
      <term><option>--paravirt</option></term>
      <listitem>
        <para>
         To set up a paravirtualized guest.
                This is the default if the &vmhost; supports paravirtualization
                and full virtualization.
        </para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term><option>--hvm</option></term>
      <listitem>
        <para>To set up a fully virtualized guest.</para>
      </listitem>
    </varlistentry>

  </variablelist>
</topic>
