<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="reference-zypper-install"
 role="reference" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>&zypper; reference</title>
    <!--add author's email address-->
    <meta name="maintainer" content="cwickert@suse.com" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        Introductory text
      </para>
    </abstract>
  </info>
  <section xml:id="zypper-install-software">
    <title>Install packages with &zypper;</title>
    <para>
      To install a package, run:
    </para>
<screen>&prompt.sudo;zypper install <replaceable>PACKAGE_NAME</replaceable></screen>
    <para>
      There are multiple ways to specify the package name:
    </para>
    <variablelist xml:id="vl-zypper-install-package">
      <varlistentry>
        <term>By exact name</term>
        <listitem>
<screen>&prompt.sudo;zypper install MozillaFirefox</screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>By exact name and version number</term>
        <listitem>
<screen>&prompt.sudo;zypper install MozillaFirefox-128.11</screen>
        <para>
          Versions must be
            appended to the end of the string and must be preceded by an operator:
            <literal>&lt;</literal> (lesser than), <literal>&lt;=</literal> (lesser than or equal),
            <literal>=</literal> (equal), <literal>&gt;=</literal> (greater than or equal),
            <literal>&gt;</literal> (greater than):
        </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>By exact name and repository</term>
        <listitem>
<screen>&prompt.sudo;zypper install mozilla:MozillaFirefox</screen>
          <para>
            This is useful of a package is available in more than one repository. In this example,
            the <package>MozillaFirefox</package> package from the <literal>mozilla</literal>
            repository is installed.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>By the capability it provides</term>
        <listitem>
<screen>&prompt.sudo;zypper install firefox</screen>
          <para>
            In this example, the <literal>firefox</literal> capability is provided by the
            <package>MozillaFirefox</package> package, thus running the command installs this
            specific package.
            <!-- FIXME-->
            For more information on the concept of capability, refer to "FIXME".
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>By its capability, hardware architecture and version</term>
        <listitem>
<screen>&prompt.sudo;zypper install 'firefox.x86_64'</screen>
          <para>
            Install a package by its capability, hardware architecture and version.
          </para>
<screen>&prompt.sudo;zypper install 'firefox&gt;=128.11'</screen>
          <para>
            You can also combine a hardware architecture and version requirement:
          </para>
<screen>&prompt.sudo;zypper install 'firefox.x86_64&gt;=128.11'</screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>using wild cards in its name.</term>
        <listitem>
<screen>&prompt.sudo;zypper install 'Moz*'</screen>
          <para>
            The example command above installs all packages which names start with <quote>Moz</quote>.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Install a package using a local or remote RPM file.</term>
        <listitem>
<screen>&prompt.sudo;zypper install /tmp/install/MozillaFirefox.rpm</screen>
          <para>
            <command>zypper install /tmp/install/MozillaFirefox.rpm</command> and <command>zypper
            install http://download.&exampledomain;/MozillaFirefox.rpm</command>
          </para>
        </listitem>
      </varlistentry>
    </variablelist>
  </section>
</topic>
