<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="reference-zypper-remove"
 role="reference" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Removing package with &zypper;</title>
    <!-- can be changed via merge in the assembly -->
    <!--add author's email address-->
    <meta name="maintainer" content="cwickert" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        Removing packages is as easy as installing them.
      </para>
    </abstract>
  </info>
  <section xml:id="reference-zypper-remove-FIXME">
    <title>Removing packages</title>
    <para>
      To remove a package, run:
    </para>
<screen>&prompt.sudo;zypper remove <replaceable>PACKAGE_NAME</replaceable></screen>
    <para>
      To specify the package name, you can use the same methods as for installation.
      (<xref linkend="vl-zypper-install-package"/>).
    </para>
    <para>
      Note that only the specified package(s) will be removed. Use the <option>-u</option> option
      to also remove packages that were installed as dependencies and that are no longer required:
    </para>
<screen>&prompt.sudo;zypper remove -u <replaceable>PACKAGE_NAME</replaceable></screen>
    <para></para>
  </section>
  <section>
    <title>Replacing packages</title>
    <para></para>
  </section>
  <section>
    <title>Removing unused packages</title>
    <para>
      Use <command>zypper packages</command> to check for orphaned and unneeded packages.
    </para>
    <para>
      Orphaned Packages are no longer available in any of the configured package repositories. They
      can no longer get updated and become unsupported.
    </para>
    <para>
      For a list of orphaned packages, run:
    </para>
<screen>&prompt.user;<command>zypper packages --orphaned</command></screen>
    <para>
      Unneeded packages are dependencies of packages that have been installed either explicitly by
      the user or implicitly as part a pattern or product, and that have been removed in the
      meantime. They are usually no longer needed and should be removed, too.
    </para>
    <para>
      For a list of unneeded packages, run:
    </para>
<screen>&prompt.user;<command>zypper packages --unneeded</command></screen>
    <tip>
      <para>
        To avoid unneeded packages, use <command>zypper rm</command> with the
        <option>--clean-deps</option> option or &yast; with <menuchoice>
        <guimenu>Options</guimenu><guimenu>Clean up when deleting packages</guimenu></menuchoice>
        enabled.
      </para>
    </tip>
    <para>
      You can combine both lists into one:
    </para>
<screen>&prompt.user;<command>zypper packages --orphaned --unneeded</command></screen>
    <para>
      Use these lists to determine which packages are still needed and which can be safely removed.
    </para>
    <warning>
      <title>Do not remove packages you need</title>
      <para>
        If packages are renamed or removed from a pattern or product, <command>zypper</command> may
        no longer consider them explicitly installed and mark them as unneeded, even though they
        are still crucial for your installation.
      </para>
      <para>
        Carefully review the list of packages you are removing.
      </para>
    </warning>
    <para>
      To remove all orphaned and unneeded packages with a single command, run:
    </para>
<screen>&prompt.sudo;<command>zypper rm $(zypper --no-refresh packages --orphaned --unneeded | gawk '{print $5}' | tail -n +5)</command></screen>
    <para>
      Exclude a single package or pattern from being uninstalled:
    </para>
<screen>&prompt.sudo;<command>zypper rm $(zypper --no-refresh packages --orphaned --unneeded | gawk '{print $5}' | tail -n +5 | grep -v <replaceable>PACKAGE_TO_EXCLUDE</replaceable>)</command></screen>
    <para>
      Exclude multiple packages defined in a text file, separated by a newline:
    </para>
<screen>&prompt.sudo;<command>zypper rm $(zypper --no-refresh packages --orphaned --unneeded | gawk '{print $5}' | tail -n +5 | grep -v -f <replaceable>/PACKAGES/TO/KEEP.txt</replaceable>)</command></screen>
  </section>
</topic>
