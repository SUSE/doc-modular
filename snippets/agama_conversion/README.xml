<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="website">
    <title>Website</title>
    <para>
      This website is built using
      <link xlink:href="https://docusaurus.io/">Docusaurus</link>, a
      modern static website generator.
    </para>
    <section xml:id="installation">
      <title>Installation</title>
      <programlisting>
$ yarn
</programlisting>
    </section>
    <section xml:id="local-development">
      <title>Local Development</title>
      <programlisting>
$ yarn start
</programlisting>
      <para>
        This command starts a local development server and opens up a
        browser window. Most changes are reflected live without having
        to restart the server.
      </para>
    </section>
    <section xml:id="build">
      <title>Build</title>
      <programlisting>
$ yarn build
</programlisting>
      <para>
        This command generates static content into the
        <literal>build</literal> directory and can be served using any
        static contents hosting service.
      </para>
    </section>
    <section xml:id="deployment">
      <title>Deployment</title>
      <para>
        Using SSH:
      </para>
      <programlisting>
$ USE_SSH=true yarn deploy
</programlisting>
      <para>
        Not using SSH:
      </para>
      <programlisting>
$ GIT_USER=&lt;Your GitHub username&gt; yarn deploy
</programlisting>
      <para>
        If you are using GitHub pages for hosting, this command is a
        convenient way to build the website and push to the
        <literal>gh-pages</literal> branch.
      </para>
    </section>
    <section xml:id="search">
      <title>Search</title>
      <para>
        The search functionality is provided using the
        <link xlink:href="https://github.com/praveenn77/docusaurus-lunr-search">docusaurus-lunr-search</link>
        plugin. It automatically indexes all documentation pages and the
        blog, it does not index the normal pages like the main page or
        the about page. The search field is added at each page in the
        page title bar at the top.
      </para>
      <para>
        It uses the <link xlink:href="https://lunrjs.com">lunr</link>
        search engine internally. The engine supports using wildcards or
        usual <literal>+</literal> or <literal>-</literal> search terms,
        see more details in the
        <link xlink:href="https://lunrjs.com/guides/searching.html">documentation</link>.
      </para>
      <para>
        :warning: <emphasis>The search index is generated only in a
        production build, not during development! To test the search
        functionality locally you need to run the
        <literal>yarn build &amp;&amp; yarn serve</literal>
        commands!</emphasis>
      </para>
    </section>
    <section xml:id="blog">
      <title>Blog</title>
      <para>
        To create a new blog post run the
        <link xlink:href="create_blog_post">create_blog_post</link>
        script with a short title as the argument, for example:
      </para>
      <programlisting language="bash">
./create_blog_post &quot;New blog post!&quot;
</programlisting>
      <para>
        The title is used in the created file name and in the post URL
        so use something short and meaningful. The blog post itself then
        can contain a longer title specified in the
        <literal>title</literal> front matter.
      </para>
      <para>
        See more details about writing blog posts in the
        <link xlink:href="blog.md">blog.md</link> document.
      </para>
    </section>
    <section xml:id="discussions">
      <title>Discussions</title>
      <para>
        The <link xlink:href="https://giscus.app/">Giscus</link>
        application is used for discussing the blog posts. Internally it
        uses the
        <link xlink:href="https://docs.github.com/en/discussions">GitHub
        discussions</link> as the backend and storage. The advantage is
        that we can use the existing GitHub infrastructure and user
        authentication. Giscus is basically just a wrapper for embedding
        the GitHub discussions into another page.
      </para>
      <para>
        By default discussions are enabled below all blog posts. To
        disable discussion under a specific blog post set
        <literal>discussion: false</literal> in the blog post front
        matter.
      </para>
      <para>
        For blog posts discussions it was created a special
        <link xlink:href="https://github.com/agama-project/agama-project.github.io/discussions/categories/blog">Blog</link>
        discussion category to easily distinguish the blog posts
        discussions from others.
      </para>
      <section xml:id="permissions">
        <title>Permissions</title>
        <para>
          The new blog posts discussions can be created by Agama
          developers, the other users can create them only via Giscus.
          All interactions from the blog post page are done through the
          Giscus application. That means the users need to authorize the
          Giscus application to post on their behalf.
        </para>
        <para>
          If you do not want to authorize the Giscus you can comment
          directly at the respective existing GitHub discussion without
          using the Giscus proxy.
        </para>
      </section>
      <section xml:id="mapping">
        <title>Mapping</title>
        <para>
          There two ways how to find the respective GitHub discussion
          for a blog post. Each approach has different pros and cons so
          the Agama blog supports both.
        </para>
        <section xml:id="search-blog-post-url-path">
          <title>Search blog post URL path</title>
          <para>
            The respective discussion at GitHub is found by searching
            the URL path in the discussion title. E.g. for the
            <literal>https://agama-project.github.io/blog/2023/02/16/d-installer-0-7</literal>
            blog post URL it searches for a discussion with the
            <literal>blog/2023/02/16/d-installer-0-7</literal> title (or
            similar, see below).
          </para>
          <para>
            Advantages:
          </para>
          <itemizedlist spacing="compact">
            <listitem>
              <para>
                Giscus can create a new discussion automatically if it
                does not exist yet
              </para>
            </listitem>
            <listitem>
              <para>
                Discussion are created only when needed, if nobody
                comments no discussion is created
              </para>
            </listitem>
          </itemizedlist>
          <para>
            Disadvantages:
          </para>
          <itemizedlist spacing="compact">
            <listitem>
              <para>
                The first user who comments needs to authorize the
                Giscus application, it is not possible to comment at
                GitHub because the discussion does not exist yet and
                external users cannot create a new blog post discussion,
                they can only comment in an existing one
              </para>
            </listitem>
            <listitem>
              <para>
                Slightly slower loading, Giscus first needs to run a
                GitHub search query to find the URL of the respective
                discussion
              </para>
            </listitem>
            <listitem>
              <para>
                The GitHub search query uses a fuzzy matching, if more
                blog posts use a similar URL path or title then GitHub
                might return a wrong discussion as the search result
              </para>
            </listitem>
          </itemizedlist>
        </section>
        <section xml:id="explicit-discussion-id">
          <title>Explicit discussion ID</title>
          <para>
            The respective GitHub discussion ID is defined directly in
            the blog post front matter. E.g. for the
            <literal>https://github.com/agama-project/agama-project.github.io/discussions/42</literal>
            discussion URL set the <literal>discussion_id: 42</literal>
            value in the blog post front matter.
          </para>
          <para>
            Advantages:
          </para>
          <itemizedlist spacing="compact">
            <listitem>
              <para>
                Faster loading, no need to run a GitHub query, the
                discussion URL is already known
              </para>
            </listitem>
            <listitem>
              <para>
                Avoids using a wrong discussion because of the fuzzy
                GitHub search (see above)
              </para>
            </listitem>
            <listitem>
              <para>
                The first user can add a comment directly to GitHub
                discussion without need to authorize the Giscus
                application
              </para>
            </listitem>
          </itemizedlist>
          <para>
            Disadvantages:
          </para>
          <itemizedlist spacing="compact">
            <listitem>
              <para>
                The discussion needs to be created manually in advance
                by the Agama developers, it cannot be created
                automatically by Giscus or any external user later
              </para>
            </listitem>
          </itemizedlist>
        </section>
        <section xml:id="usage">
          <title>Usage</title>
          <para>
            For the old imported blog posts we should use the first
            approach with URL path mapping to avoid creating more than a
            dozen of discussions manually where very likely nobody would
            comment.
          </para>
          <para>
            For new blog posts we should prefer using a fixed discussion
            ID to allow commenting at GitHub right from the first
            comment.
          </para>
          <para>
            Of course it is possible to change the mapping later,
            e.g. let the Giscus create a new discussion automatically
            and later pin it to the blog using its ID.
          </para>
        </section>
      </section>
    </section>
    <section xml:id="versioning">
      <title>Versioning</title>
      <para>
        If you document a new feature added in some specific Agama
        release or if you want to mark some features as deprecated or
        removed then use the <literal>&lt;Since&gt;</literal>,
        <literal>&lt;Deprecated&gt;</literal> or
        <literal>&lt;RemovedIn&gt;</literal> custom components.
      </para>
      <programlisting language="markdown">
import { Since } from &quot;@site/src/components/Badge&quot;; ... `foo` &lt;Since version=&quot;16.1&quot;/&gt;: configuration
option for ...
</programlisting>
    </section>
    <section xml:id="formatting">
      <title>Formatting</title>
      <para>
        For consistency reasons, we use
        <link xlink:href="https://prettier.io/">prettier</link> to
        format the Markdown files. You can automatically format them
        running the following command:
      </para>
      <programlisting>
$ yarn fmt
</programlisting>
      <para>
        If you only want to check whether the format is OK, you can use:
      </para>
      <programlisting>
$ yarn fmt:check
</programlisting>
      <para>
        Our Continuous Integration runs that command to make sure the
        Markdown files are properly formatted.
      </para>
    </section>
  </section>
</article>
