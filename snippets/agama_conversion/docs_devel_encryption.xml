<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="encryption">
    <title>Encryption</title>
    <section xml:id="http-vs-https">
      <title>HTTP vs HTTPS</title>
      <para>
        If the server and the client run in the same machine, the
        communication can be done over HTTP or HTTPS. However, if the
        server and the client are on different machines,
        <emphasis role="strong">HTTPS is mandatory</emphasis>. In such
        case all HTTP requests are automatically redirected to
        HTTPS<footnote>
          <para>
            The client gets a response with code 308 (permanent
            redirect).
          </para>
        </footnote>.
      </para>
      <para>
        The same applies principle applies to the WebSocket, which is
        encrypted in case of remote installations.
      </para>
    </section>
    <section xml:id="ssl-certificates">
      <title>SSL certificates</title>
      <para>
        If no certificate is given, Agama uses a self-signed one to
        secure the communications between the server and the clients.
      </para>
      <para>
        Most likely, you will want to check the certificate’s
        fingerprint to make sure you are connecting to the right system.
        Agama’s live medium writes the fingerprint to the console and
        the serial console as you can see on the screenshot below.
      </para>
      <figure>
        <title>Displaying the SSL certificate’s fingerprint in the
        console</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="./img/certificate-fingerprint.png" />
          </imageobject>
          <textobject><phrase>Displaying the SSL certificate’s
          fingerprint in the console</phrase></textobject>
        </mediaobject>
      </figure>
      <warning>
        <para>
          There is no easy way yet to inject your own certificate when
          using Agama’s live medium, although the
          <literal>agama-web-server</literal> command provides options
          to set the certificate and its key.
        </para>
      </warning>
    </section>
  </section>
</article>
