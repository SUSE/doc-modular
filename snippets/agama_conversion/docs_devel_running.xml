<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="running-from-sources">
    <title>Running from sources</title>
    <para>
      You can run Agama from its sources by cloning and configuring the
      project:
    </para>
    <programlisting>
git clone https://github.com/openSUSE/agama
cd agama
./setup.sh
</programlisting>
    <para>
      Then point your browser to
      <literal>http://localhost:8080/</literal>, log in with your root
      password and that’s all.
    </para>
    <para>
      The
      <link xlink:href="https://github.com/openSUSE/agama/blob/master/setup.sh">setup.sh</link>
      script installs the required dependencies to build and run the
      project and it also configures the Agama services. It uses
      <literal>sudo</literal> to install packages and files to system
      locations. The script is well commented so we refer you to it
      instead of repeating its steps here.
    </para>
    <para>
      To start or stop Agama D-Bus and web services at any time, use the
      <literal>agama</literal> and <literal>agama-web-server</literal>
      systemd services:
    </para>
    <programlisting>
sudo systemctl start agama
sudo systemctl start agama-web-server
</programlisting>
    <para>
      If something goes wrong, you can use <literal>journalctl</literal>
      to get Agama logs:
    </para>
    <programlisting>
sudo journalctl -u agama
sudo journalctl -u agama-web-server
</programlisting>
    <para>
      Another alternative is to run source checkout inside container so
      system is not affected by doing testing run beside real actions
      really done by installer. See more details
      <link xlink:href="./testing_using_container.md">in the
      documentation</link>.
    </para>
    <warning>
      <para>
        To do: integrate the information about running the proxy web
        server (<literal>AGAMA_SERVER</literal> argument).
      </para>
    </warning>
  </section>
</article>
