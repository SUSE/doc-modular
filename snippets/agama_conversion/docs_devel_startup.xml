<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="startup-process">
    <title>Startup process</title>
    <para>
      As described in the <link xlink:href="./architecture">architecture
      section</link>, Agama is composed of a web server, a set of D-Bus
      services, a web client and a command-line interface. The startup
      process aims to get those D-Bus services up and running and make
      the web server available. Additionally, the auto-installation
      procedure could be started if required by the user.
    </para>
    <para>
      The startup process is handled by systemd and D-Bus. The only
      exception is starting the local browser in the Agama Live image.
    </para>
    <section xml:id="starting-the-d-bus-services">
      <title>Starting the D-Bus Services</title>
      <para>
        <link xlink:href="https://github.com/openSUSE/agama/blob/master/service/share/agama.service">agama.service</link>
        is responsible for starting up Agama’s D-Bus daemon process
        using the <literal>agamactl --daemon</literal> command. This
        process uses a dedicated bus with a
        <link xlink:href="https://github.com/openSUSE/agama/blob/master/service/share/dbus.conf">specific
        configuration</link>.
      </para>
      <para>
        Once the daemon process is running, each D-Bus service will be
        automatically activated when required. The definitions of those
        services are located in
        <literal>/usr/share/dbus-1/agama-services</literal>, although
        you can find the sources in the
        <link xlink:href="https://github.com/openSUSE/agama/tree/master/service/share">repository</link>
        (<literal>org.opensuse.Agama*.service</literal> files).
      </para>
    </section>
    <section xml:id="starting-the-web-server">
      <title>Starting the web server</title>
      <para>
        <link xlink:href="https://github.com/openSUSE/agama/blob/master/rust/share/agama-web-server.service">agama-web-server</link>
        is responsible for starting up Agama’s web server using the
        <literal>agama-web-server</literal> command.
      </para>
    </section>
    <section xml:id="auto-installation">
      <title>Auto-installation</title>
      <para>
        If the <literal>inst.auto</literal> option is specified in the
        kernel command-line, the
        <link xlink:href="https://github.com/openSUSE/agama/blob/master/autoinstallation/systemd/agama-auto.service">agama-auto.service</link>
        comes into play. It runs after the
        <literal>agama-web-server.service</literal> so the web server
        and the D-Bus daemon are ready.
      </para>
    </section>
    <section xml:id="web-user-interface">
      <title>Web user interface</title>
      <para>
        When discussing the web user interface, we can distinguish two
        sides: the server process and the web browser. Regarding the
        server, Agama’s web UI is implemented as a React application
        which is served by the web server. You can connect to the UI
        using the <literal>http://$SERVER_IP</literal> address.
      </para>
      <para>
        When using Agama Live, a local web browser is automatically
        started. In the default image, it is launched using an
        <link xlink:href="https://github.com/openSUSE/agama/blob/master/live/root/root/.icewm/startup">IceWM
        startup script</link>.
      </para>
    </section>
  </section>
</article>
