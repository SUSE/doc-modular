<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="storage-tips-tricks">
    <title>Storage tips &amp; tricks</title>
    <para>
      Agama relies on yast2-storage-ng for the management of storage
      devices such as disks, partitions, LVM, RAID, etc. Thus, the
      <link xlink:href="https://github.com/yast/yast-storage-ng/blob/master/src/lib/y2storage/storage_env.rb">environment
      variables</link> honored by that YaST component can be used to
      modify its behavior.
    </para>
    <para>
      That is especially useful for development purposes. In particular,
      setting the following two variables for the Agama service will
      allow to emulate a system with any storage setup.
    </para>
    <programlisting>
YAST_STORAGE_TEST_MODE=1
YAST_DEVICEGRAPH_FILE=/path/to/a/devicegraph.xml
</programlisting>
    <para>
      The file specified at <literal>YAST_DEVICEGRAPH_FILE</literal>
      must contain a
      <link xlink:href="https://github.com/openSUSE/libstorage-ng/blob/master/doc/overview.md#device-graph">devicegraph</link>
      description in any of the formats supported for that purpose by
      yast2-storage-ng, either XML (native libstorage-ng format) or YAML
      (documented
      <link xlink:href="https://github.com/yast/yast-storage-ng/blob/master/doc/fake-devicegraphs-yaml-format.md">here</link>).
    </para>
    <para>
      Note that, at several points during the system installation
      process, yast2-storage-ng stores representations of the
      devicegraph (in both formats) at
      <literal>/var/log/YaST2/storage-inst</literal>.
    </para>
  </section>
</article>
