<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="patching-agama-live-iso">
    <title>Patching Agama Live ISO</title>
    <para>
      The Agama installer allows patching itself directly from the
      GitHub sources or from your local Git checkout using the
      <link xlink:href="https://github.com/yast/yast-installation/blob/master/bin/yupdate">yupdate
      script</link>. See more details in the
      <link xlink:href="https://github.com/yast/yast-installation/blob/master/doc/yupdate.md">yupdate
      documentation</link>.
    </para>
    <para>
      This patching only works when running from a live medium. You can
      also patch the standard YaST modules included in the installer if
      needed.
    </para>
    <para>
      The goal is to provide an easy way for testing fixes or new
      features for the end users or testers.
    </para>
    <warning>
      <para>
        This guide does not cover patching the Rust code. Given the
        amount of disk space that the compiler takes, you must build the
        binary on a different machine.
      </para>
    </warning>
    <section xml:id="patching-from-github">
      <title>Patching from GitHub</title>
      <programlisting>
yupdate patch openSUSE/agama master
</programlisting>
      <para>
        You can replace the <literal>master</literal> branch with any
        branch containing a fix or a new feature.
      </para>
    </section>
    <section xml:id="patching-from-a-local-git-checkout">
      <title>Patching from a local Git checkout</title>
      <para>
        First you need to run the <literal>rake server</literal> command
        in your Agama Git checkout.
      </para>
      <para>
        Then run this command:
      </para>
      <programlisting>
yupdate patch 192.168.1.2:8000
</programlisting>
      <para>
        Replace the IP address with the IP address of your machine
        running the <literal>rake server</literal> command. Port 8000 is
        the default port used, modify it if you use a different port
        number.
      </para>
    </section>
    <section xml:id="options">
      <title>Options</title>
      <para>
        You can modify the update process with these environment
        variables:
      </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>
            <literal>DEBUG=1</literal> - to find more about what is
            going behind process. If it fails in rake install see
            VERBOSE below.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>VERBOSE</literal> - makes visible all commands and
            its output when doing rake install. Useful to debug if
            yupdate failed.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>NPM_CACHE=1</literal> - The installed NPM packages
            will be saved to a local cache and will be reused in the
            next run. This can speed up the patching process if you need
            to patch the installer several times. On the other hand this
            increases the amount of needed RAM memory. The cache is
            stored in <literal>$HOME/.cache/agama-devel/</literal>
            directory, if you need to refresh the content of the cache
            then delete this directory.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>NODE_ENV=development</literal> - The web front-end
            will be built in the development mode. The files will not be
            minimized and additional <literal>*.map</literal> files will
            be generated. This helps with debugging in the browser, you
            can get the locations in the original source files.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>YUPDATE_SKIP_FRONTEND=1</literal> - Skip updating
            the web front-end. Use this option when you use the webpack
            development server for running the web front-end. In that
            case updating the web front-end does not make sense because
            it is running in a different server. This saves some time
            and disk/RAM space.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>YUPDATE_SKIP_BACKEND=1</literal> - Skip updating
            the D-Bus service back-end. This is similar to the previous
            option, use it when you do want to keep the D-Bus service
            unchanged.
          </para>
        </listitem>
      </itemizedlist>
    </section>
    <section xml:id="notes">
      <title>Notes</title>
      <para>
        For compiling the Javascript code and SCSS files the yupdate
        installs several RPM and NPM packages. Because the live medium
        is completely running in a RAM disk all downloaded files are
        stored in the RAM memory.
      </para>
      <para>
        That means the machine should have enough memory for this
        process. If there is not enough memory the system becomes
        completely frozen and might not respond to any input event.
      </para>
      <para>
        Currently the minimum for a safe operation is around 4GB RAM. If
        you use the <literal>NPM_CACHE=1</literal> option then
        recommended minimum is around 5GB.
      </para>
    </section>
    <section xml:id="activating-the-changes">
      <title>Activating the changes</title>
      <para>
        After patching the files the DBus service is restarted if any
        related file has been changed. That means the configured
        settings will be lost.
      </para>
      <para>
        To activate the changes in the web front-end you need to reload
        the page in the browser.
      </para>
      <warning>
        <para>
          In the Firefox browser you need to use the
          <literal>Ctrl+F5</literal> combination for reloading the page,
          this uses full reload ignoring the cache. Plain
          <literal>F5</literal> uses cached files and will not reflect
          the update on the server!
        </para>
      </warning>
      <para>
        In some special cases you might need to do some additional
        actions manually, the update script might not handle all corner
        cases.
      </para>
    </section>
    <section xml:id="implementation-details">
      <title>Implementation details</title>
      <para>
        The support is implemented in the main
        <link xlink:href="https://github.com/openSUSE/agama/blob/master/Rakefile">Rakefile</link>
        and in the
        <link xlink:href="https://github.com/openSUSE/agama/blob/master/.yupdate.pre">.yupdate.pre</link>
        and
        <link xlink:href="https://github.com/openSUSE/agama/blob/master/.yupdate.post">.yupdate.post</link>
        hook scripts.
      </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>
            The <literal>.yupdate.pre</literal> script prepares the
            system for compiling and installing new Agama files.
          </para>
        </listitem>
        <listitem>
          <para>
            The <literal>Rakefile</literal> code builds and installs
            both back-end and front-end parts.
          </para>
        </listitem>
        <listitem>
          <para>
            The <literal>.yupdate.pre</literal> script activates the
            changes, it restarts the back-end if needed.
          </para>
        </listitem>
      </itemizedlist>
    </section>
  </section>
</article>
