<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="remote-access-to-the-graphical-interface">
    <title>Remote access to the graphical interface</title>
    <para>
      The Live ISO automatically starts a graphical interface (using the
      local browser). However, you might want to access remotely to the
      installer. If you know the IP address of the system, you just need
      to point your browser to <literal>https://$IP</literal>.
    </para>
    <para>
      For the case you do not know the address, or just for convenience,
      the Live ISO is configured to use
      <link xlink:href="https://en.wikipedia.org/wiki/Multicast_DNS">Multicast
      DNS</link> (sometimes called mDNS, Avahi, Zeroconf, Bonjour) for
      hostname resolution. Therefore, connecting to
      <literal>https://agama.local</literal> should do the trick.
    </para>
    <para>
      :::warning[Beware of the <literal>.local</literal> hostname]
    </para>
    <para>
      Do not use the <literal>.local</literal> hostnames in untrusted
      networks (like public WiFi networks, shared networks), it is a
      security risk. An attacker can easily send malicious responses for
      the <literal>.local</literal> hostname resolutions and point you
      to a wrong Agama instance which could for example steal your root
      password!
    </para>
    <para>
      :::
    </para>
    <para>
      The mDNS resolution also works for other services like ping or
      SSH. So you can use commands like:
    </para>
    <programlisting>
ping agama.local
ssh root@agama.local
</programlisting>
    <para>
      If you want to know more about the mDNS support, please check the
      <link xlink:href="../user/guides/remote.md">remote access
      documentation</link>.
    </para>
  </section>
</article>
