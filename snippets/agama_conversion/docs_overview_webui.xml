<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="interactive-installation-process">
    <title>Interactive installation process</title>
    <para>
      Assuming that you are already connected to the system you want to
      install, let’s go briefly through the installation workflow.
    </para>
    <section xml:id="product">
      <title>Product selection</title>
      <para>
        Agama allows installing several SUSE and openSUSE-based
        distributions. The <link xlink:href="/download">openSUSE-based
        image</link> image includes
        <link xlink:href="https://www.opensuse.org/#Tumbleweed">Tumbleweed</link>,
        <link xlink:href="https://en.opensuse.org/Portal:Slowroll">Slowroll</link>
        and
        <link xlink:href="https://get.opensuse.org/microos/">openSUSE
        Micro OS</link>. Additionally, Agama is the installer for
        <link xlink:href="https://get.opensuse.org/leap">openSUSE
        Leap</link> too.
      </para>
      <para>
        It is noteworthy to mention that the product selection does not
        only determine which software we will install, but it may affect
        other aspects like the system’s partitioning.
      </para>
      <figure>
        <title>Product selection screen offering Slowroll, MicroOS and
        Tumbleweed</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/product-selection.png" />
          </imageobject>
          <textobject><phrase>Product selection screen offering
          Slowroll, MicroOS and Tumbleweed</phrase></textobject>
        </mediaobject>
      </figure>
      <para>
        Some products may, like SUSE Linux Enterprise Server, offer
        different installation modes (for instance, a
        <quote>standard</quote> or an <quote>immutable</quote> mode, the
        latter providing an <emphasis role="strong">immutable
        OS</emphasis> with <emphasis role="strong">transactional
        updates</emphasis>). If the selected product supports multiple
        modes, choosing one is mandatory to proceed with the
        installation.
      </para>
      <figure>
        <title>Product selection with modes</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/product-selection-modes.png" />
          </imageobject>
          <textobject><phrase>Product selection with
          modes</phrase></textobject>
        </mediaobject>
      </figure>
      <para>
        You can change the product later if you change your mind, but
        you should carefully check the configuration as some values can
        be reset.
      </para>
    </section>
    <section xml:id="layout">
      <title>General layout</title>
      <para>
        Once you select a product, Agama takes you to the
        <emphasis>overview page</emphasis>. In the main area, you can
        find a summary of the installation settings including
        localization, network, storage, software, authentication, etc.
        The installer let you know if there is an error or something is
        missing in any of those areas.
      </para>
      <figure>
        <title>Overview of the installation settings</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/overview.png" />
          </imageobject>
          <textobject><phrase>Overview of the installation
          settings</phrase></textobject>
        </mediaobject>
      </figure>
      <para>
        You can find the button to start the installation in the bottom
        of this <emphasis>overview page</emphasis>. Check the
        <link linkend="installing">Starting the installation</link>
        section for further information. In the image above, the
        <literal>Install</literal> button cannot actually start the
        process because Agama is asking to setup some authentication
        method first.
      </para>
      <para>
        Finally, at the top right corner there is another button to
        change the localization settings for the installer and a menu
        offering some special options, like the possibility of changing
        the chosen product as discussed above, downloading the logs or
        the current configuration in JSON format.
      </para>
      <figure>
        <title>Menu with special settings like changing product</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/installer-menu.png" />
          </imageobject>
          <textobject><phrase>Menu with special settings like changing
          product</phrase></textobject>
        </mediaobject>
      </figure>
      <para>
        Now we are ready to browse the different sections of the
        installer in no particular order, making use of the freedom of
        movements provided by the Agama interface.
      </para>
    </section>
    <section xml:id="authentication">
      <title>Authentication</title>
      <para>
        Most likely you need a mechanism to access the system once it is
        installed. And we already know Agama forces us to setup some
        authentication before proceeding with the installation. So let’s
        start by inspecting that area.
      </para>
      <para>
        There Agama offers you to set up a <literal>root</literal>
        authentication method (a password or a public key) and/or to
        create a dedicated user. As mentioned, it is mandatory to do any
        before starting the installation.
      </para>
      <figure>
        <title>Users section including the root authentication and the
        dedicated user</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/users.png" />
          </imageobject>
          <textobject><phrase>Users section including the root
          authentication and the dedicated user</phrase></textobject>
        </mediaobject>
      </figure>
      <para>
        Once authentication is set, it would be possible to just
        <link linkend="installing">start the installation</link> unless
        Agama reports other issues. But even if no issues are reported,
        it is always a good idea to take a look to the different areas
        of the Agama interface before proceeding.
      </para>
    </section>
    <section xml:id="network">
      <title>Network</title>
      <para>
        Agama makes it easy to adjust your network configuration. It
        allows setting up wired and wireless devices, specifying the
        mode (DHCP/manual), IP addresses, name servers, etc. Advanced
        connections types, like bridges or bonds, do not have support in
        the user interface yet. However, it is a matter of time that
        they get added given that Agama supports many of them during an
        <link xlink:href="./unattended.mdx">unattended
        installation</link> or using the
        <link xlink:href="./cli.md">command-line interface</link>.
      </para>
      <warning>
        <para>
          Network changes are applied instantly Beware that network
          configuration changes are applied instantly. So you must be
          careful when adjusting the network configuration on a remote
          installation.
        </para>
      </warning>
      <figure>
        <title>Network settings including wired and wireless
        devices</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/network.png" />
          </imageobject>
          <textobject><phrase>Network settings including wired and
          wireless devices</phrase></textobject>
        </mediaobject>
      </figure>
    </section>
    <section xml:id="storage">
      <title>Storage</title>
      <para>
        Setting up the different file systems and their corresponding
        logical devices -like partitions, LVM volumes or RAIDs- is one
        of the most complex aspects of the installation.
      </para>
      <para>
        The starting point at the Agama interface is pretty simple, you
        can choose the disk to install into and decide what to do in
        order to find space for the new system in that disk, like
        deleting or shrinking the current partitions.
      </para>
      <figure>
        <title>Overview of the storage configuration page</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/storage-overview.png" />
          </imageobject>
          <textobject><phrase>Overview of the storage configuration
          page</phrase></textobject>
        </mediaobject>
      </figure>
      <para>
        But Agama’s storage user interface offers many more options that
        are described on its own
        <link xlink:href="/docs/user/guides/storage">separate
        document</link>. Nevertheless, similar to the case of the
        <emphasis role="strong">Network</emphasis> area described
        <link linkend="network">above</link>, the full Agama potential
        (like creating software MD RAIDs) is currently only accesible
        using the unattended installation or the command-line interface.
      </para>
    </section>
    <section xml:id="registration">
      <title>Registration</title>
      <para>
        If a product must be registered (e.g., SUSE Linux Enterprise
        Server 16), then a
        <emphasis role="strong">Registration</emphasis> section is
        offered. In that section, the product can be registered through
        the SUSE Customer Center server or specifying a custom
        registration server
        (<link xlink:href="https://documentation.suse.com/en-us/sles/15-SP7/html/SLES-all/rmt-overview.html">RMT</link>
        or SCC proxy).
      </para>
      <figure>
        <title>Product registration</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/registration.png" />
          </imageobject>
          <textobject><phrase>Product registration</phrase></textobject>
        </mediaobject>
      </figure>
      <para>
        The product will be registered using the hostname configured in
        the <emphasis role="strong">Hostname</emphasis> section. Note
        that the hostname cannot be changed after registering the
        product.
      </para>
    </section>
    <section xml:id="localization">
      <title>Localization</title>
      <para>
        This section allows to select the language, keyboard layout and
        timezone that the system you are installing will use. Just click
        on the corresponding <emphasis role="strong">Change</emphasis>
        button to change any of them.
      </para>
      <figure>
        <title>Language, keyboard layout and timezone options</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/localization.png" />
          </imageobject>
          <textobject><phrase>Language, keyboard layout and timezone
          options</phrase></textobject>
        </mediaobject>
      </figure>
      <para>
        As there are dozens of possible values, Agama features an small
        search box to make it easier to find the value you want to
        select.
      </para>
      <figure>
        <title>Filtering the list of languages by country</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/select-language.png" />
          </imageobject>
          <textobject><phrase>Filtering the list of languages by
          country</phrase></textobject>
        </mediaobject>
      </figure>
    </section>
    <section xml:id="software">
      <title>Software</title>
      <para>
        Agama software selection is rather simple. Depending on the
        product, it offers a reduced set of software patterns. The image
        below partially shows how the software selector looks like. As
        in other places of the interfaces, Agama offers a search box. If
        you need any advanced package selection, we suggest to do it
        once the system is installed.
      </para>
      <figure>
        <title>Software selection including a short list of software
        patterns</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/software.png" />
          </imageobject>
          <textobject><phrase>Software selection including a short list
          of software patterns</phrase></textobject>
        </mediaobject>
      </figure>
    </section>
    <section xml:id="installing">
      <title>Starting the installation</title>
      <para>
        As mentioned above, the <literal>Install</literal> button is
        always present, but with minor changes depending on the
        situation:
      </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>
            When it is not possible to start the installation, usually
            because there is some configuration problem you need to
            solve first, the button is disabled.
          </para>
        </listitem>
        <listitem>
          <para>
            When the installation might cause data loss, for instance
            removing existing file systems, the button text is adapted
            accordingly (<emphasis>Install now with potential data
            loss</emphasis>).
          </para>
        </listitem>
        <listitem>
          <para>
            In other case, the button label reads <emphasis>Install
            now</emphasis>.
          </para>
        </listitem>
      </itemizedlist>
      <para>
        Once you press the button, Agama will ask for confirmation.
      </para>
      <figure>
        <title>Confirmation after pressing the
        <literal>Install</literal> button</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/install-button.png" />
          </imageobject>
          <textobject><phrase>Confirmation after pressing the
          <literal>Install</literal> button</phrase></textobject>
        </mediaobject>
      </figure>
    </section>
  </section>
</article>
