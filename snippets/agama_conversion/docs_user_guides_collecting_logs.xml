<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="collecting-logs">
    <title>Collecting logs</title>
    <section xml:id="getting-agama-logs-from-an-installed-system">
      <title>Getting Agama logs from an installed system</title>
      <para>
        If the installation was successful and you can boot into the
        newly installed system and log in, get the Agama logs from
        <literal>/var/log/agama-installation</literal>.
      </para>
    </section>
    <section xml:id="from-the-agama-web-ui">
      <title>From the Agama web UI</title>
      <para>
        From a running Agama installation, use the
        <literal>(v)</literal> (circle with a down arrow) in the top
        right corner to get the <quote>Installer</quote> menu, then
        <quote>Download logs</quote>.
      </para>
      <section xml:id="local-installation">
        <title>Local installation</title>
        <para>
          In a local installation (i.e. running the installer web user
          interface on the same machine that you are installing), this
          will create a logs tarball in
          <literal>/root/Downloads/agama-logs.tar.gz</literal>. But that
          is on a RAM disk; you need to copy it to a permanent location,
          i.e. over the network or to a USB stick or SD card. See
          section
          <link linkend="saving-the-logs-to-a-permanent-location">saving
          the logs to a permanent location</link> below.
        </para>
        <para>
          If you used <quote>Show downloads</quote>, and you get stuck
          in the <quote>downloads</quote> window, use the
          <quote>Organize</quote> menu, then the last item
          <quote>Close</quote> (or
          <literal>Ctrl</literal>+<literal>W</literal>) to go back to
          the normal Agama window.
        </para>
      </section>
      <section xml:id="remote-installation">
        <title>Remote installation</title>
        <para>
          In a remote installation (i.e. if you connected a web browser
          running on your normal workstation computer to the machine
          that you are installing), the logs tarball is downloaded
          directly to the download directory on your workstation.
        </para>
      </section>
    </section>
    <section xml:id="the-agama-logs-store-command">
      <title>The <quote>agama logs store</quote> command</title>
      <para>
        In a running Agama live ISO (i.e. when booted from the
        installation ISO), use this command to collect logs:
      </para>
      <programlisting>
agama logs store
</programlisting>
      <para>
        It will tell you the exact path and filename where it stored the
        logs.
      </para>
      <para>
        See also the
        <link xlink:href="../reference/cli#agama-logs-store">agama logs
        store command line reference</link>.
      </para>
      <section xml:id="beware-ram-disk">
        <title>Beware: RAM disk!</title>
        <para>
          Notice that you will still have to copy them to a permanent
          location; the installation runs from a RAM disk at this point
          that will be gone when the Agama live session terminates, or
          when the installation boots (or tries to boot) into the
          freshly installed system.
        </para>
        <para>
          See section
          <link linkend="saving-the-logs-to-a-permanent-location">saving
          the logs to a permanent location</link> below.
        </para>
      </section>
      <section xml:id="waiting-for-the-right-time">
        <title>Waiting for the right time</title>
        <para>
          Of course it’s important to pick the right time to collect the
          logs; they are typically needed for a bug report, so they
          should be collected when the problematic part happened, not
          before, so the logs actually contain the relevant information.
        </para>
        <para>
          If the installation works all the way to the end, that’s easy:
          Wait until the <quote>reboot now</quote> page appears and then
          collect the logs.
        </para>
        <para>
          If there is a crash during the installation, and you can still
          access a shell (a text console), do it after the crash.
        </para>
        <para>
          If the whole live system hangs or crashes at a random point,
          it’s tricky; then you may have to use boot parameters to
          enable an ssh session from the outside, log in via that ssh
          session and hope for the best (i.e. that your ssh session
          remains useable).
        </para>
      </section>
    </section>
    <section xml:id="shell">
      <title>How to get a shell</title>
      <section xml:id="switching-to-a-text-console">
        <title>Switching to a text console</title>
        <para>
          If you are running Agama in graphical mode on the system
          console, you can switch a text console (no password needed)
          with
          <literal>Ctrl</literal>+<literal>Alt</literal>+<literal>F8</literal>.
        </para>
        <para>
          Then log in as <quote>root</quote> and use the password just
          above the login prompt. Notice that this password is freshly
          generated; it’s different each time the live system is booted.
        </para>
        <para>
          Ignore the QR code on the console: That does NOT represent the
          password, it’s the URL with the IP of the machine that is
          being installed; something like
          <literal>https://192.168.178.171</literal>.
        </para>
        <para>
          Your keyboard may still be using the US layout (QWERTY); if
          you are used to a national keyboard layout (e.g. German
          QWERTZ), some letters or special characters may be on
          different keys.
        </para>
        <section xml:id="back-to-the-graphical-session">
          <title>Back to the graphical session</title>
          <para>
            Use
            <literal>Ctrl</literal>+<literal>Alt</literal>+<literal>F2</literal>
            or just <literal>Alt</literal>+<literal>F2</literal> to get
            back to the graphical session.
          </para>
        </section>
        <section xml:id="virtualbox-etc.-on-screen-keyboard">
          <title>VirtualBox etc.: On-screen keyboard</title>
          <para>
            If running in a virtualization environment like VirtualBox,
            you may want to use the on-screen keyboard to use those
            <literal>Ctrl</literal>+<literal>Alt</literal>+<literal>Fx</literal>
            key combinations (VirtualBox: Menu <quote>Input</quote>
            -&gt; <quote>Keyboard</quote> -&gt; <quote>Soft
            Keyboard</quote>). This may also help with the keyboard
            layout problem when entering the password.
          </para>
          <para>
            Notice that you need to click the <literal>Shift</literal>
            key on the on-screen keyboard to get upper-case letters;
            using the physical <literal>Shift</literal> key on your
            normal keyboard will probably not have any effect.
          </para>
        </section>
      </section>
    </section>
    <section xml:id="saving-the-logs-to-a-permanent-location">
      <title>Saving the logs to a permanent location</title>
      <para>
        Since the installation runs from a RAM disk, you need to copy
        any collected logs to a permanent location: One of
      </para>
      <itemizedlist>
        <listitem>
          <para>
            A networked machine
          </para>
        </listitem>
        <listitem>
          <para>
            A mounted storage medium (USB stick, SD card)
          </para>
        </listitem>
      </itemizedlist>
      <section xml:id="copying-over-the-network">
        <title>Copying over the network</title>
        <para>
          From the machine where the installation is running:
        </para>
        <programlisting>
scp /tmp/agama-logs*.tar.gz root@myworkingmachine:/tmp
</programlisting>
        <para>
          Or use the IP address of the target machine: (find out on that
          machine with the <literal>ip a</literal>command)
        </para>
        <programlisting>
scp /tmp/agama-logs*.tar.gz root@192.176.178.42:/tmp
</programlisting>
      </section>
      <section xml:id="copying-on-a-usb-stick-or-sd-card">
        <title>Copying on a USB stick or SD card</title>
        <para>
          Plug in the USB stick or SD card, then check what device name
          it uses with
        </para>
        <programlisting>
lsblk
</programlisting>
        <para>
          Then mount that device, copy the agama logs file and unmount
          the device again:
        </para>
        <programlisting>
mount /dev/sdx1 /mnt
cp /tmp/agama-logs*.tar.gz /mnt
umount /mnt
</programlisting>
        <para>
          (Assuming the device for the USB stick or SD card was
          <literal>/dev/sdx1</literal>)
        </para>
        <para>
          If <literal>/mnt</literal> was busy at that point, create a
          new directory (say, <literal>/mnt2</literal>) and use that one
          instead.
        </para>
      </section>
    </section>
  </section>
</article>
