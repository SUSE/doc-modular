<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="editing-autoinstallation-profile">
    <title>Editing autoinstallation profile</title>
    <para>
      The auto-installation profile uses a JSON format which you can
      write in any text editor. However, some editors provide automatic
      validation and code completion (suggesting the keys or values to
      use) which makes editing much easier.
    </para>
    <para>
      The most common editor which supports validation and code
      completion is the
      <link xlink:href="https://code.visualstudio.com/">VSCode
      IDE</link>, but there are some more.
    </para>
    <section xml:id="specifying-the-profile-schema">
      <title>Specifying the profile schema</title>
      <para>
        Unlike the XML format, JSON does not natively support
        referencing the used validation schema. But some editors support
        unofficial schema specification using the
        <literal>$schema</literal> key which can point to the URL with
        the schema definition.
      </para>
    </section>
    <section xml:id="schema-urls">
      <title>Schema URLs</title>
      <para>
        For the SUSE SLES-16.0 or openSUSE Leap 16.0 profiles you can
        use this line:
      </para>
      <programlisting language="json">
{
  &quot;$schema&quot;: &quot;https://raw.githubusercontent.com/agama-project/agama/refs/heads/SLE-16/rust/agama-lib/share/profile.schema.json&quot;
}
</programlisting>
      <para>
        For the profiles for the openSUSE Tumbleweed installer using the
        latest development version use this line:
      </para>
      <programlisting language="json">
{
  &quot;$schema&quot;: &quot;https://raw.githubusercontent.com/agama-project/agama/refs/heads/master/rust/agama-lib/share/profile.schema.json&quot;
}
</programlisting>
    </section>
    <section xml:id="notes">
      <title>Notes</title>
      <para>
        The schema URL is used only for validation in the editor while
        writing the profile, the Agama installer always uses its own
        embedded schema regardless of the <literal>$schema</literal>
        value. This value is completely ignored by the installer.
      </para>
      <warning>
        <para>
          The <literal>$schema</literal> key in the installation profile
          is currently accepted only in the openSUSE Tumbleweed based
          installer. You have to remove this key before using the
          profile in the SUSE SLES-16.0 or openSUSE Leap 16.0
          installations otherwise the installer will complain about not
          valid profile.
        </para>
      </warning>
    </section>
  </section>
</article>
