<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="activate-multipath-during-installation">
    <title>Activate multipath during installation</title>
    <para>
      Using
      <link xlink:href="https://en.wikipedia.org/wiki/Linux_DM_Multipath">multipath</link>
      offers fault-tolerance by defining more than one physical path
      between each storage device and the CPU. In order to get it
      benefits, the multipath subsystem must be activated by Agama
      during the installation process. Once multipath is activated, it
      affect all devices in the system. So it is expected that only the
      corresponding multipath device name will then be used to reference
      any disk.
    </para>
    <para>
      Agama currently offers several mechanisms for the user to specify
      that multipath must be activated, although several improvements in
      the area are expected in future versions.
    </para>
    <section xml:id="force-activation">
      <title>Force activation</title>
      <para>
        Agama will always activate multipath if the environment variable
        <literal>LIBSTORAGE_MULTIPATH_AUTOSTART</literal> is set. The
        easiest way to set that variable during the installation process
        is to use the following boot argument.
      </para>
      <programlisting>
LIBSTORAGE_MULTIPATH_AUTOSTART=1
</programlisting>
      <para>
        If you are certain the system uses multipath, this would be the
        most robust method to ensure that current versions of Agama
        activate the corresponding subsystems.
      </para>
      <figure>
        <title>Enforcing multipath with a boot argument</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/multipath-boot.png" />
          </imageobject>
          <textobject><phrase>Enforcing multipath with a boot
          argument</phrase></textobject>
        </mediaobject>
      </figure>
    </section>
    <section xml:id="conditional-activation">
      <title>Conditional activation</title>
      <para>
        If multipath activation is not enforced with the mentioned boot
        argument, then Agama will always try to detect whether the
        system contains any multipath device. If that is the case, Agama
        will ask whether multipath must be activated. If no multipath
        devices are detected, Agama will proceed without asking. Bear in
        mind that detection of multipath devices is not 100% reliable.
      </para>
      <para>
        In case Agama gets to the point of raising the mentioned
        question, the answer can be set beforehand at the Agama
        configuration or provided interactively.
      </para>
      <para>
        The latter case is as straightforward as answering to the
        corresponding dialog at the Agama web interface or,
        alternatively, using the
        <link xlink:href="../reference/cli#agama-questions"><literal>agama questions</literal></link>
        command.
      </para>
      <figure>
        <title>Multipath detection at the web interface</title>
        <mediaobject>
          <imageobject>
            <imagedata fileref="/img/user/multipath-web.png" />
          </imageobject>
          <textobject><phrase>Multipath detection at the web
          interface</phrase></textobject>
        </mediaobject>
      </figure>
      <para>
        Agama can also be configured to activate multipath if detected,
        without waiting for any interaction. For that, a JSON
        configuration containing the following settings must be loaded.
      </para>
      <programlisting language="json">
{
  &quot;questions&quot;: {
    &quot;answers&quot;: [
      {
        &quot;class&quot;: &quot;storage.activate_multipath&quot;,
        &quot;answer&quot;: &quot;yes&quot;
      }
    ]
  }
}
</programlisting>
      <para>
        Read <link xlink:href="../reference/profile/answers">the profile
        format reference</link> for more info about specifying answers
        at the Agama configuration.
      </para>
      <para>
        Also check the starter’s guide if you need any clarification
        regarding how to load an Agama configuration using
        <link xlink:href="/docs/overview/cli">the command-line
        tools</link> or during boot during an
        <link xlink:href="/docs/overview/unattended">unattended
        installation</link>.
      </para>
    </section>
    <section xml:id="future-improvements">
      <title>Future improvements</title>
      <para>
        The mechanisms described in this document will keep working in
        the foreseeable future, but upcoming versions of Agama will add
        more explicit and powerful methods to configure multipath making
        use of the JSON configuration.
      </para>
      <para>
        Thus, users heavily relying at multipath may consider revisiting
        this document in the future.
      </para>
    </section>
  </section>
</article>
