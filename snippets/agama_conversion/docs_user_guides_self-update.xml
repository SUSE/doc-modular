<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="self-update">
    <title>Self-update</title>
    <para>
      To fix possible bugs and include new features the SLE-16-QU0
      installer includes a self-update process. This process
      automatically updates the installer before starting the
      installation to fix some problems or allow adding new features.
    </para>
    <section xml:id="limitations">
      <title>Limitations</title>
      <para>
        The self-update process can update most of the components
        present on the installation medium. It can update the installer
        with dependent libraries and tools, add new languages, etc…
      </para>
      <para>
        However, there are several parts of the system which cannot be
        updated by the self-update:
      </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>
            The boot loader on the medium, the used Linux kernel and the
            initramfs image.
          </para>
        </listitem>
        <listitem>
          <para>
            The self-update process itself.
          </para>
        </listitem>
        <listitem>
          <para>
            The included packages on the Full installation medium (to
            install updated. packages register your system)
          </para>
        </listitem>
      </itemizedlist>
    </section>
    <section xml:id="disabling-the-self-update">
      <title>Disabling the self-update</title>
      <para>
        The self update process is enabled by default, but it can be
        explicitly disabled by using the
        <literal>inst.self_update=0</literal> boot option. With this
        option it is completely skipped and the system uses the original
        installer without changing anything.
      </para>
    </section>
    <section xml:id="workflow">
      <title>Workflow</title>
      <para>
        The self-update step runs very early in the process. It runs in
        the initramfs system before switching the root to full installer
        root image. The advantage is that it can update the installer or
        other services without need for restarting them. This ensures
        that the services are already started in the updated versions.
        That’s especially important for the security fixes.
      </para>
      <itemizedlist>
        <listitem>
          <para>
            By default the self-update contacts SCC and asks it for the
            self-update repository for the product present on the
            installation medium (the self-update repository is different
            than the regular updates released for the installed system).
          </para>
        </listitem>
        <listitem>
          <para>
            The self-update repository is added to the system, it is
            refreshed and then <literal>zypper update</literal> is
            called. This should update all packages present in the
            running installation system to newer versions available in
            the self-update repository.
          </para>
        </listitem>
        <listitem>
          <para>
            The dependencies are evaluated so dependent packages are
            updated as well. If there is some dependency issue blocking
            the update then it fails without changing anything.
          </para>
        </listitem>
        <listitem>
          <para>
            If contacting the SCC server fails or it returns an empty
            response then the self-update uses a builtin fallback
            repository URL which should be the same as the repository
            returned by SCC.
          </para>
        </listitem>
        <listitem>
          <para>
            The self-update repository is disabled at the end, but it is
            kept in the system.
          </para>
        </listitem>
      </itemizedlist>
      <para>
        <emphasis>Note: The self-update process uses the Live system
        root (<literal>/</literal>) while the Agama installer runs the
        software stack in a different root
        (<literal>/run/agama/zypp</literal>) so the self-update process
        and the repositories used for installing the target system
        cannot influence each other.</emphasis>
      </para>
      <section xml:id="restarting-self-update">
        <title>Restarting self-update</title>
        <para>
          If the self-update fails or you want to simply run the process
          again then manually restart the self-update service using the
          <literal>systemctl restart live-self-update</literal> command.
        </para>
        <para>
          But when restarting the self-update in the running root image
          (when the installer is already running) the updated services
          need to be manually restarted to take effect. That depends on
          which packages were updated, check the output of the
          self-update service to see the list of updated packages and
          restart the relevant services.
        </para>
        <para>
          When restarting the self-update in the initramfs emergency
          shell invoked by the <literal>rd.break</literal> boot option
          no restart should be needed because the updated installer and
          the services are not running yet and they will be started
          later in the already updated versions.
        </para>
      </section>
    </section>
    <section xml:id="network-configuration">
      <title>Network configuration</title>
      <para>
        By default the network is automatically configured with DHCP
        protocol. If a static network configuration is needed then it
        must be manually configured via the <literal>ip=</literal> boot
        options. See the Network section in
        <literal>man dracut.cmdline</literal> or in the
        <link xlink:href="https://man7.org/linux/man-pages/man7/dracut.cmdline.7.html">online
        documentation</link>.
      </para>
      <para>
        If the self-update is manually disabled with
        <literal>inst.self_update=0</literal> boot option then the
        network configuration is skipped and it is done later in the
        boot process.
      </para>
      <section xml:id="network-proxy">
        <title>Network proxy</title>
        <para>
          If you need to use a network proxy for communication with SCC
          or for downloading the updates you can specify it with the
          <literal>proxy=&lt;URL&gt;</literal> boot option.
        </para>
        <para>
          If you need to use a user name with password for the proxy
          then include it in the proxy URL, example:
          <literal>proxy=http://user:password@example.com</literal>. If
          the username or the password contains special reserved
          characters like <literal>@</literal> or <literal>:</literal>
          then they must be
          <link xlink:href="https://en.wikipedia.org/wiki/Percent-encoding#Reserved_characters">percent-encoded</link>
          in the URL.
        </para>
      </section>
    </section>
    <section xml:id="custom-repository">
      <title>Custom repository</title>
      <para>
        A custom self-update repository can be used with the
        <literal>inst.self_update=&lt;URL&gt;</literal> boot option. In
        this case the SCC step is skipped and the specified repository
        is directly used as the self-update source. When using a custom
        repository you might need to use some less strict security
        checks. See the security section below.
      </para>
    </section>
    <section xml:id="rmt">
      <title>RMT</title>
      <para>
        An RMT server can be used for downloading the updates instead of
        the default SCC server with the
        <literal>inst.register_url=&lt;URL&gt;</literal> boot option.
        The process is the same, the only difference is that the
        specified RMT server is used instead of the SCC.
      </para>
      <para>
        The advantage is that RMT will return the self-update repository
        present on the RMT server itself so the packages will be
        downloaded locally and not from the internet.
      </para>
      <para>
        In most cases RMT uses a self-signed SSL certificate, see the
        <literal>inst.self_update_ssl=0</literal> option below.
      </para>
    </section>
    <section xml:id="security-related-options">
      <title>Security related options</title>
      <para>
        By default all usual security checks are applied (verifying the
        SSL certificates, verifying the repository and packages GPG
        signatures), just like in an installed system. This ensures the
        integrity and security of the self-update process and of the
        updated installer.
      </para>
      <para>
        However, in some special cases you might need to disable some
        security related features.
      </para>
      <warning>
        <para>
          Using any options below decreases the security of the
          self-update process and potentially can even make the system
          vulnerable to security attacks!
        </para>
        <para>
          Use these options with caution! While using these options in a
          trusted network could be acceptable, they should be avoided
          when accessing the resources on the internet.
        </para>
      </warning>
      <para>
        Currently these boot options are supported:
      </para>
      <itemizedlist>
        <listitem>
<literallayout><literal>inst.self_update_ssl=0</literal> ignore the SSL certificate problems when contacting the registration server and when downloading packages from the self-update repository via the HTTPS protocol. This option is needed when using an RMT server with a self-signed certificate.
<emphasis>Note: This is basically almost the same as using the insecure HTTP protocol, which means that the authenticity of the remote server is not verified.</emphasis></literallayout>
        </listitem>
        <listitem>
          <para>
            <literal>inst.self_update_unsigned_repo=1</literal> - allow
            using an unsigned self-update repository (but the package
            GPG signatures are still verified)
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>inst.self_update_import_key=1</literal> -
            automatically trust and import the GPG key from the
            repository. Should be used with HTTPS repositories without
            disabling the SSL checks to at least ensure that the correct
            server is used.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>inst.self_update_gpg=0</literal> - completely
            disables all GPG checks, allows installing packages with
            unknown GPG signatures or even broken or modified packages.
            This is very insecure and if possible should be avoided.
          </para>
        </listitem>
      </itemizedlist>
    </section>
    <section xml:id="live-iso-package-index">
      <title>Live ISO package index</title>
      <para>
        The Live ISO contains the
        <literal>/LiveOS/.packages.gz</literal> file with list of
        packages included in the root image. This is a static file and
        includes the packages in the original image before running the
        self-update. After running the self-update some packages might
        be updated but the file is obviously not updated. If you need to
        get the included packages with updates run the
        <literal>rpm -qa</literal> command.
      </para>
    </section>
    <section xml:id="debugging">
      <title>Debugging</title>
      <para>
        Here are some hints what to do if the self-update process fails:
      </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>
            <literal>systemctl status live-self-update</literal> - check
            the result of the self-update process, see whether it
            succeeded or failed
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>journalctl -t live-self-update</literal> - show the
            log with details of the self-update run
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>systemctl restart live-self-update</literal> -
            start the self-update process again
          </para>
        </listitem>
      </itemizedlist>
      <para>
        The self-update process creates several helper files in the
        <literal>/run/live-self-update</literal> subdirectory. They are
        not deleted at the end because they could be helpful when
        diagnosing problems:
      </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>
            <literal>result</literal> - contains the exit code of the
            main self-update script. On success it contains the
            <literal>0</literal> value, on error it contains a non-zero
            exit code. When the self-update is disabled the file is not
            created.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>skip</literal> - flag file, if it exists the
            self-update was disabled with the
            <literal>inst.self_update=0</literal> boot option.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>server_response</literal> - contains the raw
            response from the SCC or RMT registration server in JSON
            format.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>reg_repos</literal> - contains the self-update
            repository URL from the SCC or RMT server, it is extracted
            from the <literal>server_response</literal> file.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>repositories</literal> - contains the custom
            self-update repository URL obtained from the boot command
            line.
          </para>
        </listitem>
      </itemizedlist>
    </section>
  </section>
</article>
