<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="network-time-protocol-ntp">
    <title>Network Time Protocol (NTP)</title>
    <para>
      It is crucial that the system you are installing has the right
      date and time. Otherwise, you can face some weird errors during
      the installation. And that’s where the Network Time Protocol (NTP)
      comes in.
    </para>
    <para>
      The Agama installation medium ships
      <link xlink:href="https://chrony-project.org/">chrony</link>,
      which will synchronize the system date and time automatically so
      you do not need to care about it. However, there are some
      situations where you might want to force Agama to use a specific
      NTP server.
    </para>
    <para>
      The configuration file might include an <literal>ntp</literal>
      section where you can define a list of time sources to use. If
      that list if given, Agama will force <literal>chrony</literal> to
      synchronize using those sources and it will copy the configuration
      to the installed system.
    </para>
    <programlisting language="json">
{
  &quot;ntp&quot;: {
    &quot;sources&quot;: [
      {
        &quot;type&quot;: &quot;pool&quot;,
        &quot;address&quot;: &quot;2.opensuse.pool.ntp.org&quot;,
        &quot;iburst&quot;: true,
        &quot;offline&quot;: false
      }
    ]
  }
}
</programlisting>
    <para>
      The <literal>ntp</literal> section includes a list of
      <literal>sources</literal> to define the systems to synchronize
      with. Each source can specify:
    </para>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          <literal>address</literal>: the hostname or IP address of the
          NTP source.
        </para>
      </listitem>
      <listitem>
        <para>
          <literal>type</literal>: the type of NTP source. It may use:
        </para>
        <itemizedlist spacing="compact">
          <listitem>
            <para>
              <literal>pool</literal>: a pool of NTP servers (e.g.,
              <literal>pool.ntp.org</literal>). The pool name will be
              resolved to multiple servers and chrony will use the best
              ones.
            </para>
          </listitem>
          <listitem>
            <para>
              <literal>server</literal>: a specific NTP server to
              synchronize time from.
            </para>
          </listitem>
          <listitem>
            <para>
              <literal>peer</literal>: an NTP peer for symmetric
              synchronization (bidirectional time exchange).
            </para>
          </listitem>
        </itemizedlist>
      </listitem>
      <listitem>
        <para>
          <literal>iburst</literal>: (optional, boolean) if set to
          <literal>true</literal>, chrony will send a burst of packets
          at startup to speed up the initial synchronization. Defaults
          to <literal>false</literal>.
        </para>
      </listitem>
      <listitem>
        <para>
          <literal>offline</literal>: (optional, boolean) if set to
          <literal>true</literal>, the source is marked as offline
          initially. This is useful for sources that are not always
          available (e.g., sources that require network connectivity
          that may not be present at boot time). Defaults to
          <literal>false</literal>.
        </para>
      </listitem>
    </itemizedlist>
    <note>
      <para>
        If you need to synchronize the date/time before Agama starts
        (e.g., if your system does not have a clock), check the
        <link xlink:href="./../boot_options"><literal>rd.ntp</literal>
        boot option</link>.
      </para>
    </note>
  </section>
</article>
