<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="software">
    <title>Software</title>
    <para>
      Depending on the <link xlink:href="./product">product you are
      installing</link>, Agama will select a set of repositories,
      patterns and packages to install. However, it is possible to add
      your own list of repositories, patterns and packages using the
      <literal>software</literal> section.
    </para>
    <para>
      This section supports following keys:
    </para>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          <literal>patterns</literal>: list of patterns to install
          (e.g., <literal>gnome</literal>, <literal>kde</literal>,
          <literal>fips</literal>, etc.). You can find a list of
          patterns for your distribution using
          <literal>zypper se --type pattern</literal>. If you specify a
          list of patterns, pre-selected patterns (e.g.,
          <literal>selinux</literal>) will not get installed unless they
          are included in the list. If you just want to add a set of
          patterns to the default selection, check the
          <link linkend="adding-and-removing-patterns">Adding and
          removing patterns</link> section.
        </para>
      </listitem>
      <listitem>
        <para>
          <literal>packages</literal>: list of packages to install
          (e.g., <literal>neovim</literal>).
        </para>
      </listitem>
      <listitem>
        <para>
          <literal>extraRepositories</literal>: list of additional
          repositories that will be used as installation source. See the
          <link linkend="user-defined-repositories">User-defined
          repositories</link> for further information.
        </para>
      </listitem>
      <listitem>
        <para>
          <literal>onlyRequired</literal>: flag that sets if agama
          should install only mandatory dependencies or also optional
          one. If omited, it will use libzypp defaults which is false.
        </para>
      </listitem>
    </itemizedlist>
    <programlisting>
{
  software: {
    patterns: [&quot;gnome&quot;, &quot;office&quot;],
    packages: [&quot;neovim&quot;, &quot;emacs&quot;],
    extraRepositories: [
      {
        alias: &quot;games&quot;,
        url: &quot;https://download.opensuse.org/repositories/games/openSUSE_Tumbleweed/&quot;,
        gpgFingerprints: [&quot;AEF1 EE83 0A27 90AB ABC2 CDA2 8F91 B0E6 C862 B42C&quot;]
      }
    ],
    onlyRequired: false
  }
}
</programlisting>
    <section xml:id="adding-and-removing-patterns">
      <title>Adding and removing patterns</title>
      <para>
        If you specify a list of patterns, pre-selected patterns like
        <literal>selinux</literal> will not get installed unless they
        are included in the list. So the following list will disable
        <literal>selinux</literal>:
      </para>
      <programlisting>
{
  software: {
    patterns: [&quot;gnome&quot;, &quot;office&quot;]
  }
}
</programlisting>
      <para>
        If you want to keep the pre-selected patterns, it might be
        better to use the <literal>add</literal> key:
      </para>
      <programlisting>
{
  software: {
    patterns: {
      add: [&quot;gnome&quot;, &quot;office&quot;]
    }
  }
}
</programlisting>
      <para>
        Additionally, there is an special <literal>remove</literal> that
        it is useful when you want to remove an specific set of
        patterns:
      </para>
      <programlisting>
{
  software: {
    patterns: {
      remove: [&quot;selinux&quot;]
    }
  }
}
</programlisting>
    </section>
    <section xml:id="user-defined-repositories">
      <title>User-defined repositories</title>
      <para>
        The <literal>software</literal> section allows you to define
        custom repositories using a list under the
        <literal>extraRepositories</literal> key. Each repository in the
        list can have the following properties:
      </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>
            <literal>alias</literal>: A unique identifier for the
            repository. Mandatory property.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>url</literal>: The repository’s URL, recognized by
            <literal>libzypp</literal>. Mandatory property.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>priority</literal>: Sets the priority for using
            this repository. A lower number means higher precedence. If
            you don’t specify this, it’ll use the same default priority
            as other repositories.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>name</literal> (string): A user-friendly name for
            the repository. If omitted, it defaults to the
            <literal>alias</literal>.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>productDir</literal>: A path to a specific product
            related to the URL. This is mainly used for multi-product
            media and isn’t used by default.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>enabled</literal>: Controls whether the repository
            is active. This is handy for pre-defining repositories you
            plan to enable later. By default, repositories are enabled.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>allowUnsigned</literal>: If set to
            <literal>true</literal>, the repository will be accepted
            even if it lacks a GPG signature. This can be useful for
            your own repositories. It’s not set by default.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>gpgFingerprints</literal>: A list of accepted GPG
            fingerprints for the repository. This is helpful if the
            repository is signed by a GPG key not included in your
            installation medium. This list is empty by default.
          </para>
        </listitem>
      </itemizedlist>
    </section>
  </section>
</article>
