<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article>
<article
  xmlns="http://docbook.org/ns/docbook" version="5.0"
  xmlns:xlink="http://www.w3.org/1999/xlink" >
  <info>
    <title></title>
  </info>
  <section xml:id="user-and-root">
    <title>User and root</title>
    <para>
      There are two sections related to users and authentication in an
      Agama profile:
    </para>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          <literal>root</literal>: allows to set the authentication
          mechanism for the <literal>root</literal> user.
        </para>
      </listitem>
      <listitem>
        <para>
          <literal>user</literal>: defines a <quote>first user</quote>.
        </para>
      </listitem>
    </itemizedlist>
    <para>
      For a successful installation, one of the following conditions
      must be met:
    </para>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          A password or an SSH public key is defined for the
          <literal>root</literal> user.
        </para>
      </listitem>
      <listitem>
        <para>
          A first user is defined.
        </para>
      </listitem>
    </itemizedlist>
    <section xml:id="root-user">
      <title><literal>root</literal> user</title>
      <para>
        The <literal>root</literal> section allows to specify an
        authentication method for the <literal>root</literal> user.
      </para>
      <programlisting>
{
  root: {
    sshPublicKey: &quot;ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPj/J1N38v/7Gbxz5A6jiSBhLbUwrJOVlBxDQhuW8tvg user@example.net&quot;,
    hashedPassword: true,
    password: &quot;$y$j9T$qGMrxRmEHUV3kCXQAywcP1$g4n4.O2tUf2Dq9CJTohTb4/HJ9Wdzr2Z33MD8srkPV1&quot;
  }
}
</programlisting>
      <para>
        The following fields are supported:
      </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>
            <literal>password</literal>: <literal>root</literal>
            password, hashed or not depending on
            <literal>hashedPassword</literal>.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>hashedPassword</literal>: whether the
            <literal>password</literal> is hashed
            (<literal>true</literal>) or not (<literal>false</literal>
            or undefined).
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>sshPublicKey</literal>: SSH public key to be copied
            to the <literal>/root/.ssh/authorized_keys</literal> file.
            It can be single string or array of strings (keys).
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>sshPublicKeys</literal>: an alias for
            <literal>sshPublicKey</literal>, the behavior is exactly the
            same.
          </para>
        </listitem>
      </itemizedlist>
    </section>
    <section xml:id="first-user">
      <title>First user</title>
      <para>
        The <literal>user</literal> section defines a first user:
      </para>
      <programlisting>
{
  user: {
    fullName: &quot;Jane Doe&quot;,
    userName: &quot;jane.doe&quot;,
    hashedPassword: false,
    password: &quot;123456&quot;
  }
}
</programlisting>
      <para>
        This section includes the following fields:
      </para>
      <itemizedlist spacing="compact">
        <listitem>
          <para>
            <literal>userName</literal>: user name (or login name).
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>fullName</literal>: user’s full name.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>password</literal>: <literal>root</literal>
            password, hashed or not depending on
            <literal>hashedPassword</literal>.
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>hashedPassword</literal>: whether the
            <literal>password</literal> is hashed
            (<literal>true</literal>) or not (<literal>false</literal>
            or undefined).
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>sshPublicKey</literal>: SSH public key to be copied
            to the
            <literal>/home/&lt;userName&gt;/.ssh/authorized_keys</literal>
            file. It can be single string or array of strings (keys).
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>sshPublicKeys</literal>: an alias for
            <literal>sshPublicKey</literal>, the behavior is exactly the
            same.
          </para>
        </listitem>
      </itemizedlist>
    </section>
    <section xml:id="encrypted-passwords">
      <title>Encrypted passwords</title>
      <para>
        The encrypted password can be obtained by running the
        <literal>mkpasswd</literal> command from the
        <literal>whois</literal> package.
      </para>
      <para>
        To get the list of supported hashing methods run the
        <literal>mkpasswd --method=help</literal> command. Then use the
        selected method for hashing your password, for example
        <literal>mkpasswd --method=yescrypt</literal>.
      </para>
      <para>
        Make sure the selected hashing method is supported by the target
        system, different systems might support different set of
        methods.
      </para>
      <warning>
        <para>
          Do not use any DES or MD5 based algorithms, these are
          considered insecure. Check <literal>man 5 crypt</literal>
          manual page for details about the hashing methods and their
          strength.
        </para>
      </warning>
      <para>
        Alternatively you can use the
        <literal>openssl passwd -6</literal> command. This generates a
        SHA-512 password hash, for the SHA-256 method use the
        <literal>-5</literal> option.
      </para>
    </section>
  </section>
</article>
