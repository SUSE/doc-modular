<!-- include like this:
<xi:include href="../snippets/ha-cluster-restart-warning.xml"/>
-->
<!DOCTYPE important [
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
  %entities;
]>
<important xmlns="http://docbook.org/ns/docbook" version="5.2"
xmlns:xi="http://www.w3.org/2001/XInclude"
xmlns:xlink="http://www.w3.org/1999/xlink">
  <title>Cluster restart might be required</title>
  <para>
    The setup script checks whether a cluster restart is required and whether it is safe to do so
    automatically. If any non-&stonith; resources are running, the script does <emphasis>not</emphasis>
    automatically restart the cluster services. Instead, it warns you to restart the cluster services
    manually. This allows you to put the cluster into maintenance mode first to avoid resource downtime.
  </para>
</important>
