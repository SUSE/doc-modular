<!-- include like this:
<xi:include href="../snippets/ha-crmsh-cluster-scripts.xml"/>
-->
<!DOCTYPE tip [
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
  %entities;
]>
<para xmlns="http://docbook.org/ns/docbook" version="5.2"
xmlns:xi="http://www.w3.org/2001/XInclude"
xmlns:xlink="http://www.w3.org/1999/xlink">
  You can use cluster scripts to automate certain tasks. Unlike <emphasis>shell scripts</emphasis>,
  which can only contain <command>crm</command> subcommands, <emphasis>cluster scripts</emphasis>
  can also perform additional tasks, such as installing required packages, creating or modifying
  configuration files, and deploying changes to all nodes. &sleha; comes with preconfigured
  cluster scripts that are managed with the <command>crm script</command> command.
</para>
