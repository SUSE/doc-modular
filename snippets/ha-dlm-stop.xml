<!-- include like this:
<xi:include href="../snippets/ha-dlm-stop.xml"/>
-->
<!DOCTYPE warning [
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
  %entities;
]>
<step xmlns="http://docbook.org/ns/docbook" version="5.2"
xmlns:xi="http://www.w3.org/2001/XInclude"
xmlns:xlink="http://www.w3.org/1999/xlink">
  <para>
    If &dlm; and any &dlm;-dependent resources are running, stop them first. If the resources
    are in a group, you can just stop the group. If the resources aren't in a group, stop the
    &dlm;-dependent resources first and then stop &dlm;. You can stop multiple resources at once:
  </para>
<screen>&prompt.user;<command>sudo crm --wait resource stop <replaceable>RESOURCE1 RESOURCE2</replaceable></command></screen>
  <para>
    Use <option>--wait</option> to make sure the resources have really stopped before the
    command returns.
  </para>
</step>
