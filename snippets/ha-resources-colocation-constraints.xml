<!-- include like this:
<xi:include href="../snippets/ha-resources-colocation-constraints.xml"/>
-->
<!DOCTYPE para [
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
  %entities;
]>
<para xmlns="http://docbook.org/ns/docbook" version="5.2"
xmlns:xi="http://www.w3.org/2001/XInclude"
xmlns:xlink="http://www.w3.org/1999/xlink">
  Colocation constraints tell the cluster which resources can or cannot run together on the same
  node. You can specify that the resources <emphasis>must</emphasis> or must <emphasis>not</emphasis>
  run together, or give a resource an optional preference for or against another resource.
</para>
