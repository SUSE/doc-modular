<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="cgroups-resources-limits"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Configuring resource limits and accounting</title><!-- can be changed via merge
in the assembly -->
    <!-- add author's e-mail -->
    <meta name="maintainer" content="shalaka.harne@suse.com" its:translate="no"/>
    <abstract><!-- can be changed via merge in the assembly -->
      <para>
       Beyond simply organizing processes, the integration between &systemd; and cgroups provides you with the dual capability to both measure and restrict resource consumption. Before applying strict constraints, &systemd; allows you to audit exactly how much CPU, memory, or I/O a workload uses. Once you establish these baselines, you can enforce runtime limitations dynamically to protect system stability.
      </para>
    </abstract>
  </info>
  <section xml:id="cgroups-resources-limits-accounting">
    <title>Resource accounting</title>
    <para>Organizing processes into different <literal>cgroups</literal> can be used to obtain per-cgroup resource consumption data. The accounting has comparatively small but non-zero overhead, whose impact depends on the workload. Activating accounting for one unit also implicitly activates it for all units in the same slice, and for all its parent slices, and the units contained in them.</para>
<para>The accounting can be set on a per-unit basis with directives such as <literal>MemoryAccounting=</literal> or globally for all units in <filename>/etc/systemd/system.conf</filename> with the directive <literal>DefaultMemoryAccounting=</literal>.</para>
  </section>
  <section xml:id="cgroups-resources-limits-configuration">
    <title>Setting resource limits</title>
    <note>
      <title>Implicit resource consumption</title>
      <para>
Be aware that resource consumption implicitly depends on the environment where your workload executes (for example, size of data structures in libraries/kernel, forking behavior of utilities, computational efficiency). Hence it is recommended to (re)calibrate your limits should the environment change.
      </para>
    </note>
  <para>Limitations to <literal>cgroups</literal> are primarily set with the <command>systemctl
  set-property</command> command. The command syntax follows:</para>
<screen>&prompt.sudo;<command>systemctl set-property [--runtime] <replaceable>NAME</replaceable> <replaceable>PROPERTY1=VALUE</replaceable><replaceable>[PROPERTY2=VALUE]</replaceable></command></screen>
<itemizedlist><listitem><para><literal>NAME</literal>: A &systemd; service, scope, or slice name.</para></listitem>
<listitem><para><literal>--runtime</literal>: Optional. Use this if you do not want values to persist after reboot.</para></listitem>
<listitem><para><literal>PROPERTY</literal>: for a complete list, see <command>man systemd.resource-control</command>.</para></listitem>
</itemizedlist>
</section>
</topic>
