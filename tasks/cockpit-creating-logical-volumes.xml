<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="task-creating-logical-volumes"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Creating a thin logical volume</title>
    <meta name="maintainer" content="jsindelarova@suse.com" its:translate="no"/>
    <abstract>
      <para>
        The following topic describes how to create a thin logical volume from
        an existing volume group.
      </para>
    </abstract>
  </info>
  <para>
    If you have a volume group, you can create a thin logical volume as
    described below:
  </para>
  <procedure xml:id="proc-cockpit-block">
    <title>Creating a thin logical volume</title>
    <step>
      <para>
        Navigate to the <guimenu>Storage</guimenu> page.
      </para>
    </step>
    <step>
      <para>
        Click the volume group in <guimenu>Devices</guimenu>.
      </para>
    </step>
    <step>
      <para>
        In the volume group details, click <guimenu>Create new logical
        volume</guimenu>.
      </para>
    </step>
    <step>
      <para>
        Specify a logical volume name.
      </para>
    </step>
    <step>
      <para>
        Select a pool of thinly provisioned volumes.
      </para>
    </step>
    <step>
      <para>
        Select the size to use.
      </para>
    </step>
    <step>
      <para>
        Click <guimenu>Create</guimenu> to confirm the details.
      </para>
    </step>
    <step>
      <para>
        Create a thin volume by clicking <guimenu>Create thin volume</guimenu>.
      </para>
    </step>
    <step>
      <para>
        Enter a unique name.
      </para>
    </step>
    <step>
      <para>
        Select the size of the volume.
      </para>
    </step>
    <step>
      <para>
        Click <guimenu>Create</guimenu> to confirm the thin volume.
      </para>
    </step>
    <step>
      <para>
        You can create several volumes of the particular volume group by
        clicking <guimenu>Create thin volume</guimenu> again and repeating the
        steps above.
      </para>
    </step>
    <step>
      <para>
        Format the volumes by clicking <guimenu>Format</guimenu> and filling
        the details as described in
        <xref linkend="formatting-filesystems-details" xrefstyle="template:formatting file systems"/>.
      </para>
    </step>
  </procedure>
</topic>
