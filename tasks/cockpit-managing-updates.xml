<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->

<topic xml:id="task-managing-updates"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Updating &productnameshort; using &cockpit;</title>
    <meta name="maintainer" content="jsindelarova@suse.com" its:translate="no"/>
    <abstract>
      <para>
        This topic focuses on updating your system using &cockpit;.
      </para>
    </abstract>
  </info>
  <para>
   To update your system, proceed as follows: 
  </para>
  <procedure>
    <step>
      <para>
        Navigate to the <guimenu>Software Updates</guimenu> page.
      </para>
    </step>
    <step>
      <para>
        Click <guimenu>Check for Updates</guimenu> to get a list of new package
    updates and patches available for your system. It is recommended to install the patches marked as important
    as soon as possible.
      </para>
    </step>
    <step>
      <para>
        Now you can update your system either with immediate reboot or the reboot might be postponed:
      </para>
      <substeps>
        <step>
        <para>
          Click <guimenu>Update and
    Reboot</guimenu> to apply patches and updates. After the update is complete, your system will be restarted and boot into the new snapshot.
        </para>
      </step>
      <step>
        <para>
          To postpone reboot after the update, select <guimenu>Update without Reboot</guimenu> from the three dots
    menu. Bear in mind that you need to reboot the system to activate the
    snapshot with updates. If you perform further changes without rebooting the
    system beforehand, a new snapshot will be created from the same point as
    the snapshots with updates. Therefore, the new snapshot will not include
    the updates.
        </para>
      </step>
      </substeps>
    </step>
  </procedure>
</topic>
