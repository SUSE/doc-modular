<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sle/blob/main/xml/adm_sudo.xml -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="configure-kdump"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <meta name="maintainer" content="amrita.sakthivel@suse.com" its:translate="no"/>
    <title>Installing and configuring &kdump;</title>
    <abstract><para>This section describes how to install and configure &kdump;.</para></abstract>
    </info>
 <para os="sles">To install &kdump; on the immutable system, run the following command and reboot:</para>
<screen os="sles">&prompt.sudo; <command>transactional-update pkg install kdump</command></screen>
<para os="sles4sap">To install &kdump; run the following command:</para>
<screen os="sles4sap">&prompt.sudo; <command>zypper install kdump</command></screen>
<para>This command downloads the following packages:</para>
<itemizedlist>
<listitem><para><command>kdump</command></para></listitem>
<listitem><para><command>kexec-tools</command></para></listitem>
<listitem><para><command>makedumpfile</command></para></listitem>
 </itemizedlist>
 <para>To store the vmcore file remotely, you can install any of the following recommended packages:</para>
<itemizedlist>
    <listitem><para>Install the <package>cifs-utils</package> package to store on Samba stores. </para></listitem>
    <listitem><para>Install the <package>lftp</package> package to store on FTP / SFTP servers. </para></listitem>
    <listitem><para>Install the <package>nfs-client</package> package to store on NFS servers.</para></listitem>
    <listitem><para>Install the <package>openssh-clients</package> package to store over SSH.</para></listitem>
    </itemizedlist>
    <important><title>Managing &kdump; and GRUB configuration conflicts</title>
    <para>The <literal>kdump-commandline</literal> service automatically manages your crash memory allocation. It will overwrite any manual <literal>crashkernel=</literal> entries in <literal>GRUB_CMDLINE_LINUX_DEFAULT</literal> inside <filename>/etc/default/grub</filename> unless the automation is explicitly disabled.
  To prevent this behavior, modify the <literal>KDUMP_UPDATE_BOOTLOADER</literal> variable to <literal>false</literal>.</para>
 <para><emphasis role="bold">How do you stop &kdump; from overwriting GRUB configurations?</emphasis></para>
<para>To keep your manual <literal>crashkernel=</literal> settings from being erased, update your configuration with these rules:</para>
<itemizedlist>
<listitem><para>Set <literal>KDUMP_UPDATE_BOOTLOADER="false"</literal> to disable automatic updates.</para></listitem>
<listitem><para>Keep all <literal>crashkernel=</literal> parameters out of the <literal>GRUB_CMDLINE_LINUX</literal> line.</para></listitem>
<listitem><para>Manual configurations belong exclusively in <literal>GRUB_CMDLINE_LINUX_DEFAULT</literal>.</para></listitem>
</itemizedlist>
<para><emphasis role="bold">Why should you avoid adding crash kernel settings to <literal>GRUB_CMDLINE_LINUX</literal>?</emphasis></para>
<para>Adding <literal>crashkernel=</literal> to <literal>GRUB_CMDLINE_LINUX</literal> causes system conflicts.</para>
<itemizedlist>
  <listitem>
      <para>
          Settings in <literal>GRUB_CMDLINE_LINUX</literal> apply globally to all boot targets.
      </para>
  </listitem>
  <listitem>
      <para>
          This includes the rescue system, which does not run &kdump;.
      </para>
  </listitem>
  <listitem>
      <para>
          The <literal>kdump-commandline</literal> service never alters this specific line.
      </para>
  </listitem>
  <listitem>
      <para>
          Leaving values here creates direct conflicts with automated service settings.
      </para>
  </listitem>
</itemizedlist>
</important>
 <section xml:id="kdump-non-transact">
  <title>Configuring &kdump; for non-immutable systems</title>
   <para>To use &kdump;, a specific amount of system memory (RAM)—often referred to as crash kernel memory- must be permanently reserved at boot time for the capture kernel and initramfs.
    The amount of memory to reserve is specified using the <literal>crashkernel= kernel</literal> command-line parameter. &kdump; includes <literal>kdumptool calibrate</literal> and <literal>kdumptool commandline</literal> to calculate exactly how much memory needs to be reserved.</para>
      <para>When the <command> systemctl enable --now kdump</command> command triggers the main <literal>kdump.service</literal>, it automatically calls a helper service named <literal>kdump-commandline.service</literal> before attempting to load the kernel.
    This helper service verifies if the current kernel command line contains the expected <literal>crashkernel= </literal> options.
   The options are:</para>
   <itemizedlist>
    <listitem><para>Fixed memory allocation (e.g. ,<literal>256M</literal>) reserves a strict hardcoded amount of RAM.</para></listitem>
    <listitem><para>Range-based allocation (e.g. <literal>2G-8G:192M</literal> ) dynamically scales the reserved memory based on the total RAM installed in the system.</para></listitem>
    <listitem><para>Automatic allocation ( <literal>auto</literal>) where the kernel automatically calculates the required memory based on system architecture.</para></listitem>
    <listitem><para>High/low split (e.g. <literal>512M,high crashkernel=128M,low</literal>) allocates memory across different addressable zones specifically to accommodate hardware drivers on large enterprise systems.</para></listitem>
  </itemizedlist>
  <para>If the options are missing, the helper service automatically modifies the boot loader configuration to contain the crash kernel options to be applied on the next boot.</para>
<para>Before attempting to load, the <literal>kdump.service</literal>: </para>
<itemizedlist>
<listitem><para>Starts the <literal>kdump-commandline.service</literal> </para></listitem>
<listitem><para>Updates the boot loader</para></listitem>
<listitem><para>Builds the capture initramfs </para></listitem>
</itemizedlist>
<para>To verify that the service successfully checked and updated your kernel boot options, inspect the system logs:</para>
<screen>&prompt.sudo; journalctl -u kdump-commandline.service</screen>
<para>Expected output:</para>
<screen>  Started Check and update kdump options on the kernel command line.
  Kdump expects these crashkernel= values on the kernel command line:
  crashkernel=72M,low crashkernel=443M,high
  (based on the output of 'kdumptool calibrate')
  No crashkernel= found in /proc/cmdline. Kdump can't be started.
  Updated the command line options in the bootloader.
  Changes will take effect on next boot.</screen>
<para>The boot loader has been updated. Reboot the machine to apply the changes and start &kdump;.</para>
<para><emphasis role="bold">Other configurations:</emphasis></para>
<para>The <filename>/etc/sysconfig/kdump</filename> file is the primary configuration file for managing the &kdump; service.
  It includes essential options such as:</para>
  <itemizedlist>
<listitem><para> <literal>KDUMP_CRASHKERNEL</literal> for defining the reserved crash memory size</para></listitem>
<listitem><para> <literal>KDUMP_VERBOSE</literal> for increasing debugging log levels</para></listitem>
<listitem><para> <literal>KDUMP_SAVEDIR</literal> for specifying the core dump (vmcore) storage location </para></listitem>
</itemizedlist>
<example><title>Contents of a <filename>/etc/sysconfig/kdump</filename> file </title>
<screen>cat /etc/sysconfig/kdump
  ## Path:        System/Kernel/Kdump
  ## Description: Crash Dump Configuration

  ## Type:        string
  ## Default:     "auto"
  ## ServiceRestart:      kdump
  #
  # KDUMP_CRASHKERNEL is used by "kdumptool commandline" to determine
  # the value of the kernel "crashkernel=" command-line options.
  #
  # Possible values are "auto" or one or more "crashkernel=..." values.
  #
  # KDUMP_CRASHKERNEL="auto" makes "kdumptool commandline" use the default
  # values as proposed by "kdumptool calibrate".
  #
  # If the default values are not adequate you may provide a manual setting,
  # e.g., KDUMP_CRASHKERNEL="crashkernel=72M,low crashkernel=300M,high"
  #
  # See also: kdump(5).
  #
  KDUMP_CRASHKERNEL="auto"


  ## Type:        boolean
  ## Default:     "true"
  ## ServiceRestart:      kdump
  #
  # When KDUMP_UPDATE_BOOTLOADER is set to "true" "kdumptool commandline -c -U"
  # called from the kdump-commandline.service  will check if the expected kernel
  # command-line options are present and update the bootloader using *pbl*(8) if
  # not.
  #
  # Also the kdump options are removed from the command line when kdump is
  # uninstalled.
  #
  # When set to false "kdumptool commandline -c -U" will only check the options
  # and report possible warnings but bootloader will not be updated.
  #
  KDUMP_UPDATE_BOOTLOADER="true"

  ## Type:        string
  ## Default:     ""
  ## ServiceRestart:      kdump
  #
  # Kernel Version string for the -kdump kernel. Use an empty string to
  # auto-detect a suitable kernel.
  #
  # See also: kdump(5).
  #
  KDUMP_KERNELVER=""

  ## Type:        integer
  ## Default:     32
  ## ServiceRestart:      kdump
  #
  # Number of CPUs to be used in the kdump environment. You may want to
  # increase the number if computing power is the bottleneck in your setup.
  #
  # If the value is zero, use all available CPUs.
  #
  # Decreasing the number of CPUs lowers the memory required by kdump.
  # Increasing it raises memory usage and may cause failures if crashkernel
  # memory is insufficient.
  #
  # See also: kdump(5).
  #
  KDUMP_CPUS=32

  ## Type:        string
  ## Default:     ""
  ## ServiceRestart:      kdump
  #
  # The kdump commandline is the command line that needs to be passed off to
  # the kdump kernel. If a command line is not specified, the default will be
  # taken from /proc/cmdline and adjusted.
  #
  # WARNING: There are a few options that always should be passed to the kdump
  # kernel. See kdump(5) for details. Don't use that variable, consider using
  # KDUMP_COMMANDLINE_APPEND instead.
  #
  KDUMP_COMMANDLINE=""

  ## Type:        string
  ## Default:     ""
  ## ServiceRestart:     kdump
  #
  # Set this variable if you only want to _append_ values to the default
  # command line string. The string also gets appended if KDUMP_COMMANDLINE
  # is set.
  #
  # For network based dumps, you may have to add a "net_delay" parameter
  # here. Consult the man page for details.
[...]
</screen>
</example>
<para>For more information, refer to <literal>man kdump(5)</literal> and <literal>man kdump(7)</literal>.</para>
<para>Any modifications made to this configuration file require a manual restart of the &kdump; service to take effect. However, if any changes are made specifically to the <literal>KDUMP_CRASHKERNEL</literal>  memory allocation size, a full system reboot is required because these settings alter the system's core boot parameters.</para>
</section>
 <section xml:id="kdump-transact" os="sles">
  <title>Configuring &kdump; for immutable systems </title>
  <para>On an immutable file system, the standard <literal>kdump-commandline.service</literal> is unable to directly modify the boot loader configuration.
    To configure &kdump; in these specialized environments, use the <literal>transactional-update setup-kdump</literal> command instead.
    Executing this command handles the entire configuration process by automatically installing &kdump;, disabling <literal>KDUMP_UPDATE_BOOTLOADER</literal>, and running the necessary calibration and command-line tools to update the boot loader within a new system snapshot.
  A reboot is required to boot into the new snapshot and activate the updated &kdump; configuration.
  </para>
 <para>If the automatically calculated memory allocation estimated by the calibration tool proves to be insufficient for your system requirements, you can manually override the settings.
  To specify custom memory sizes directly on an immutable system, you can append the <literal>crashkernel</literal> parameter to the command by running:</para>
  <screen>&prompt.sudo; transactional-update setup-kdump [--crashkernel=low,high]</screen>
  <para><emphasis role="bold">Other configurations:</emphasis></para>
  <para>Modifying the <filename>/etc/sysconfig/kdump </filename> configuration file on an immutable system requires the execution of the <command>transactional-update</command> command and reboot for the changes to take effect.
    However, if your modifications alter the crashkernel memory allocation size, you must perform an additional step to ensure the changes are safely written to the read-only environment; you need to execute the <command>transactional-update run kdumptool commandline -u</command> command. This applies the new memory settings directly to the boot loader configuration so they can take physical effect during the next system boot.</para>
    <para>The <filename>/etc/sysconfig/kdump</filename> file is the primary configuration file for managing the &kdump; service.
      It includes essential options such as:</para>
      <itemizedlist>
    <listitem><para> <literal>KDUMP_CRASHKERNEL</literal> for defining reserved crash memory size</para></listitem>
    <listitem><para> <literal>KDUMP_VERBOSE</literal> for increasing debugging log levels</para></listitem>
    <listitem><para> <literal>KDUMP_SAVEDIR</literal> for specifying the core dump (vmcore) storage location </para></listitem>
    </itemizedlist>
    <example><title>Contents of a <filename>/etc/sysconfig/kdump</filename> file </title>
    <screen>cat /etc/sysconfig/kdump
      ## Path:        System/Kernel/Kdump
      ## Description: Crash Dump Configuration

      ## Type:        string
      ## Default:     "auto"
      ## ServiceRestart:      kdump
      #
      # KDUMP_CRASHKERNEL is used by "kdumptool commandline" to determine
      # the value of the kernel "crashkernel=" command-line options.
      #
      # Possible values are "auto" or one or more "crashkernel=..." values.
      #
      # KDUMP_CRASHKERNEL="auto" makes "kdumptool commandline" use the default
      # values as proposed by "kdumptool calibrate".
      #
      # If the default values are not adequate you may provide a manual setting,
      # e.g., KDUMP_CRASHKERNEL="crashkernel=72M,low crashkernel=300M,high"
      #
      # See also: kdump(5).
      #
      KDUMP_CRASHKERNEL="auto"


      ## Type:        boolean
      ## Default:     "true"
      ## ServiceRestart:      kdump
      #
      # When KDUMP_UPDATE_BOOTLOADER is set to "true" "kdumptool commandline -c -U"
      # called from the kdump-commandline.service  will check if the expected kernel
      # command-line options are present and update the bootloader using *pbl*(8) if
      # not.
      #
      # Also the kdump options are removed from the command line when kdump is
      # uninstalled.
      #
      # When set to false "kdumptool commandline -c -U" will only check the options
      # and report possible warnings but bootloader will not be updated.
      #
      KDUMP_UPDATE_BOOTLOADER="true"

      ## Type:        string
      ## Default:     ""
      ## ServiceRestart:      kdump
      #
      # Kernel Version string for the -kdump kernel. Use an empty string to
      # auto-detect a suitable kernel.
      #
      # See also: kdump(5).
      #
      KDUMP_KERNELVER=""

      ## Type:        integer
      ## Default:     32
      ## ServiceRestart:      kdump
      #
      # Number of CPUs to be used in the kdump environment. You may want to
      # increase the number if computing power is the bottleneck in your setup.
      #
      # If the value is zero, use all available CPUs.
      #
      # Decreasing the number of CPUs lowers the memory required by kdump.
      # Increasing it raises memory usage and may cause failures if crashkernel
      # memory is insufficient.
      #
      # See also: kdump(5).
      #
      KDUMP_CPUS=32

      ## Type:        string
      ## Default:     ""
      ## ServiceRestart:      kdump
      #
      # The kdump commandline is the command line that needs to be passed off to
      # the kdump kernel. If a command line is not specified, the default will be
      # taken from /proc/cmdline and adjusted.
      #
      # WARNING: There are a few options that always should be passed to the kdump
      # kernel. See kdump(5) for details. Don't use that variable, consider using
      # KDUMP_COMMANDLINE_APPEND instead.
      #
      KDUMP_COMMANDLINE=""

      ## Type:        string
      ## Default:     ""
      ## ServiceRestart:     kdump
      #
      # Set this variable if you only want to _append_ values to the default
      # command line string. The string also gets appended if KDUMP_COMMANDLINE
      # is set.
      #
      # For network based dumps, you may have to add a "net_delay" parameter
      # here. Consult the man page for details.
    [...]
    </screen>
    </example>
  </section>
<section xml:id="testing-kdump">
  <title>Testing &kdump;</title>
  <para>It is advisable to test &kdump; after configuring it by simulating a kernel crash.
Otherwise, you may only find out that it does not work when an actual kernel crash occurs, leaving you with no possibility to debug the crash.
Ensure no critical workloads are running and no unsaved data is present on the system. Additionally, ensure to
sync and unmount file systems:</para>
<para>Force an immediate emergency sync of all cached memory data to your hard drives to prevent data loss:</para>
<screen>echo s &gt; /proc/sysrq-trigger</screen>
<para>Force an immediate emergency remount of all file systems to read-only mode to prevent data corruption:</para>
<screen>echo u &gt; /proc/sysrq-trigger </screen>
<para>Then you can simulate a kernel crash:</para>
<screen>echo c &gt; /proc/sysrq-trigger</screen>
<para>Check if there is a new directory created under your <literal>KDUMP_SAVEDIR</literal>, which is <filename>/var/crash</filename> by default. This contains the dmesg and
vmcore of the crashed kernel.
  </para>
  </section>
</topic>
