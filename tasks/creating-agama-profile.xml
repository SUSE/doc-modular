<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="creating-agama-profile"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Creating an &agama; profile</title>
    <!-- can be changed via merge in the assembly -->
    <!--add author's email address-->
    <meta name="maintainer" content="souvik.sarkar@suse.com" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        This topic shows how to write a minimal &agama; profile with the basic configuration
        needed for an automated installation.
      </para>
    </abstract>
  </info>
  <section xml:id="creating-agama-profile-requirements">
    <title>Requirements</title>
    <itemizedlist>
      <listitem>
        <para>
          The product identifier of your target product, and, if applicable, a registration code
          and email.
        </para>
      </listitem>
    </itemizedlist>
  </section>
  <section xml:id="creating-agama-profile-executing">
    <title>Writing a minimal profile</title>
    <para>
      A minimal &agama; JSON profile must at least include sections for product identification,
      product registration, and credentials for the root user. &agama; uses the defaults for the
      rest of the profile. As a best practice, you should also configure the following:
    </para>
    <simplelist><member>A host name</member><member>
      A non-root user
     </member><member>
      Minimal localization settings
     </member>
    </simplelist>
    <example xml:id="agama-minimal-profile">
      <title>A minimal &agama; profile for automated installation</title>
<screen>
{
  "product": {
    "id": "<replaceable>PRODUCT_ID</replaceable>",
    "registrationCode": "<replaceable>REGISTRATION_CODE</replaceable>",
    "registrationEmail": "<replaceable>EMAIL</replaceable>"
  },
  "hostname": {
    "static": "<replaceable>STATIC_HOSTNAME</replaceable>",
    "transient": "<replaceable>TRANSIENT_HOSTNAME</replaceable>"
  },
  "root": {
    "hashedPassword": true,
    "password": "<replaceable>HASHED_ROOT_PASSWORD</replaceable>", <co xml:id="hashed-password"></co>
    "sshPublicKey": "<replaceable>SSH_PUBLIC_KEY</replaceable>" <co xml:id="ssh-pub-key"></co>
  },
  "user": {
    "hashedPassword": false,
    "fullName": "<replaceable>FULL_NAME</replaceable>",
    "userName": "<replaceable>USERNAME</replaceable>",
    "password": "<replaceable>PLAINTEXT_PASSWORD</replaceable>",
    "sshPublicKey": "<replaceable>SSH_PUBLIC_KEY</replaceable>" <co xml:id="user-ssh-pub-key"></co>
  },
  "localization": {
    "language": "<replaceable>LANGUAGE</replaceable>",
    "keyboard": "<replaceable>KEYBOARD_LAYOUT</replaceable>",
    "timezone": "<replaceable>TIMEZONE</replaceable>"
  }
}
</screen>
      <calloutlist>
        <callout arearefs="hashed-password">
          <para>
            You can generate a hashed password by running the following command:
          </para>
<screen>&prompt.sudo;<command>openssl passwd -6</command></screen>
        </callout>
        <callout arearefs="ssh-pub-key user-ssh-pub-key">
          <para>
            You can generate an SSH public key by running the following command:
          </para>
<screen>&prompt.sudo;<command>ssh-keygen -t rsa -b 4096 -C "<replaceable>YOUR_EMAIL@EXAMPLE.COM</replaceable>"
</command></screen>
          <para>
            Based on your requirements, choose the key type and the key size. However, it is better
            to adopt stronger security.
          </para>
        </callout>
      </calloutlist>
    </example>
    <para>
      To evaluate the correctness of the profile, run the following command:
    </para>
<screen>&prompt.sudo;<command>agama config validate <replaceable>AGAMA_PROFILE</replaceable>.json</command></screen>
  </section>
  <section xml:id="creating-agama-profile-summary">
    <title>Summary</title>
    <para>
      With the sections above in place, you have a working &agama; profile that installs
      &productname; with a host name, a non-root user, and basic localization settings. Add
      further sections, such as <literal>storage</literal>, <literal>network</literal>, or
      <literal>software</literal>, as your deployment requires.
    </para>
  </section>
</topic>
