<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="firewalld-adding-zones-cli"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Managing firewall rules and zones using <command>firewalld-cmd</command></title><!-- can be changed via merge
in the assembly -->
    <!-- add author's e-mail -->
    <meta name="maintainer" content="amrita.sakthivel@suse.com" its:translate="no"/>
    <abstract><!-- can be changed via merge in the assembly -->
      <para>
       You can use the CLI interface to manage &firewalld; zones.
      </para>
    </abstract>
  </info>
  <section xml:id="firewalld-adding-zones-cli-add-zone">
    <title>Adding &firewalld; zones</title>
    <para>
      To add a new &firewalld; zone, proceed as follows:
    </para>
    <procedure>
      <step>
        <para>
          Create the new zone named <emphasis>example</emphasis> in the example:
        </para>
        <screen>&prompt.sudo;<command>firewall-cmd --permanent --new-zone=<replaceable>example</replaceable></command></screen>
      </step>
      <step>
        <para>
          Set the trust level of the zone that defines the default behavior:
        </para>
        <screen>&prompt.sudo;<command>firewall-cmd --permanent --zone=example --set-target=<replaceable>trusted</replaceable></command></screen>
      </step>
      <step>
        <para>
          Reload the &firewalld; service to apply the new configuration:
        </para>
        <screen>&prompt.sudo;firewall-cmd --reload</screen>
      </step>
    </procedure>
  </section>
  <section xml:id="firewalld-adding-zones-cli-add-service">
    <title>Adding allowed services and ports to a zone</title>
    
  </section>
  <section xml:id="firewalld-adding-zones-cli-add-service">
    <title>Deleting zones</title>
    <para>
      A paragraph of text.
    </para>
    
  </section>
</topic>
