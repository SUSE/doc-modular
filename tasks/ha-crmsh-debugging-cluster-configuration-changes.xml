<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/edit/main/xml/ha_config_cli.xml -->

<topic xml:id="ha-crmsh-debugging-cluster-configuration-changes"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Reviewing cluster changes before committing</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        When you make changes to the cluster configuration in &crmsh;'s interactive mode, the changes only apply to the live cluster after you <command>commit</command> them. This procedure shows how to review your changes before you commit them to the live cluster.
      </para>
    </abstract>
  </info>

  <procedure>
    <step>
      <para>
        Log in either as the &rootuser; user or as a user with <command>sudo</command> privileges.
      </para>
    </step>
    <step>
      <para>
        Start &crmsh;'s interactive mode at the <literal>configure</literal> level:
      </para>
<screen>&prompt.user;<command>sudo crm configure</command></screen>
    </step>
    <step>
      <para>
        Make your changes.
      </para>
    </step>
    <step>
      <para>
        Show a diff of the changes to the cluster configuration:
      </para>
<screen>&prompt.crm.conf;<command>show changed</command></screen>
    </step>
    <step>
      <para>
        Generate a report on the changes to the cluster status:
      </para>
<screen>&prompt.crm.conf;<command>ptest</command></screen>
      <para>
        This command displays the following information:
      </para>
      <itemizedlist>
        <listitem>
          <para>
            The current cluster status, showing what the cluster looks like before the changes are committed
          </para>
        </listitem>
        <listitem>
          <para>
            A transition summary, listing the actions the cluster will take after the changes are committed
          </para>
        </listitem>
        <listitem>
          <para>
            A revised cluster status, showing what the cluster will look like after the changes are committed
          </para>
        </listitem>
      </itemizedlist>
    </step>
    <step>
      <para>
        If you're happy with the changes, commit the new configuration:
      </para>
<screen>&prompt.crm.conf;<command>commit</command></screen>
      <para>
        Alternatively, you can discard the changes by exiting interactive mode with <command>quit</command>.
      </para>
    </step>
  </procedure>

  <itemizedlist>
    <title>For more information</title>
    <listitem>
      <para>
        <command>crm configure help ptest</command>
      </para>
    </listitem>
  </itemizedlist>
</topic>
