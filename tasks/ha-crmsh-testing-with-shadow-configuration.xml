<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/edit/main/xml/ha_config_cli.xml -->

<topic xml:id="ha-crmsh-testing-with-shadow-configuration"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Testing with shadow configuration</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        You can use a <emphasis>shadow configuration</emphasis> to test different configuration scenarios. If you create multiple shadow configurations, you can test them one by one to see the effects of your changes.
      </para>
    </abstract>
  </info>

  <para>
    The usual process looks like this:
   </para>
   <procedure>
    <step>
      <para>
        Log in either as the &rootuser; user or as a user with <command>sudo</command> privileges.
      </para>
    </step>
    <step>
      <para>
        Start &crmsh;'s interactive mode:
      </para>
<screen>&prompt.user;<command>sudo crm configure</command></screen>
    </step>
    <step>
      <para>
        Create a new shadow configuration:
      </para>
<screen>&prompt.crm.conf;<command>cib new test1</command>
INFO: configure.cib.new: test1 shadow CIB created
<prompt>crm(test1)configure#</prompt></screen>
      <para>
        The prompt changes from <literal>live</literal> to the name of the shadow CIB.
        If you don't specify a name for the new shadow CIB, a temporary name
        <literal>@tmp@</literal> is used instead.
      </para>
    </step>
    <step performance="optional">
      <para>
        <!-- Changes nothing if you've newly created it; seems more useful for coming back to later -->
        You can copy the current live configuration into your shadow configuration to make it easier to modify any existing resources later:
      </para>
<screen><prompt>crm(test1)configure#</prompt> <command>cib reset test1</command></screen>
    </step>
    <step>
     <para>
      Make your changes as usual. After you have created the shadow
      configuration, all changes go there. To save all your changes, use the
      following command:
     </para>
<screen><prompt>crm(test1)configure#</prompt> <command>commit</command></screen>
    </step>
    <step>
     <para>
      If you need the live cluster configuration again, switch back with the
      following command:
     </para>
<screen><prompt>crm(test1)configure#</prompt> <command>cib use live</command>
&prompt.crm.conf;</screen>
    </step>
   </procedure>
</topic>
