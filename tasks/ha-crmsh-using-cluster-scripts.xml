<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/edit/main/xml/ha_config_cli.xml -->

<topic xml:id="ha-crmsh-using-cluster-scripts"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Using &crmsh;'s cluster scripts</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        Collecting information from all cluster nodes and deploying any
      changes is a key cluster administration task. Instead of performing
      the same procedures manually on different nodes (which is error-prone),
      you can use the &crmsh; cluster scripts.
      Do not confuse them with the <emphasis>&crmsh; shell scripts</emphasis>.
      </para>
    </abstract>
  </info>

  <para>
    Unlike &crmshell; scripts, which can only contain <command>crm </command> subcommands, cluster scripts can also perform additional tasks. For example:
  </para>
  <itemizedlist>
    <listitem>
      <para>
        Installing software that is required for a specific task
      </para>
    </listitem>
    <listitem>
      <para>
        Creating or modifying any configuration files
      </para>
    </listitem>
    <listitem>
      <para>
        Collecting information and reporting potential problems with the cluster
      </para>
    </listitem>
    <listitem>
      <para>
        Deploying the changes to all nodes
      </para>
    </listitem>
  </itemizedlist>

    <para>
      Cluster scripts don't replace other tools for managing clusters, but they provide an integrated way to perform the above tasks across the cluster.
    </para>

  <procedure>
    <step>
      <para>
        Log in either as the &rootuser; user or as a user with <command>sudo</command> privileges.
      </para>
    </step>
    <step>
      <para>
        Show a list of all available cluster scripts:
      </para>
<screen>&prompt.user;<command>sudo crm script list</command></screen>
    </step>
    <step>
      <para>
        View the components of a script:
      </para>
<screen>&prompt.user;<command>sudo crm script show <replaceable>SCRIPT</replaceable></command></screen>
      <para>
        For example:
      </para>
<screen>&prompt.user;<command>sudo crm script show mailto</command>
mailto (Basic)
E-mail

Notifies recipient by e-mail in the event of a resource takeover.

1. Notifies recipients by email in the event of resource takeover

  id (required)  (unique)
      Identifier for the cluster resource
  email (required)
      Email address
  subject
      Subject</screen>
      <para>
        The output of <command>show</command> contains a title, a short description, and a procedure. If necessary, each procedure is divided into steps, performed in the given order. Each step contains a list of required and optional parameters, along with a short description and its default value.
      </para>
    </step>
    <step>
      <para>
        Review the actions the script will perform and verify its parameters:
      </para>
<screen>&prompt.user;<command>sudo crm script verify <replaceable>SCRIPT PARAMETER=VALUE</replaceable></command></screen>
      <para>
        The <command>verify</command> command prints the steps the script will take and replaces any placeholders with your given parameters. If <command>verify</command> finds any problems, it reports them. For example:
      </para>
<screen>&prompt.user;<command>sudo crm script verify mailto id=sysadmin email=&exampleuser_plain;</command>
ERROR: script.verify: email=&exampleuser_plain; is not a valid email address
&prompt.user;<command>sudo crm script verify mailto id=sysadmin email=&exampleuser_plain;@example.org</command>
1. Ensure mail package is installed

        mailx

2. Configure cluster resources

        primitive sysadmin ocf:heartbeat:MailTo
                email="tux@example.org"
                op start timeout="10"
                op stop timeout="10"
                op monitor interval="10" timeout="10"

        clone c-sysadmin sysadmin</screen>
    </step>
    <step>
      <para>
        If everything is fine, replace <command>verify</command> with <command>run</command>:
      </para>
<screen>&prompt.user;<command>sudo crm script run <replaceable>SCRIPT PARAMETER=VALUE</replaceable></command></screen>
      <para>
        For example:
      </para>
<screen>&prompt.user;<command>sudo crm script run mailto id=sysadmin email=&exampleuser_plain;@example.org</command>
INFO: E-Mail
INFO: Nodes: &node1;, &node2;
INFO: Ensure mail package is installed
INFO: Configure cluster resources</screen>
    </step>
    <step>
      <para>
        Check whether the resource was added to the cluster:
      </para>
<screen>&prompt.user;<command>sudo crm status</command>
[...]
  * Clone Set: c-sysadmin [sysadmin]:
    * Started: [ &node1; &node2; ]</screen>
    </step>
  </procedure>

  <itemizedlist>
    <title>For more information</title>
    <listitem>
      <para>
        <command>crm help script</command>
      </para>
    </listitem>
    <listitem>
      <para>
        <link xlink:href="https://crmsh.github.io/scripts/"/>
      </para>
    </listitem>
  </itemizedlist>
</topic>
