<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/edit/main/xml/ha_config_cli.xml -->

<topic xml:id="ha-crmsh-using-interactive-mode"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Using &crmsh;'s interactive mode</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <xi:include href="../snippets/ha-crmsh-interactive-mode.xml"/>
    </abstract>
  </info>

  <procedure>
    <step>
      <para>
        Log in either as the &rootuser; user or as a user with <command>sudo</command> privileges.
      </para>
    </step>
    <step>
      <para>
        Start interactive mode:
      </para>
<screen>&prompt.user;<command>sudo crm</command></screen>
      <para>
        The prompt changes to <prompt>crm(live/HOSTNAME)#</prompt>. However, for better readability
        we omit the host name in our documentation:
      </para>
<screen><prompt>crm(live)#</prompt></screen>
      <tip>
        <title>Starting a subcommand directly</title>
        <para>
          You can also start interactive mode directly at the subcommand level. For example:
        </para>
<screen>&prompt.user;<command>sudo crm configure</command>
&prompt.crm.conf;</screen>
      </tip>
    </step>
    <step>
      <para>
        You can navigate interactive mode with the following options:
      </para>
      <itemizedlist>
        <listitem>
          <para>
            List the available subcommands:
          </para>
<screen>&prompt.crm;<command>ls</command></screen>
        </listitem>
        <listitem>
          <para>
            Enter the name of a subcommand to switch to that level. For example:
          </para>
<screen>&prompt.crm;<command>resource</command>
<prompt>crm(live)resource#</prompt></screen>
        </listitem>
        <listitem>
          <para>
            Switch between subcommands with <command>cd</command>. For example:
          </para>
<screen><prompt>crm(live)resource#</prompt> <command>cd ../node</command>
<prompt>crm(live)node#</prompt></screen>
        </listitem>
        <listitem>
          <para>
            Go back one level with <command>up</command> or <command>cd</command>:
          </para>
<screen><prompt>crm(live)node#</prompt> <command>up</command>
&prompt.crm;</screen>
        </listitem>
      </itemizedlist>
    </step>
    <step>
      <para>
        Make your changes. Example: create resource and add constraints
      </para>
<screen>&prompt.crm;<command>configure</command>
&prompt.crm.conf;<command>primitive vip1 IPaddr2 params ip=&subnetI;.5</command>
&prompt.crm.conf;<command>location loc-vip-alice vip1 inf: alice</command>
&prompt.crm.conf;<command>show changed</command></screen>
    </step>
    <step>
      <para>
        Commit your changes to apply them to the live cluster:
      </para>
<screen>&prompt.crm.conf;<command>commit</command></screen>
      <para>
        You must commit your changes before you exit interactive mode, switch back to the <command>crm</command> level, or switch to another subcommand. The changes will be lost if you don't commit them, but the &crmshell; will warn you before this happens.
      </para>
    </step>
    <step>
      <para>
        Exit interactive mode:
      </para>
<screen>&prompt.crm.conf;<command>quit</command></screen>
    </step>
  </procedure>

  <itemizedlist>
    <title>For more information</title>
    <listitem>
      <para>
        <link xlink:href="https://crmsh.github.io/man-5.0/#topics.Introduction.Interface"/>
      </para>
    </listitem>
  </itemizedlist>
</topic>
