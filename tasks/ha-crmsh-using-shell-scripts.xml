<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/edit/main/xml/ha_config_cli.xml -->

<topic xml:id="ha-crmsh-using-shell-scripts"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Using &crmsh;'s shell scripts</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <xi:include href="../snippets/ha-crmsh-shell-scripts.xml"/>
    </abstract>
  </info>

  <procedure>
    <step>
      <para>
        Log in either as the &rootuser; user or as a user with <command>sudo</command> privileges.
      </para>
    </step>
    <step>
      <para>
        Create a file and add the <command>crm</command> subcommands you want to run together. For
        example, the commands in this file print the status of the cluster and a list of all nodes:
      </para>
<screen># A small example file with some crm subcommands
<command>status</command>
<command>node show</command></screen>
      <para>
        Any line starting with the hash symbol (<literal>#</literal>) is a comment and is ignored.
      </para>
      <para>
        If a line is too long, add a backslash (<literal>\</literal>) at the end and continue in
        the next line. We recommend indenting lines belonging to a certain subcommand to improve
        readability.
      </para>
    </step>
    <step>
      <para>
        Run the script:
      </para>
<screen>&prompt.user;<command>sudo crm -f <replaceable>SCRIPT</replaceable></command></screen>
      <para>
        For example, this &crmshell; script is named <filename>example.cli</filename>:
      </para>
<screen>&prompt.user;<command>sudo crm -f example.cli</command></screen>
    </step>
  </procedure>
</topic>
