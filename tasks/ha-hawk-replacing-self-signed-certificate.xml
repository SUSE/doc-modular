<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_troubleshooting.xml -->

<topic xml:id="ha-hawk-replacing-self-signed-certificate"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Replacing &hawk;'s self-signed certificate</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        By default, &hawk; generates and presents a self-signed certificate. We recommend replacing
        the generated certificate with your own certificate, or a certificate that was signed by an
        official Certificate Authority.
      </para>
      <para>
        &hawk; supports wild card certificates. A wild card certificate is a public key certificate
        that is valid for multiple sub-domains. For example, a wild card certificate for
        <systemitem>*.&exampledomain;</systemitem> is valid for <systemitem>www.&exampledomain;</systemitem>,
        <systemitem>login.&exampledomain;</systemitem>, etc.
      </para>
    </abstract>
  </info>
<!-- Do you have to do this on every node or just one? -->
  <procedure>
    <step>
      <para>
        Replace <filename>/etc/hawk/hawk.key</filename> with the new private key.
      </para>
    </step>
    <step>
      <para>
        Replace <filename>/etc/hawk/hawk.pem</filename> with the new certificate for &hawk; to
        present.
      </para>
    </step>
    <step>
      <para>
        Change ownership of the files to <literal>root:haclient</literal> and make the files
        accessible to the group:
      </para>
<screen>&prompt.user;<command>sudo chown root:haclient /etc/hawk/hawk.key /etc/hawk/hawk.pem</command>
&prompt.user;<command>sudo chmod 640 /etc/hawk/hawk.key /etc/hawk/hawk.pem</command></screen>
    </step>
    <step>
      <para>
        Restart the &hawk; services to reload the new certificate:
      </para>
<screen>&prompt.user;<command>sudo systemctl restart hawk-backend hawk</command></screen>
    </step>
  </procedure>
</topic>
