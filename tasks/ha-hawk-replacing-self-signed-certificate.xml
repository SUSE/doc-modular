<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_troubleshooting.xml -->

<topic xml:id="ha-hawk-replacing-self-signed-certificate"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Replacing &hawk;'s self-signed certificate</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        To avoid the warning about the self-signed certificate on first &hawk; startup, replace the automatically created certificate with your own certificate (or a certificate that was signed by an official Certificate Authority, CA)
      </para>
    </abstract>
  </info>

  <procedure>
    <step>
      <para>
        Replace <filename>/etc/hawk/hawk.key</filename> with the private key.
      </para>
    </step>
    <step>
      <para>
        Replace <filename>/etc/hawk/hawk.pem</filename> with the certificate that &hawk; should present.
      </para>
    </step>
    <step>
      <para>
        Change ownership of the files to <literal>root:haclient</literal> and make the files accessible to the group:
      </para>
<screen>&prompt.root;<command>chown root:haclient /etc/hawk/hawk.key /etc/hawk/hawk.pem</command>
&prompt.root;<command>chmod 640 /etc/hawk/hawk.key /etc/hawk/hawk.pem</command></screen>
    </step>
    <step>
      <para>
        Restart the &hawk; services to reload the new certificate:
      </para>
<screen>&prompt.root;<command>systemctl restart hawk-backend hawk</command></screen>
    </step>
  </procedure>
</topic>
