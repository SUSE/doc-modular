<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_hawk2_batch_i.xml -->

<topic xml:id="ha-hawk-using-batch-mode"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Using &hawk; in batch mode</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        &hawk;'s <emphasis>batch mode</emphasis> allows you to stage configuration changes and apply
        them all as a single transaction, instead of having each change take effect immediately.
        This can be useful when configuring resources that are dependent on one another and require
        constraints.
      </para>
      <para>
        Batch mode also includes a <emphasis>cluster simulator</emphasis>, which shows the expected
        outcome of each change without affecting the live cluster. For example, if you stop a
        resource while in batch mode, the user interface shows the resource as stopped, but in the
        live cluster the resource is still running.
      </para>
      <para>
        You can also <emphasis>inject</emphasis> cluster events, such as node changes and resource
        operations, to explore potential failure scenarios. These changes are only simulations,
        and won't be applied to the live cluster along with the configuration changes.
      </para>

    </abstract>
  </info>

  <important>
    <title>Using configuration wizards in batch mode</title>
    <para>
      Certain wizards include actions beyond cluster configuration, such as installing packages.
      When using those wizards in batch mode, any changes that go beyond cluster configuration
      would be applied to the live system immediately.
    </para>
    <para>
      Therefore, wizards that require &rootuser; permission can't be used while in batch mode.
    </para>
  </important>
  <procedure>
    <step>
      <para>
        In a Web browser, go to <literal>https://<replaceable>HAWKSERVER</replaceable>:7630/</literal>
        and log in.
      </para>
    </step>
    <step>
      <para>
        In the top-level row, click <guimenu>Batch</guimenu>.
      </para>
      <para>
        An additional bar appears below the top-level row. It indicates that batch mode is active
        and contains links to actions that you can perform in batch mode.
      </para>
      <!--figure xml:id="fig-hawk2-batch">
        <title>&hawk; batch mode activated</title>
        <mediaobject>
        <imageobject role="fo">
          <imagedata fileref="hawk-batchmode-active.png" width="100%"/>
        </imageobject>
        <imageobject role="html">
          <imagedata fileref="hawk-batchmode-active.png" width="100%"/>
        </imageobject>
        <textobject role="description">
          <phrase>
            At the top of the screen there is an orange banner that says <quote>Batch mode active</quote>.
            The right side of the banner has three options: <literal>Show</literal>,
            <literal>Discard</literal> and <literal>Apply</literal>.
          </phrase>
        </textobject>
        </mediaobject>
      </figure-->
    </step>
    <step>
      <para>
        While batch mode is active, perform any changes to your cluster, like adding or editing
        resources and constraints, or editing the cluster configuration. The changes will be
        simulated but are not applied to the live cluster yet.
      </para>
    </step>
    <step>
      <para>
        To view details of your changes, select <guimenu>Show</guimenu> from the batch mode bar.
      </para>
      <para>
        The <guimenu>Batch Mode</guimenu> window opens and shows the difference between the live
        cluster and the simulated changes in &crmsh; syntax. Lines starting with a <literal>-</literal>
        character represent the current state whereas lines starting with <literal>+</literal> show
        the proposed state.
      </para>
    </step>
    <step performance="optional">
      <para>
        To test potential failures or to see how the cluster behaves in a specific scenario, you
        can simulate node changes and resource operations:
      </para>
      <itemizedlist>
        <listitem>
          <para>
            Simulate a node failure or status change:
          </para>
          <orderedlist numeration="loweralpha">
            <listitem>
              <para>
                Click <menuchoice><guimenu>Inject</guimenu><guimenu>Node Event</guimenu></menuchoice>.
              </para>
            </listitem>
            <listitem>
              <para>
                Select a <guimenu>Node</guimenu> and the node <guimenu>State</guimenu> to simulate.
              </para>
            </listitem>
            <listitem>
              <para>
                Click <guimenu>OK</guimenu>.
              </para>
            </listitem>
          </orderedlist>
        </listitem>
        <listitem>
          <para>
            Simulate a resource operation:
          </para>
          <orderedlist numeration="loweralpha">
            <listitem>
              <para>
                Click <menuchoice><guimenu>Inject</guimenu><guimenu>Resource Event</guimenu></menuchoice>.
              </para>
            </listitem>
            <listitem>
              <para>
                Select a <guimenu>Resource</guimenu> and the <guimenu>Operation</guimenu> to simulate.
              </para>
            </listitem>
            <listitem>
              <para>
                If necessary, define an <guimenu>Interval</guimenu>.
              </para>
            </listitem>
            <listitem>
              <para>
                Select the <guimenu>Node</guimenu> on which to run the operation.
              </para>
            </listitem>
            <listitem>
              <para>
                Select the operation <guimenu>Result</guimenu> to simulate.
              </para>
            </listitem>
            <listitem>
              <para>
                Click <guimenu>OK</guimenu>.
              </para>
            </listitem>
          </orderedlist>
        </listitem>
      </itemizedlist>
      <para>
        The <guimenu>Batch Mode</guimenu> window shows a new line per injected event. Any event
        listed here is simulated immediately and is reflected on the <guimenu>Status</guimenu> screen.
      </para>
      <para>
        To remove an injected event, click the <guimenu>x</guimenu> icon next to it. &hawk; updates
        the <guimenu>Status</guimenu> screen accordingly.
      </para>
      <!--figure xml:id="fig-hawk2-batch-show">
        <title>&hawk; batch mode&mdash;injected invents and configuration changes</title>
        <mediaobject>
        <imageobject role="fo">
          <imagedata fileref="hawk2-batchmode-show.png" width="100%"/>
        </imageobject>
        <imageobject role="html">
          <imagedata fileref="hawk2-batchmode-show.png" width="100%"/>
        </imageobject>
        <textobject role="description">
          <phrase>
            This example shows two buttons: <literal>Simulator</literal> and <literal>Inject</literal>.
            Below the buttons are two events: <literal>node &node1; unclean</literal> and
            <literal>op monitor:0 nfs-prim 0 &node1;</literal>.
          </phrase>
        </textobject>
        </mediaobject>
      </figure-->
    </step>
    <step performance="optional">
    <para>
      To view more details about the changes, click <guimenu>Simulator</guimenu> and choose one of
      the following:
    </para>
    <itemizedlist>
      <listitem>
        <para>
          <guimenu>Summary</guimenu>: Shows a detailed summary.
        </para>
      </listitem>
      <listitem>
        <para>
          <guimenu>CIB (in)</guimenu>: Shows the initial CIB state.
        </para>
      </listitem>
      <listitem>
        <para>
          <guimenu>CIB (out)</guimenu>: Shows what the CIB would look like after the transition.
        </para>
      </listitem>
      <listitem>
        <para>
          <guimenu>Transition Graph</guimenu>: Shows a graphical representation of the transition.
        </para>
      </listitem>
      <listitem>
        <para>
          <guimenu>Transition</guimenu>: Shows an XML representation of the transition.
        </para>
      </listitem>
    </itemizedlist>
    </step>
    <step>
      <para>
        After reviewing your changes, click <guimenu>Close</guimenu> to exit the
        <guimenu>Batch Mode</guimenu> window.
      </para>
    </step>
    <step>
      <para>
        Choose whether to keep your changes or discard them:
      </para>
      <itemizedlist>
        <listitem>
          <para>
            To keep your changes, click <guimenu>Apply</guimenu> in the top-level row.
            The <guimenu>Apply Configuration</guimenu> window opens and shows a list of the changes.
            Only configuration changes are applied; injected events are not. Click
            <guimenu>Apply</guimenu> to confirm.
          </para>
        </listitem>
        <listitem>
          <para>
            To discard your changes, click <guimenu>Discard</guimenu> in the top-level row, then
            click <guimenu>OK</guimenu> to confirm.
          </para>
        </listitem>
      </itemizedlist>
      <para>
        Both options deactivate batch mode and take you back to normal mode.
      </para>
    </step>
  </procedure>
</topic>
