<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_config_hawk2.xml -->

<topic xml:id="ha-hawk-using-configuration-wizards"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Using the &hawk; configuration wizards</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        The &hawk; wizard is a convenient way of setting up simple resources like a
   virtual IP address or an SBD &stonith; resource, for example. It is
   also useful for complex configurations that include multiple resources,
   like the resource configuration for a DRBD block device or an Apache Web
   server. The wizard guides you through the configuration steps and provides
   information about the parameters you need to enter.
      </para>
    </abstract>
  </info>

  <procedure>
   <step>
    <para>
     Log in to &hawk;:
    </para>
<screen>https://<replaceable>HAWKSERVER</replaceable>:7630/</screen>
   </step>
   <step>
    <para>
     From the left navigation bar, select <menuchoice><guimenu>Configuration</guimenu>
     <guimenu>Wizards</guimenu></menuchoice>.
    </para>
   </step>
   <step>
    <para>
     Expand the individual categories by clicking the arrow down icon next to
     them and select the desired wizard.
    </para>
   </step>
   <step>
    <para>
     Follow the instructions on the screen. After the last configuration step,
     <guimenu>Verify</guimenu> the values you have entered.
    </para>
    <para>
     &hawk; shows which actions it is going to perform and what the
     configuration looks like. Depending on the configuration, you might be
     prompted for the &rootuser; password before you can
     <guimenu>Apply</guimenu> the configuration.
    </para>
   </step>
  </procedure>
  <!--figure xml:id="fig-ha-hawk-using-configuration-wizards">
    <title>&hawk; wizard for Apache web server</title>
    <mediaobject>
      <imageobject role="fo">
        <imagedata fileref="hawk-wizard-apache.png" width="100%"/>
      </imageobject>
      <imageobject role="html">
        <imagedata fileref="hawk-wizard-apache.png" width="100%"/>
      </imageobject>
      <textobject role="description">
        <phrase>
          This example shows the first page of the Apache Web server configuration wizard.
          It shows a drop-down list to specify whether to install Apache, and a field to
          enter an ID for the Apache cluster resource. There is also an option to add
          Advanced configuration.
        </phrase>
      </textobject>
    </mediaobject>
  </figure-->
</topic>
