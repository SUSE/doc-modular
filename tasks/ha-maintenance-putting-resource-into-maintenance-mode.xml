<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_maintenance.xml -->

<topic xml:id="ha-maintenance-putting-resource-into-maintenance-mode"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Putting a resource into maintenance mode</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        Resource maintenance mode puts a single resource into maintenance mode. The cluster stops
        managing the resource and won't start, stop or monitor it. You can also put groups and
        clones into resource maintenance mode.
      </para>
    </abstract>
  </info>

  <xi:include href="../snippets/ha-maintenance-mode-warning.xml"/>

  <variablelist role="tabs">
    <varlistentry>
      <term>&crmshell;</term>
      <listitem>
        <para>
          You can perform this procedure on any node in the cluster:
        </para>
        <procedure>
          <step>
            <para>
              Log in either as the &rootuser; user or as a user with <command>sudo</command>
              privileges.
            </para>
          </step>
          <step>
            <para>
              Put the resource into maintenance mode:
            </para>
<screen>&prompt.user;<command>sudo crm resource maintenance <replaceable>RESOURCE</replaceable> on</command></screen>
          </step>
          <step>
            <para>
              Check the status of the cluster:
            </para>
<screen>&prompt.user;<command>sudo crm status</command></screen>
            <para>
              The resource should show the status <literal>maintenance</literal>.
            </para>
          </step>
        </procedure>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>&hawk;</term>
      <listitem>
        <para>
          You can use &hawk; on any machine with a Web browser and network access to the nodes.
        </para>
        <procedure>
          <step>
            <para>
              In a Web browser, go to
              <literal>https://<replaceable>HAWKSERVER</replaceable>:7630/</literal> and log in.
            </para>
          </step>
          <step>
            <para>
              In the left navigation bar, select <menuchoice><guimenu>Monitoring</guimenu>
              <guimenu>Status</guimenu></menuchoice>.
            </para>
          </step>
          <step>
            <para>
              Find the resource you want to put into maintenance mode. In its
              <guimenu>Operations</guimenu> column, click the down arrow icon and select
              <guimenu>Maintenance</guimenu>.
            </para>
          </step>
          <step>
            <para>
              Click <guimenu>OK</guimenu> to confirm.
            </para>
            <para>
              The resource should now have a wrench icon in the <guimenu>Status</guimenu> column.
            </para>
          </step>
        </procedure>
      </listitem>
    </varlistentry>
  </variablelist>
</topic>
