<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<topic xml:id="ha-qdevice-changing-configuration"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Changing &qdevice; configuration</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        Use this procedure to change the configuration of an existing &qdevice; setup (for
        example, if you need to change the tie-breaker method from <literal>lowest</literal>
        to <literal>highest</literal>).
      </para>
    </abstract>
  </info>

  <procedure>
    <step>
      <para>
        Log in either as the &rootuser; user or as a user with <command>sudo</command> privileges.
      </para>
    </step>
    <step>
      <para>
        Put the cluster into maintenance mode:
      </para>
<screen>&prompt.user;<command>sudo crm maintenance on</command></screen>
      <para>
        In this state, the cluster stops monitoring all resources. This allows the services
        managed by the resources to keep running even when you stop the cluster services.
      </para>
    </step>
    <step>
      <para>
        Stop the cluster services on all nodes:
      </para>
<screen>&prompt.user;<command>sudo crm cluster stop --all</command></screen>
    </step>
    <step>
      <para>
        Open <filename>/etc/corosync/corosync.conf</filename> and change the required setting in
        the <literal>quorum</literal> section.
      </para>
    </step>
    <step>
      <para>
        Copy the new configuration to all nodes:
      </para>
<screen>&prompt.user;<command>sudo csync2 -xv</command></screen>
    </step>
    <step>
      <para>
        Start the cluster service on all nodes:
      </para>
<screen>&prompt.user;<command>sudo crm cluster start --all</command></screen>
    </step>
    <step>
      <para>
        Check the status of the cluster:
      </para>
<screen>&prompt.user;<command>sudo crm status</command></screen>
      <para>
        The nodes will have the status <literal>UNCLEAN (offline)</literal>, but will soon
        change to <literal>Online</literal>.
      </para>
    </step>
    <step>
      <para>
        When the nodes are back online, put the cluster back into normal operation:
      </para>
<screen>&prompt.user;<command>sudo crm maintenance off</command></screen>
    </step>
    <step>
      <para>
        Verify that the change was successful:
      </para>
<screen>&prompt.user;<command>sudo crm corosync status qnetd</command></screen>
    </step>
  </procedure>
</topic>
