<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_resource_constraints.xml -->

<topic xml:id="ha-resources-colocating-without-adding-dependency"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Colocating resources without adding dependency</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        A colocation constraint creates dependency between the resources. If one resource fails,
        the cluster also restarts the other resource. However, sometimes you might need to place
        resources together <emphasis>without</emphasis> any dependency. You can do this with the
        &crmshell;'s <command>weak-bond</command> command. This creates a <literal>Dummy</literal>
        resource, then creates a colocation constraint between the <literal>Dummy</literal>
        resource and the resources you specify.
    </para>
    </abstract>
  </info>

  <xi:include href="../snippets/ha-crmsh-procedure-intro.xml"/>
  <procedure>
    <step>
      <para>
        Log in either as the &rootuser; user or as a user with <command>sudo</command> privileges.
      </para>
    </step>
    <step>
      <para>
        Run the following command, specifying two or more primitive resources:
      </para>
<screen>&prompt.user;<command>sudo crm configure assist weak-bond <replaceable>RESOURCE1 RESOURCE2</replaceable></command>
Create weak bond / independent colocation
The following elements will be created:
   * Colocation constraint, ID: place-constraint-1
   * Dummy resource, ID: place-dummy-1
Create resources (y/n)?</screen>
    </step>
    <step>
      <para>
        Enter <literal>y</literal> to create the <literal>Dummy</literal> resource and
        colocation constraint.
      </para>
    </step>
    <step>
      <para>
        View the new constraint:
      </para>
<screen>&prompt.user;<command>sudo crm configure show place-constraint-1</command></screen>
    </step>
    <step>
      <para>
        Check which node the resources are running on:
      </para>
<screen>&prompt.user;<command>sudo crm status</command></screen>
    </step>
  </procedure>

  <itemizedlist>
    <title>For more information</title>
    <listitem>
      <para>
        <command>crm assist weak-bond --help</command>
<!-- tahliar 2026-01-27: This is the help command that works, even though the actual command includes `configure` -->
      </para>
    </listitem>
  </itemizedlist>
</topic>
