<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_configuring_resources.xml -->

<topic xml:id="ha-resources-configuring-primitives"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Configuring primitive resources</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <xi:include href="../snippets/ha-resources-primitives.xml"/>
    </abstract>
  </info>

  <para>
    You can create primitive resources with the &crmshell; (&crmsh;) or with the &hawk; Web interface.
  </para>
  <itemizedlist>
    <title>Requirements</title>
    <listitem>
      <para>
        The application or service is already set up. For example, you must install and configure
        an Apache server before you can create a cluster resource to manage it.
      </para>
    </listitem>
    <listitem>
      <para>
        If an application or service has specific environment requirements, make sure the required
        settings are identical on all cluster nodes. Alternatively, add resource constraints to
        make sure the resource can only run on nodes that meet the requirements.
      </para>
    </listitem>
  </itemizedlist>

  <variablelist role="tabs">
    <varlistentry>
      <term>&crmshell;</term>
      <listitem>
        <xi:include href="../snippets/ha-crmsh-procedure-intro.xml"/>
        <procedure>
          <step>
            <para>
              Log in either as the &rootuser; user or as a user with <command>sudo</command> privileges.
            </para>
          </step>
          <step>
            <para>
              Show the required and optional parameters for a resource agent:
            </para>
<screen>&prompt.user;<command>sudo crm ra info <replaceable>RESOURCE_AGENT</replaceable></command></screen>
          </step>
          <step>
            <para>
            Configure a primitive resource. The basic command requires at least a name,
            a resource agent, and any parameters required by the resource agent:
            </para>
<screen>&prompt.user;<command>sudo crm configure primitive <replaceable>RESOURCE_NAME RESOURCE_AGENT</replaceable> \
  params <replaceable>ATTRIBUTE=VALUE</replaceable></command></screen>
            <para>
              This example shows a basic cluster resource for managing an IP address:
            </para>
<screen>&prompt.user;<command>sudo crm configure primitive myIP IPaddr2 params ip=&subnetI;.10
</command></screen>
            <variablelist>
              <title>Additional options</title>
              <varlistentry>
                <term>Operations</term>
                <listitem>
                  <para>
                    To show the default operations for a resource agent, run
                    <command>crm ra info <replaceable>RESOURCE_AGENT</replaceable></command>
                    and scroll to the bottom of the output. You can keep the default operations
                    or change them as required. For example, to clone a resource, the primitive
                    must have two monitor operations (one for each role):
                  </para>
<screen>&prompt.user;<command>sudo crm configure primitive db1 mysql \
  op monitor timeout=30 interval=10 role=Promoted \
  op monitor timeout=30 interval=20 role=Unpromoted</command></screen>
                  <important role="compact">
                    <para>
                      Before configuring a primitive in a production environment, we recommend
                      testing it to make sure the <literal>start</literal> and <literal>stop</literal>
                      timeouts are long enough under load. Timeouts that are too short can lead to
                      resource failure and potentially node fencing.
                    </para>
                  </important>
                </listitem>
              </varlistentry>
              <varlistentry>
                <term>Meta attributes</term>
                <listitem>
                  <para>
                    Meta attributes change how the cluster treats a resource.
                    To show all the meta attributes available for primitives, run
                    <command>crm_resource --list-options primitive</command>. For example,
                    <literal>allow-migrate</literal> enables live migration for a VM resource:
                  </para>
<screen>&prompt.user;<command>sudo crm configure primitive VM1 VirtualDomain \
  params config="/mnt/shared/VM1.xml" \
  meta allow-migrate=true</command></screen>
                </listitem>
              </varlistentry>
              <varlistentry>
                <term>Utilization attributes</term>
                <listitem>
                  <para>
                    If the cluster nodes have utilization attributes configured, you can add these
                    attributes to cluster resources to help with load balancing. For example, if
                    the nodes have the attribute <literal>memory</literal>, you can add the same
                    attribute to a resource so that it can only run on nodes with enough capacity:
                  </para>
<screen>&prompt.user;<command>sudo crm configure primitive web1 apache \
  utilization memory=1024</command></screen>
                </listitem>
              </varlistentry>
            </variablelist>
          </step>
          <step>
            <para>
              Check the status of the resource:
            </para>
<screen>&prompt.user;<command>sudo crm status</command></screen>
          </step>
        </procedure>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>&hawk;</term>
      <listitem>
        <xi:include href="../snippets/ha-hawk-procedure-intro.xml"/>
        <procedure>
          <step>
            <para>
              In a Web browser, go to
              <literal>https://<replaceable>HAWKSERVER</replaceable>:7630/</literal>
              and log in.
            </para>
          </step>
          <step>
            <para>
              From the left navigation bar, select <menuchoice><guimenu>Configuration</guimenu>
              <guimenu>Add Resource</guimenu></menuchoice>.
            </para>
          </step>
          <step>
            <para>
              Click <guimenu>Primitive</guimenu>.
            </para>
          </step>
          <step>
            <para>
              Enter a unique <guimenu>Resource ID</guimenu>.
            </para>
          </step>
          <step>
            <para>
              Select the resource agent <guimenu>Class</guimenu>: <literal>ocf</literal>,
              <literal>stonith</literal> or <literal>systemd</literal>.
            </para>
          </step>
          <step>
            <para>
              If you selected the <literal>ocf</literal> class, select the resource agent
              <guimenu>Provider</guimenu>.
            </para>
          </step>
          <step>
            <para>
              From the <guimenu>Type</guimenu> list, select a resource agent.
            </para>
            <para>
              &hawk; displays a description of the selected resource agent.
            </para>
            <tip role="compact">
              <para>
                For each of the next steps, after you select an item you can hover the cursor
                over it to see a description.
              </para>
            </tip>
          </step>
          <step>
            <para>
              &hawk; automatically adds the required <guimenu>Parameters</guimenu> for the selected
              resource agent. Fill in the appropriate values for these fields to create a valid
              resource configuration.
            </para>
            <para>
              If needed, select more parameters from the empty drop-down list.
            </para>
          </step>
          <step performance="optional">
            <para>
              &hawk; automatically adds the recommended <guimenu>Operations</guimenu> for
              the selected resource agent (usually <literal>start</literal>, <literal>stop</literal>
              and <literal>monitor</literal>). Either keep the suggested operations, or click the
              <guimenu>Edit</guimenu> icon to edit them as required.
            </para>
            <para>
              If needed, select more operations from the empty drop-down list.
            </para>
            <important role="compact">
              <para>
                Before configuring a primitive in a production environment, we recommend testing
                it to make sure the <literal>start</literal> and <literal>stop</literal> timeouts
                are long enough under load. Timeouts that are too short can lead to resource
                failure and potentially node fencing.
              </para>
            </important>
          </step>
          <step performance="optional">
            <para>
              &hawk; automatically adds the recommended <guimenu>Meta Attributes</guimenu>
              for a resource (for example, <literal>target-role</literal> is set to
              <literal>Stopped</literal> by default so the resource doesn't start immediately).
              Either keep the suggested meta attributes, or edit them as required.
            </para>
            <para>
              If needed, select more meta attributes from the empty drop-down list.
            </para>
            <important role="compact">
              <para>
                Currently, &hawk; can't add <guimenu>Utilization</guimenu> attributes to
                primitive resources.
<!-- Despite changing the placement strategy and adding utilization attributes to the nodes,
nothing appears under the <guimenu>Utilization</guimenu> category for primitives. Might be a bug? -->
              </para>
            </important>
          </step>
          <step>
            <para>
              Click <guimenu>Create</guimenu> to finish the configuration.
            </para>
            <para>
              A message at the top of the screen shows if the action was successful.
            </para>
          </step>
        </procedure>
      </listitem>
    </varlistentry>
  </variablelist>
  <xi:include href="../snippets/ha-resource-management-warning.xml"/>

  <itemizedlist>
    <title>For more information</title>
    <listitem>
      <para>
        <command>crm configure help primitive</command>
      </para>
    </listitem>
    <listitem>
      <para>
        <link xlink:href="https://clusterlabs.org/projects/pacemaker/doc/3.0/Pacemaker_Explained/html/resources.html#resource-properties"/>
      </para>
    </listitem>
    <listitem>
      <para>
        <link xlink:href="https://clusterlabs.org/projects/pacemaker/doc/3.0/Pacemaker_Explained/html/resources.html#resource-options"/>
      </para>
    </listitem>
    <listitem>
      <para>
        <link xlink:href="https://clusterlabs.org/projects/pacemaker/doc/3.0/Pacemaker_Explained/html/operations.html"/>
      </para>
    </listitem>
    <listitem>
      <para>
        <link xlink:href="https://clusterlabs.org/projects/pacemaker/doc/3.0/Pacemaker_Explained/html/utilization.html"/>
      </para>
    </listitem>
  </itemizedlist>
</topic>
