<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_managing_resources.xml -->

<topic xml:id="ha-resources-deleting"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Deleting cluster resources</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        You can delete a resource from the cluster configuration with the &crmshell; (&crmsh;)
        or with the &hawk; Web interface.
      </para>
    </abstract>
  </info>

  <itemizedlist>
    <title>Requirements</title>
    <listitem>
      <para>
        If the resource has the status <literal>FAILED</literal>, you must clean it up before you can delete it.
      </para>
    </listitem>
  </itemizedlist>

  <variablelist role="tabs">
    <varlistentry>
      <term>&crmshell;</term>
      <listitem>
        <xi:include href="../snippets/ha-crmsh-procedure-intro.xml"/>
        <procedure>
          <step>
            <para>
              Log in either as the &rootuser; user or as a user with <command>sudo</command>
              privileges.
            </para>
          </step>
          <step>
            <para>
              Check the current status of the resource:
            </para>
<screen>&prompt.user;<command>sudo crm resource status</command></screen>
          </step>
          <step>
            <para>
              If the resource is <literal>Started</literal>, you can stop it with the following
              command:
            </para>
<screen>&prompt.user;<command>sudo crm resource stop <replaceable>RESOURCE</replaceable></command></screen>
          </step>
          <step>
            <para>
              Delete the resource. If necessary, you can delete multiple resources at once:
            </para>
<screen>&prompt.user;<command>sudo crm configure delete <replaceable>RESOURCE1 RESOURCE2</replaceable></command></screen>
          </step>
        </procedure>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>&hawk;</term>
      <listitem>
        <xi:include href="../snippets/ha-hawk-procedure-intro.xml"/>
        <procedure>
          <step>
            <para>
              In a Web browser, go to
              <literal>https://<replaceable>HAWKSERVER</replaceable>:7630/</literal>
              and log in.
            </para>
          </step>
          <step>
            <para>
              From the left navigation bar, select <menuchoice><guimenu>Configuration</guimenu>
              <guimenu>Edit Configuration</guimenu></menuchoice>.
            </para>
          </step>
          <step>
            <para>
              Find the resource you want to delete. In its <guimenu>Operations</guimenu> column,
              click the <guimenu>Delete</guimenu> icon.
            </para>
            <para>
              When using &hawk;, you don't need to stop the resource before deleting it.
            </para>
          </step>
          <step>
            <para>
              Click <guimenu>OK</guimenu> to continue.
            </para>
            <para>
              A message at the top of the screen shows if the action was successful.
            </para>
          </step>
        </procedure>
      </listitem>
    </varlistentry>
  </variablelist>

  <itemizedlist>
    <title>For more information</title>
    <listitem>
      <para>
        <command>crm configure help delete</command>
      </para>
    </listitem>
  </itemizedlist>
</topic>
