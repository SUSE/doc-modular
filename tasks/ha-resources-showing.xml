<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_managing_resources.xml -->

<topic xml:id="ha-resources-showing"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Showing cluster resources</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        You can view the cluster configuration with the &crmshell; (&crmsh;) or with &hawk;.
      </para>
    </abstract>
  </info>

  <variablelist role="tabs">
    <varlistentry>
      <term>&crmshell;</term>
      <listitem>
        <para>
          You can perform this procedure on any node in the cluster.
        </para>
        <procedure>
          <step>
            <para>
              Log in either as the &rootuser; user or as a user with <command>sudo</command> privileges.
            </para>
          </step>
          <step>
            <para>
              To see which node a resource is running on, show the cluster status:
            </para>
<screen>&prompt.user;<command>sudo crm status</command></screen>
          </step>
          <step>
            <para>
              To see all cluster objects, show the cluster configuration. The basic command shows
              the full CIB:
            </para>
<screen>&prompt.user;<command>sudo crm configure show</command></screen>
            <para>
              You can limit the output by using modifiers such as an object's name, type or tag.
              For more information about the available modifiers, run the following command:
            </para>
<screen>&prompt.user;<command>sudo crm configure show --help</command></screen>
          </step>
        </procedure>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>&hawk;</term>
      <listitem>
        <para>
          You can access the &hawk; Web interface from any machine that has network access to the nodes.
        </para>
        <procedure>
          <step>
            <para>
              In a Web browser, go to
              <literal>https://<replaceable>HAWKSERVER</replaceable>:7630/</literal>
              and log in.
            </para>
            <para>
              The <guimenu>Status</guimenu> page shows the cluster resources and their statuses.
              If the resource is a group or a clone, you can click the plus icon to show the
              child resources.
            </para>
          </step>
          <step>
            <para>
              From the left navigation bar, select <menuchoice><guimenu>Configuration</guimenu>
              <guimenu>Edit Configuration</guimenu></menuchoice>.
            </para>
            <para>
              On this page, you can click different tabs to view more details about cluster objects
              such as resources, constraints or nodes. You can also limit the results by using the
              <guimenu>Search</guimenu> bar.
            </para>
          </step>
          <step>
            <para>
              To view the full CIB, click <guimenu>Show</guimenu>.
            </para>
            <para>
              Alternatively, you can click <guimenu>XML</guimenu> to view the XML representation
              of the CIB.
            </para>
          </step>
        </procedure>
      </listitem>
    </varlistentry>
  </variablelist>
</topic>
