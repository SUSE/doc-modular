<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_managing_resources.xml -->

<topic xml:id="ha-resources-stopping"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Stopping cluster resources</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        You can stop cluster resources with the &crmshell; (&crmsh;) or with the
        &hawk; Web interface.
      </para>
    </abstract>
  </info>

<!-- Not sure this is really needed here? -->
  <xi:include href="../snippets/ha-resource-management-warning.xml"/>

  <variablelist role="tabs">
    <varlistentry>
      <term>&crmshell;</term>
      <listitem>
        <xi:include href="../snippets/ha-crmsh-procedure-intro.xml"/>
        <procedure>
          <step>
            <para>
              Log in either as the &rootuser; user or as a user with <command>sudo</command>
              privileges.
            </para>
          </step>
          <step>
            <para>
              Check the current status of the resource:
            </para>
<screen>&prompt.user;<command>sudo crm resource status</command></screen>
          </step>
          <step>
            <para>
              Stop the resource. If necessary, you can stop multiple resources at once:
            </para>
<screen>&prompt.user;<command>sudo crm resource stop <replaceable>RESOURCE1</replaceable> <replaceable>RESOURCE2</replaceable></command></screen>
          </step>
          <step>
            <para>
              Check the status again to make sure the resource stopped successfully:
            </para>
<screen>&prompt.user;<command>sudo crm resource status</command></screen>
          </step>
        </procedure>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>&hawk;</term>
      <listitem>
        <xi:include href="../snippets/ha-hawk-procedure-intro.xml"/>
        <procedure>
          <step>
            <para>
              In a Web browser, go to
              <literal>https://<replaceable>HAWKSERVER</replaceable>:7630/</literal>
              and log in.
            </para>
          </step>
          <step>
            <para>
              From the left navigation bar, select <menuchoice><guimenu>Monitoring</guimenu>
              <guimenu>Status</guimenu></menuchoice>.
            </para>
            <para>
              The <guimenu>Resources</guimenu> tab shows the <guimenu>Status</guimenu> of the
              cluster resources. A running resource has a green <guimenu>Status</guimenu> icon.
            </para>
          </step>
          <step>
            <para>
              Find the resource you want to stop. In its <guimenu>Operations</guimenu> column,
              click the <guimenu>Stop</guimenu> icon.
            </para>
          </step>
          <step>
            <para>
              Click <guimenu>OK</guimenu> to continue.
            </para>
            <para>
              The resource's <guimenu>Status</guimenu> icon changes to red.
            </para>
          </step>
        </procedure>
      </listitem>
    </varlistentry>
  </variablelist>
</topic>
