<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_managing_resources.xml -->

<topic xml:id="ha-resources-stopping"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Stopping cluster resources</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
<para>
To stop one or more existing cluster resources, you need the respective identifiers.
  </para>
    </abstract>
  </info>

<procedure>
   <title>Stopping cluster resources with &crmsh;</title>
   <step>
    <para>
     Log in as &rootuser; and start the <command>crm</command>
     interactive shell:
    </para>
<screen>&prompt.root;<command>crm</command></screen>
   </step>
   <step>
    <para>
     Switch to the resource level:
    </para>
<screen>&prompt.root;<command>crm resource</command></screen>
   </step>
   <step>
    <para>
     Stop the resource with <command>stop</command> and press the
     <keycap function="tab"/> key to show all known resources:
    </para>
<screen>&prompt.root;<command>crm resource stop <replaceable>ID</replaceable></command></screen>
    <para>
     You can stop multiple resources at once:
    </para>
    <screen>&prompt.root;<command>crm resource stop <replaceable>ID1</replaceable> <replaceable>ID2</replaceable></command> ...</screen>
   </step>
  </procedure>
</topic>
