<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>

<topic xml:id="ha-sbd-removing-device"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Removing an &sbd; device</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        Use this procedure to remove an &sbd; device from a cluster with multiple &sbd; devices
        configured. You cannot use this method if there is only one &sbd; device configured.
      </para>
    </abstract>
  </info>

  <xi:include href="../snippets/ha-cluster-restart-warning.xml"/>

  <itemizedlist>
    <title>Requirements</title>
    <listitem>
      <para>
        Disk-based &sbd; is configured with more than one device.
      </para>
    </listitem>
  </itemizedlist>
  <para>
    Perform this procedure on only one node in the cluster:
  </para>
  <procedure>
    <step>
      <para>
        Log in either as the &rootuser; user or as a user with <command>sudo</command> privileges.
      </para>
    </step>
    <step>
      <para>
        Check which devices are already configured for use with &sbd;:
      </para>
<screen>&prompt.user;<command>sudo crm sbd configure show sysconfig</command></screen>
      <para>
        The output shows multiple device IDs in the <literal>SBD_DEVICE</literal> line.
      </para>
    </step>
    <step>
      <para>
        Remove a device from the &sbd; configuration:
      </para>
<screen>&prompt.user;<command>sudo crm sbd device remove /dev/disk/by-id/<replaceable>DEVICE_ID</replaceable></command></screen>
      <para>
        The script removes the device, updates the &sbd; configuration file, and checks whether it
        is safe to restart the cluster services automatically. If any non-&stonith; resources are
        running, the script warns you to restart the cluster services manually.
      </para>
    </step>
    <xi:include href="../snippets/ha-cluster-restart-steps.xml"/>
    <step>
      <para>
        Check the &sbd; configuration again:
      </para>
<screen>&prompt.user;<command>sudo crm sbd configure show sysconfig</command></screen>
      <para>
        The output should now show fewer devices.
      </para>
    </step>
    <step>
      <para>
        Check the status of &sbd; to make sure the device was removed from all of the nodes:
      </para>
<screen>&prompt.user;<command>sudo crm sbd status</command></screen>
    </step>
  </procedure>
</topic>
