<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="initiating-agama-automated-installation"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Initiating automated installation using &agama;</title>
    <!-- can be changed via merge
      in the assembly -->
    <meta name="maintainer" content="souvik.sarkar@suse.com" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        Start an unattended &agama; installation in one of three ways: add the
        <literal>inst.auto</literal> boot parameter pointing to your profile, place the profile at
        one of &agama;'s predefined locations so no boot parameter is needed, or load the profile
        from the &agama; command-line interface with <command>agama config load</command> and then
        run <command>agama install</command>. All methods read the same profile and install the
        system without further interaction.
      </para>
    </abstract>
  </info>
  <section xml:id="initiating-agama-automated-installation-requirements">
    <title>Requirements for starting an unattended installation</title>
    <itemizedlist>
      <listitem>
        <para>
          The machine booted from an &agama; installation medium, at the boot menu where kernel
          parameters can still be edited.
        </para>
      </listitem>
      <listitem>
        <para>
          An &agama; profile reachable from the target machine, either over the network or from
          local media, such as a USB stick.
        </para>
      </listitem>
    </itemizedlist>
  </section>
  <section xml:id="initiating-agama-unattended-installation">
    <title>Initiating the unattended installation</title>
    <procedure xml:id="proc-initiating-agama-unattended-installation">
      <title>Initiating the unattended installation</title>
      <step>
        <para>
          Choose one of the following two methods. Both start the same unattended installation, so
          perform only one of them.
        </para>
        <stepalternatives>
          <step>
            <para>
              Start the installation using kernel boot parameters.
            </para>
            <para>
              Add the <literal>inst.auto</literal> parameter to the kernel command line to specify
              the location of the &agama; profile:
            </para>
<screen>inst.auto=<replaceable>http://example.net/profile.json</replaceable></screen>
            <para>
              &agama; reads the profile and starts the installation without further interaction.
              This method is suitable for PXE boot setups, custom ISO builds, or cloud-init
              workflows. For setting up a PXE boot server, see
              <xref linkend="more-info-automated-installation-agama"/>.
            </para>
          </step>
          <step>
            <para>
              Start the installation without a boot parameter, by placing the profile at one of
              &agama;'s predefined locations.
            </para>
            <para>
              If no <literal>inst.auto</literal> parameter is given, &agama; automatically
              searches for a profile named <filename>autoinst.jsonnet</filename>,
              <filename>autoinst.json</filename>, or <filename>autoinst.xml</filename> (in that
              order), at the following locations, and uses the first one it finds:
            </para>
            <itemizedlist>
              <listitem>
                <para>
                  A device labeled <literal>OEMDRV</literal>, such as an attached USB
                  stick&mdash;useful for pre-installed systems.
                </para>
              </listitem>
              <listitem>
                <para>
                  The root of the installation medium itself, such as the USB stick or DVD you
                  booted from.
                </para>
              </listitem>
              <listitem>
                <para>
                  The root of the installer's own live file system&mdash;useful for a custom ISO
                  image built with the profile already embedded.
                </para>
              </listitem>
            </itemizedlist>
            <para>
              &agama; does not report an error if none of these locations contain a profile; the
              installation then proceeds interactively as usual.
            </para>
          </step>
          <step>
            <para>
              Start the installation using the &agama; CLI.
            </para>
            <substeps>
              <step>
                <para>
                  Switch to the root shell already running on virtual console 8; no password is
                  required there. If using a software such as the Virtual Machine Manager, use the
                  <guimenu>Send Keys</guimenu> menu to send the signal
                  <keycombo><keycap function="control"/><keycap function="alt"/><keycap>F8</keycap></keycombo>.
                  To return to the graphical console, use
                  <keycombo><keycap function="control"/><keycap function="alt"/><keycap>F7</keycap></keycombo>.
                </para>
                <para>
                  On that console, you will have the &agama; CLI available.
                </para>
              </step>
              <step>
                <para>
                  Optionally, set a password using the
                  <parameter>live.password=<replaceable>PASSWORD</replaceable></parameter> boot
                  parameter if you want to access the &agama; CLI over SSH instead, or want a
                  password prompt on a text console other than virtual console 8.
                </para>
              </step>
              <step>
                <para>
                  Load the profile with the following command:
                </para>
<screen>&prompt.root;<command>agama config load <replaceable>profile.json</replaceable></command></screen>
              </step>
              <step>
                <para>
                  Optionally, review and adjust the loaded configuration in an editor:
                </para>
<screen>&prompt.root;<command>agama config edit</command></screen>
              </step>
              <step>
                <para>
                  Validate the profile:
                </para>
<screen>&prompt.root;<command>agama config validate <replaceable>profile.json</replaceable></command></screen>
              </step>
              <step>
                <para>
                  Initiate the installation with the following command:
                </para>
<screen>&prompt.root;<command>agama install</command></screen>
              </step>
              <step>
                <para>
                  Monitor the installation with the following command:
                </para>
<screen>&prompt.root;<command>agama monitor</command></screen>
              </step>
              <step>
                <para>
                  Finish the installation with the following command:
                </para>
<screen>&prompt.root;<command>agama finish</command></screen>
              </step>
            </substeps>
          </step>
        </stepalternatives>
      </step>
    </procedure>
  </section>
  <section xml:id="initiating-agama-automated-installation-summary">
    <title>Summary of starting an unattended installation</title>
    <para>
      An unattended &agama; installation starts from the <literal>inst.auto</literal> kernel boot
      parameter pointing to a profile, or from a profile placed at one of &agama;'s predefined
      locations if no <literal>inst.auto</literal> parameter is given. If you also need shell
      access during the installation,
      switch to the root shell already running on virtual console 8, or set a root password with
      <literal>live.password</literal> to log in through a different text console or via SSH.
      Either way, the &agama; CLI lets you load, edit, and validate the profile, then start,
      monitor, and finish the installation.
    </para>
  </section>
  <section xml:id="initiating-agama-automated-installation-troubleshooting">
    <title>Troubleshooting the start of an installation</title>
    <itemizedlist>
      <listitem>
        <para>
          If <command>agama install</command> reports that preconditions for installation are not
          met, run <command>agama config validate <replaceable>profile.json</replaceable></command>
          again to confirm the loaded profile is valid, and check <command>agama status</command>
          for the current installation state.
        </para>
      </listitem>
      <listitem>
        <para>
          If the installation fails or behaves unexpectedly, collect the installation logs before
          the live session ends or the system reboots. See
          <xref linkend="post-installation-troubleshooting-agama-automated-installation"/>.
        </para>
      </listitem>
    </itemizedlist>
  </section>
</topic>
