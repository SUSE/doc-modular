<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="kernel-boot-parameters-modify-troubleshoot"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Troubleshooting modified kernel boot parameters</title>
    <!-- Title can be changed via merge in the assembly -->
    <meta name="maintainer" content="ssarkar@suse.com" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        A wrong or unsupported kernel boot parameter can leave a system unstable or unable to
        boot. This section covers how to identify a bad parameter, recover a system that no
        longer boots, and confirm stability after a change.
      </para>
    </abstract>
  </info>
  <section xml:id="kernel-boot-parameters-modify-troubleshoot-common-problems">
    <title>Troubleshooting modifications to kernel boot parameters</title>
    <para>
      The following steps cover the most common issues: an incorrect parameter, a system that
      fails to boot, and a system that boots but behaves unpredictably afterward.
    </para>
    <section xml:id="sec-kernel-troubleshoot-incorrect-parameters">
      <title>Identifying and correcting parameter issues</title>
      <procedure>
        <step>
          <para>
            Validate parameter syntax and configuration:
          </para>
          <substeps>
            <step>
              <para>
                Verify the spelling accuracy of all kernel parameters.
              </para>
            </step>
            <step>
              <para>
                Check for correct formatting without unnecessary spaces.
              </para>
            </step>
            <step>
              <para>
                Confirm that the parameters are recognized by the kernel. If the parameter is
                already part of the standard configuration, check the current value with
                <command>cat /proc/cmdline</command>. For a complete list of allowed parameters,
                refer to the kernel documentation at
                <link xlink:href="https://www.kernel.org/doc/Documentation/admin-guide/kernel-parameters.txt"/>.
              </para>
            </step>
          </substeps>
        </step>
        <step>
          <para>
            Investigate system logs for detailed error information:
          </para>
<screen>&prompt.sudo;<command>dmesg | grep -i error</command></screen>
<screen>&prompt.sudo;<command>journalctl -k -b</command></screen>
        <para>
          The <command>journalctl -k -b</command> output includes a <literal>Command line:</literal>
          entry showing the exact kernel boot parameters the kernel received for the current boot,
          which you can compare against what you intended to set.
        </para>
        </step>
        <step>
          <para>
            Temporarily enable or disable suspicious parameters to isolate issues. For example, use
            <parameter>nomodeset</parameter> to troubleshoot device driver issues related to
            graphics cards.
          </para>
        </step>
      </procedure>
    </section>
    <section xml:id="sec-kernel-troubleshoot-boot-failures">
      <title>Recovering from boot failures</title>
      <para>
        A system that fails to boot is no longer reachable over the network. You need physical
        or remote console access (for example, a hypervisor console or a hardware management
        interface) to perform the following steps.
      </para>
      <procedure>
        <step>
          <para>
            Boot into recovery mode:
          </para>
          <substeps>
            <step>
              <para>
                Restart the system.
              </para>
            </step>
            <step>
              <para>
                If the &grub; menu does not appear, press <keycap function="escape"/> while the
                &productnameshort; boot logo shows to interrupt the boot.
              </para>
            </step>
            <step>
              <para>
                At the &grub; menu, select <guimenu>Advanced options for &productnameshort;
                &productnumber;</guimenu>.
              </para>
            </step>
            <step>
              <para>
                Select the <literal>(recovery mode)</literal> entry for the kernel version you
                want to boot.
              </para>
            </step>
          </substeps>
          <para>
            Recovery mode boots the system in single-user mode, without the parameters set for
            normal boot. It gives you a minimal working environment to fix the configuration.
          </para>
        </step>
        <step>
          <para>
            Fix the configuration. Recovery mode does not use
            <varname>GRUB_CMDLINE_LINUX_DEFAULT</varname>, so you have a working root shell
            regardless of what it currently contains:
          </para>
          <stepalternatives>
            <step>
              <para>
                If you have a pre-modification backup, restore it:
              </para>
<screen>&prompt.sudo;<command>cp /etc/default/grub.bak /etc/default/grub</command></screen>
            </step>
            <step>
              <para>
                If you do not have a backup, remove the offending parameter directly:
              </para>
<screen>&prompt.sudo;<command>update-bootloader --del-option <replaceable>PARAMETER</replaceable></command></screen>
            </step>
          </stepalternatives>
        </step>
        <step>
          <para>
            Regenerate the &grub; configuration:
          </para>
<screen>&prompt.sudo;<command>update-bootloader --config</command></screen>
        </step>
      </procedure>
    </section>
    <section xml:id="sec-kernel-troubleshoot-system-stability">
      <title>Managing system stability</title>
      <procedure>
        <step>
          <para>
            Before making further changes, back up the current, still-working configuration:
          </para>
<screen>&prompt.sudo;<command>cp /etc/default/grub /etc/default/grub.bak</command></screen>
<screen>&prompt.sudo;<command>cp /boot/grub2/grub.cfg /boot/grub2/grub.cfg.bak</command></screen>
        </step>
        <step>
          <para>
            Check the system logs for errors or warnings introduced by the change:
          </para>
<screen>&prompt.sudo;<command>journalctl -xe</command></screen>
<screen>&prompt.sudo;<command>dmesg | tail</command></screen>
        <para>
          If a parameter change made the boot noticeably slower, check which units took the
          longest to start:
        </para>
<screen><command>systemd-analyze blame</command></screen>
        </step>
        <step>
          <para>
            Remove the recently added parameters one at a time, rebooting after each, to find
            the cause.
          </para>
        </step>
        <step>
          <para>
            If the issues persist, restore the backup from the first step, or boot from a
            previously working system snapshot.
          </para>
        </step>
      </procedure>
    </section>
  </section>
</topic>
