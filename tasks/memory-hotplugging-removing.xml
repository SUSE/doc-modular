<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="memory-hotplugging-removing"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Unplugging a RAM module for a bare machine</title><!-- can be changed via merge
in the assembly -->
    <!-- add author's e-mail -->
    <meta name="maintainer" content="" its:translate="no"/>
    <abstract><!-- can be changed via merge in the assembly -->
      <para>
       To safely unplug a physical memory module from a running bare-metal machine, you must follow
       a multi-layered procedure to avoid  electrical damage or an immediate kernel panic.
      </para>
    </abstract>
  </info>
  <para>
    Before you unplug a RAM module, perform the following checks:
  </para>
  <itemizedlist>
    <listitem>
      <para>
        The system must have enough remaining memory to run properly. Run <command>free -h</command> to verify available system memory.
      </para>
    </listitem>
    <listitem>
      <para>
      Check if any high-performance workloads (databases, JVMs, or CXL-bound applications) are
      pinned to the specific NUMA node you want to remove.
      </para>
      <itemizedlist>
        <listitem>
          <para>
            Look for applications launched with <literal>numactl</literal> or using the <literal>mbind()</literal> system call
          </para>
        </listitem>
        <listitem>
          <para>
            If a process is strictly bound to that node, gracefully stop the application or alter its affinity settings. If you skip this, the kernel will fail to clear the node, or worse, trigger the OOM Killer and terminate unrelated processes.
          </para>
        </listitem>
      </itemizedlist>
    </listitem>
  </itemizedlist>
</topic>
