<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="memory-hotplugging-removing"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Unplugging a RAM module for a bare-metal machine</title><!-- can be changed via merge
in the assembly -->
    <!-- add author's e-mail -->
    <meta name="maintainer" content="" its:translate="no"/>
    <abstract><!-- can be changed via merge in the assembly -->
      <para>
       To safely unplug a physical memory module from a running bare-metal machine, you must follow
       a multi-layered procedure to avoid electrical damage or an immediate kernel panic.
      </para>
    </abstract>
  </info>
  <para>
    Before you unplug a RAM module, perform the following checks:
  </para>
  <itemizedlist>
    <listitem>
      <para>
        The system must have enough remaining memory to run properly. Run <command>free -h</command> to verify available system memory.
      </para>
    </listitem>
    <listitem>
      <para>
      Check if any high-performance workloads (databases, JVMs, or CXL-bound applications) are
      pinned to the specific NUMA node you want to remove.
      </para>
      <itemizedlist>
        <listitem>
          <para>
            Look for applications launched with <literal>numactl</literal> or using the <literal>mbind()</literal> system call
          </para>
        </listitem>
        <listitem>
          <para>
            If a process is strictly bound to that node, gracefully stop the application or alter its affinity settings. This operation might break the memory policy if there are applications pinned to the current NUMA node being removed
          </para>
        </listitem>
      </itemizedlist>
    </listitem>
  </itemizedlist>
  <para>
    After these checks, proceed as described bellow: 
  </para>
  <procedure>
    <step>
      <para>
        Identify the Target Memory Blocks: You need to map the physical slot to the corresponding sysfs directories. You can find which memory blocks belong to which physical NUMA node by checking:
      </para>
      <screen>&prompt.sudo; <command>ls /sys/devices/system/node/nodeX/</command></screen>
    </step>
    <step>
      <para>
        Make the RAM module offline by running:
      </para>
      <screen>&prompt.user; <command>echo offline | sudo tee /sys/devices/system/memory/memoryXXX/state</command></screen>
    </step>
    <step>
      <para>
        Verify that the memory module is correctly set to <guimenu>offline</guimenu> by running the following command:
      </para>
      <screen>&prompt.user;<command>cat
      /sys/devices/system/memory/memory<replaceable>BLOCK_ID</replaceable>/state</command></screen>
      <para>
        The command must state <guimenu>offline</guimenu>.
      </para>
    </step>
    <step>
      <para>
        Remove the DIMM module from the slot.
      </para>
    </step>
      </procedure>
</topic>
