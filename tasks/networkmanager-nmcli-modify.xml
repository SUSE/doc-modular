<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="networkmanager-nmcli-modify"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Modifying Network Connection</title><!-- can be changed via merge
in the assembly -->
    <!-- add author's e-mail -->
    <meta name="maintainer" content="shalaka.harne@suse.com" its:translate="no"/>
    <abstract><!-- can be changed via merge in the assembly -->
      <para>You can use the <command>nmcli connection modify</command> command to modify network connections.</para>
     <para>
        You can set a static IP address, DNS server, Gateway, change connection name, change Wi-Fi SSID for Wi-Fi connections, and enable or disable autoconnect using the <command>nmcli connection modify</command> command.
</para>
      </abstract>
  </info>
<procedure xml:id="modify-networkmanager"><step><para>View the list of connections:</para>
<para></para>
  <screen>nmcli connection modify</screen>
  <para>The list of available active network connections appear with the names and UUIDs.</para>
  </step>
  <step><para>View the status of all network interfaces:</para>
  <screen>nmcli monitor</screen>
  </step>
<step><para>Modify a connection:</para>
<screen>nmcli connection modify <replaceable>CONNECTION-NAME</replaceable> <replaceable>PROPERTY</replaceable> <replaceable>VALUE</replaceable></screen> 
<substeps><step><para>Set a static IP address:</para>
<screen>nmcli connection modify <replaceable>CONNECTION-NAME</replaceable> ipv4.addresses <replaceable>IP-ADDRESS</replaceable>/<replaceable>PREFIX-LENGTH</replaceable>
nmcli connection modify <replaceable>CONNECTION-NAME</replaceable> ipv4.method manual
</screen>
</step>
<step><para>Set DNS servers:</para>
<screen>nmcli connection modify <replaceable>CONNECTION-NAME</replaceable> ipv4.dns "8.8.8.8 8.8.4.4"</screen></step>
<step><para>Set gateway:</para>
<screen>nmcli connection modify <replaceable>CONNECTION-NAME</replaceable> ipv4.gateway <replaceable>GATEWAY-IP</replaceable></screen></step>
<step><para>Change connection name:</para>
<screen>nmcli connection modify <replaceable>OLD-CONNECTION-NAME</replaceable> connection.id <replaceable>NEW-CONNECTION-NAME</replaceable>
</screen>
</step>
<step><para>Modify the Wi-Fi SSID:</para>
<screen>nmcli connection modify <replaceable>CONNECTION-NAME</replaceable> 802-11-wireless.ssid <replaceable>NEW-SSID</replaceable>
</screen>
</step>
<step><para>To enable autoconnect:</para>
<screen>nmcli connection modify <replaceable>CONNECTION-NAME</replaceable> connection.autoconnect yes
</screen></step><step><para>To disable autoconnect:</para>
  <screen>nmcli connection modify <replaceable>CONNECTION-NAME</replaceable> connection.autoconnect no
  </screen></step>
</substeps>
</step>
<step><para>To apply the changes, bring the network connection down and then up:</para>
<screen>nmcli connection down <replaceable>CONNECTION-NAME</replaceable>
nmcli connection up <replaceable>CONNECTION-NAME</replaceable>
  </screen>
</step></procedure>
</topic>