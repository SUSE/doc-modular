<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-unversioned/blob/main/openscap/xml/article_openscap.xml -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<topic xml:id="openscap-system-scanning"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Scanning the System for Vulnerabilities</title>
    <meta name="maintainer" content="souvik.sarkar@suse.com" its:translate="no"/>
    <abstract>
      <para>
        Use <command>oscap</command> to evaluate your &productname; system against a security profile and
        generate a report of the results.
      </para>
    </abstract>
  </info>
  <section xml:id="openscap-system-scanning-intro">
    <title>Introduction</title>
    <para>
      The <command>oscap xccdf eval</command> command evaluates a system against the rules defined
      in a security profile and produces results in XML format. An HTML report can be generated
      alongside the XML results for human review. The evaluation typically takes a few minutes,
      depending on the number of rules in the selected profile.
    </para>
    <para>
      Before scanning, ensure that the <package>openscap-utils</package> and
      <package>scap-security-guide</package> packages are installed as described in
      <xref linkend="openscap-packages-installing"/>, and that you have reviewed the available
      profile as described in <xref linkend="openscap-profiles"/>.
    </para>
  </section>
  <section xml:id="openscap-system-scanning-basic">
    <title>Running a basic scan</title>
    <para>
      To scan your system locally against the <literal>general</literal> profile and save the
      results, run the following command:
    </para>
<screen>&prompt.sudo;<command>oscap xccdf eval \
  --profile xccdf_org.ssgproject.content_profile_general \
  --results <replaceable>/tmp/results.xml</replaceable> \
  --report <replaceable>/tmp/report.html</replaceable> \
  /usr/share/xml/scap/ssg/content/ssg-slmicro6-ds.xml</command></screen>
    <para>
      The results are saved to <filename>/tmp/results.xml</filename> and the HTML report to
      <filename>/tmp/report.html</filename>. Open the report in a web browser to review the
      evaluation results.
    </para>
  </section>
  <section xml:id="openscap-system-scanning-remote-resources">
    <title>Using remote resources during a scan</title>
    <para>
      Some &ssg; content references external OVAL files, for example to check whether the system is
      patched against known CVEs. By default, &openscap; skips these remote resources and displays
      a warning. The following options control this behavior.
    </para>
    <variablelist>
      <varlistentry>
        <term>Fetching remote resources automatically</term>
        <listitem>
          <para>
            If the target system has Internet access and you trust the remote content, use the
            <option>--fetch-remote-resources</option> option to download referenced files
            automatically during the scan:
          </para>
<screen>&prompt.sudo;<command>oscap xccdf eval \
  --fetch-remote-resources \
  --profile xccdf_org.ssgproject.content_profile_general \
  --results <replaceable>/tmp/results.xml</replaceable> \
  --report <replaceable>/tmp/report.html</replaceable> \
  /usr/share/xml/scap/ssg/content/ssg-slmicro6-ds.xml</command></screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Using locally downloaded remote resources</term>
        <listitem>
          <para>
            On systems without Internet access, or in security-sensitive deployments, download the
            required remote files in advance and pass them to <command>oscap</command> using the
            <option>--local-files</option> option:
          </para>
          <procedure>
            <step>
              <para>
                Create a directory for storing the downloaded files:
              </para>
<screen>&prompt.user;<command>mkdir ~/scap-files</command></screen>
            </step>
            <step>
              <para>
                Download the required remote resource:
              </para>
<screen>&prompt.user;<command>wget -O ~/scap-files/pub-projects-security-oval-suse.linux.enterprise.15-patch.xml.bz2 \
  https://ftp.suse.com/pub/projects/security/oval/suse.linux.enterprise.15-patch.xml.bz2</command></screen>
              <tip>
                <para>
                  Use the most specific file available for your product version to reduce resource
                  usage and scan time. For example, if you are interested only in a specific
                  service pack, use the corresponding SP-specific file from
                  <link xlink:href="https://ftp.suse.com/pub/projects/security/oval/"/>.
                </para>
              </tip>
            </step>
            <step>
              <para>
                Run the scan using the locally downloaded files:
              </para>
<screen>&prompt.sudo;<command>oscap xccdf eval \
  --local-files ~/scap-files \
  --profile xccdf_org.ssgproject.content_profile_general \
  --results <replaceable>/tmp/results.xml</replaceable> \
  --report <replaceable>/tmp/report.html</replaceable> \
  /usr/share/xml/scap/ssg/content/ssg-slmicro6-ds.xml</command></screen>
            </step>
            <step>
              <para>
                Optionally, generate an HTML report separately from the XML results file:
              </para>
<screen>&prompt.user;<command>oscap xccdf generate report /tmp/results.xml > /tmp/report.html</command></screen>
              <tip>
                <para>
                  Separating the scan and the report generation steps reduces resource usage on the
                  target system during the scan itself.
                </para>
              </tip>
            </step>
          </procedure>
        </listitem>
      </varlistentry>
    </variablelist>
  </section>
  <section xml:id="openscap-system-scanning-rules">
    <title>Scanning with specific rules</title>
    <para>
      By default, <command>oscap xccdf eval</command> evaluates all rules in the selected profile.
      You can narrow the scope of evaluation using the following options.
    </para>
    <variablelist>
      <varlistentry>
        <term>Evaluating a single rule</term>
        <listitem>
          <para>
            Use the <option>--rule</option> option to evaluate only a specific rule, identified by
            its rule ID:
          </para>
<screen>&prompt.sudo;<command>oscap xccdf eval \
  --profile xccdf_org.ssgproject.content_profile_general \
  --rule xccdf_org.ssgproject.content_rule_package_aide_installed \
  --report <replaceable>/tmp/report.html</replaceable> \
  /usr/share/xml/scap/ssg/content/ssg-slmicro6-ds.xml</command></screen>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Skipping a specific rule</term>
        <listitem>
          <para>
            Use the <option>--skip-rule</option> option to exclude a specific rule from the
            evaluation:
          </para>
<screen>&prompt.sudo;<command>oscap xccdf eval \
  --profile xccdf_org.ssgproject.content_profile_general \
  --skip-rule xccdf_org.ssgproject.content_rule_package_aide_installed \
  --report <replaceable>/tmp/report.html</replaceable> \
  /usr/share/xml/scap/ssg/content/ssg-slmicro6-ds.xml</command></screen>
        </listitem>
      </varlistentry>
    </variablelist>
  </section>
  <section xml:id="openscap-system-scanning-remote-machine">
    <title>Scanning a remote machine</title>
    <para>
      To scan a remote machine over SSH, use <command>oscap-ssh</command> instead of
      <command>oscap</command>. The <package>openscap-utils</package> package must be installed on
      both the local and the remote machine. The interface mirrors that of
      <command>oscap</command>:
    </para>
<screen>&prompt.sudo;<command>oscap-ssh <replaceable>user</replaceable>@<replaceable>host</replaceable> <replaceable>22</replaceable> xccdf eval \
  --profile xccdf_org.ssgproject.content_profile_general \
  --results <replaceable>/tmp/results.xml</replaceable> \
  --report <replaceable>/tmp/report.html</replaceable> \
  /usr/share/xml/scap/ssg/content/ssg-slmicro6-ds.xml</command></screen>
  </section>
</topic>
