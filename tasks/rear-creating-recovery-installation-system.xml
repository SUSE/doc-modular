<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sleha/blob/main/xml/ha_rear.xml -->

<topic xml:id="rear-creating-recovery-installation-system"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Creating the recovery installation system</title>
    <meta name="maintainer" content="tahlia.richardson@suse.com" its:translate="no"/>
    <abstract>
      <para>
        While the system to be protected is up and running, create a file backup and
       generate a recovery system that contains a system-specific &rear; recovery installer.
      </para>
      <para>
       For basic backups with <command>tar</command>, use the <command>rear mkbackup</command>
       command to create both the backup and the recovery system.
      </para>
      <para>
       For third-party backup tools, use the <command>rear mkrescue</command> command
       to create the recovery system, and use the third-party backup tool to create the backup.
      </para>
    </abstract>
  </info>

  <para>
   After you have configured &rear; as described in
   <xref linkend="rear-configuring" xrefstyle="select:label"/>, create the
   recovery installation system (including the &rear; recovery installer) plus the file backup.
  </para>
  <example xml:id="ex-rear-mkbackup">
    <title>Creating a recovery system with a basic <command>tar</command> backup</title>
    <para>
      Run the <command>rear mkbackup</command> command:
    </para>
<screen>&prompt.root;<command>rear -d -D mkbackup</command></screen>
    <para>
     This command performs the following steps:
    </para>
    <itemizedlist>
      <listitem>
        <para>
          Analyzes the target system and gathers information, in particular about the disk layout (partitioning, file systems, mount points) and about the boot loader.
        </para>
      </listitem>
      <listitem>
        <para>
          Creates a bootable recovery system with the information gathered in the first step. The resulting &rear; recovery installer is <emphasis>specific</emphasis> to the system that you want to protect from disaster. It can only be used to re-create this specific system.
        </para>
      </listitem>
      <listitem>
        <para>
          Calls the internal backup tool to back up system files and user files.
        </para>
      </listitem>
    </itemizedlist>
  </example>
  <example xml:id="ex-rear-mkrescue">
    <title>Creating a recovery system with a third-party backup</title>
    <orderedlist>
      <listitem>
        <para>
          Run the <command>rear mkrescue</command> command:
        </para>
<screen>&prompt.root;<command>rear -d -D mkrescue</command></screen>
        <para>
          This command performs the following steps:
        </para>
        <itemizedlist>
          <listitem>
            <para>
              Analyzes the target system and gathers information, in particular about the disk layout (partitioning, file systems, mount points) and about the boot loader.
            </para>
          </listitem>
          <listitem>
            <para>
              Creates a bootable recovery system with the information gathered in the first step. The resulting &rear; recovery installer is <emphasis>specific</emphasis> to the system that you want to protect from disaster. It can only be used to re-create this specific system.
            </para>
          </listitem>
        </itemizedlist>
        <important role="compact">
          <para>
            This command <emphasis>doesn't</emphasis> create a backup of the files.
          </para>
        </important>
      </listitem>
      <listitem>
        <para>
          Create a file backup using your third-party backup tool.
        </para>
      </listitem>
    </orderedlist>
  </example>
</topic>
