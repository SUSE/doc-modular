<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sle/blob/main/xml/rmt_install.xml -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="rmt-enabling-slp-announcements"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Enabling SLP announcements</title>
    <meta name="maintainer" content="" its:translate="no"/>
    <abstract>
      <para>
        Enable SLP (Service Location Protocol) announcements so that clients can discover the &rmt;
        service on the network.
      </para>
    </abstract>
  </info>
  <para>
    &rmt; includes the SLP service description file
    <filename>/etc/slp.reg.d/rmt-server.reg</filename>. To enable SLP
    announcements of the &rmt; service, follow these steps:
  </para>

  <procedure>
    <step>
      <para>
        If &firewalld; is running, open relevant ports and reload the
        &firewalld; configuration:
      </para>
<screen>&prompt.sudo;firewall-cmd --permanent --add-port=427/tcp
success
&prompt.sudo;firewall-cmd --permanent --add-port=427/udp
success
&prompt.sudo;firewall-cmd --reload</screen>
    </step>
    <step>
      <para>
        Verify that the SLP server is installed and install it if it is not:
      </para>
<screen>&prompt.sudo;zypper install openslp-server</screen>
    </step>
    <step>
      <para>
        Enable and start the SLP service:
      </para>
<screen>&prompt.sudo;systemctl enable slpd.service
&prompt.sudo;systemctl restart slpd.service</screen>
    </step>
  </procedure>
</topic>
