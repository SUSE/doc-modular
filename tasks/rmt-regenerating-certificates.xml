<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: https://github.com/SUSE/doc-sle/blob/main/xml/rmt_certificates.xml -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="rmt-regenerating-certificates"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Managing TLS/SSL certificates</title>
    <meta name="maintainer" content="amrita.sakthivel@suse.com" its:translate="no"/>
    <abstract>
      <para>
        Regenerate the TLS/SSL certificates of your &rmt; server using the
        &ansible; playbook when they expire.
      </para>
    </abstract>
  </info>
  <section xml:id="rmt-regenerating-certificates-https">
    <title>Regenerating HTTPS certificates</title>

    <para>
      HTTPS certificates should be regenerated before they expire. No
      additional actions are required on the client machines registered to the
      &rmt; server if only HTTPS certificates are regenerated.
    </para>

    <note>
      <title>Certificates generated by the playbook are self-signed</title>
      <para>
        The playbook generates a self-signed server certificate that carries
        neither a common name nor subject alternative names, and that is not
        signed by the CA certificate in
        <filename>/etc/rmt/ssl/rmt-ca.crt</filename>. Clients that verify host
        names reject such a certificate. If your clients verify the server
        certificate, issue one yourself with a common name, the required
        subject alternative names and a signature from your CA, then store it
        as <filename>/etc/rmt/ssl/rmt-server.crt</filename> together with its
        key in <filename>/etc/rmt/ssl/rmt-server.key</filename>. Note that any
        subsequent run of the playbook overwrites both files.
      </para>
    </note>

    <procedure>
      <step>
        <para>
          Stop nginx and rmt-server services.
        </para>
<screen>&prompt.root;<command>systemctl stop nginx</command>
&prompt.root;<command>systemctl stop rmt-server</command></screen>
      </step>
      <step>
        <para>
          Remove previously generated certificates.
        </para>
<screen>&prompt.root;<command>rm /etc/rmt/ssl/rmt-server.*</command></screen>
      </step>
      <step>
        <para>
          Regenerate certificates using the &ansible; playbook:
        </para>
<screen>&prompt.root;<command>cd /usr/share/ansible/rmt</command>
&prompt.root;<command>ansible-playbook site.yml</command></screen>
      </step>
    </procedure>
  </section>
  <section xml:id="rmt-regenerating-certificates-ca">
    <title>Regenerating CA certificates and HTTPS certificates</title>

    <para>
      CA certificates can be regenerated after they have expired or in case of
      security issues.
    </para>

    <warning>
      <title>Import CA certificate on all clients</title>
      <para>
        The newly generated CA certificate must be imported on all clients
        registered to the &rmt; server. This can be done by running the
        <command>rmt-client-setup</command> script on the client machines as
        described in
        <link xlink:href="https://documentation.suse.com/sles/15-SP7/single-html/SLES-rmt/#sec-rmt-client-clientsetupscript">Configuring clients with <command>rmt-client-setup</command></link>.
      </para>
    </warning>

    <procedure>
      <step>
        <para>
          Stop nginx and rmt-server services.
        </para>
<screen>&prompt.root;<command>systemctl stop nginx</command>
&prompt.root;<command>systemctl stop rmt-server</command></screen>
      </step>
      <step>
        <para>
          Remove previously generated CA and HTTPS certificates.
        </para>
<screen>&prompt.root;<command>rm /etc/rmt/ssl/rmt-ca.*
  rm /etc/rmt/ssl/rmt-server.*</command>
</screen>
      </step>
      <step>
        <para>
          Regenerate certificates using the &ansible; playbook:
        </para>
<screen>&prompt.root;<command>cd /usr/share/ansible/rmt</command>
&prompt.root;<command>ansible-playbook site.yml</command></screen>
      </step>
    </procedure>
  </section>
</topic>
