<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="rt-docker-running"
 role="concept" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Running RT applications in &docker; containers</title><!-- can be changed via merge in the assembly -->
    <!--add author's email address-->
    <meta name="maintainer" content="jsidelarova@suse.com" its:translate="no"/>
    <abstract><!-- can be changed via merge in the assembly -->
      <para>
       If you intend to run compute-intensive applications with real-time priority, you must make sure that kernel threads cannot starve. (This is general advice that applies to other real-time scenarios as well.)
      </para>
    </abstract>
  </info>
  <section xml:id="rt-docker-running-prerequisites">
  <title>Prerequisites</title>
  <para>
  A simple precaution is to use the <literal>rtkthread=PRIORITY</literal> and <literal>rtworkqueues=PRIORITY</literal> kernel boot parameters. Set the <literal>PRIORITY</literal> values higher than the priority of any process that has the potential to dominate a CPU. This is not strictly real-time capable, but it is safer overall.
  </para>
  <para>
  On top of that kernel boot parameters, the following conditions must be met:
  </para>
  <itemizedlist>
  <listitem>
  <para>
  The kernel must be booted with the <literal>nortsched</literal> command-line parameter to hide cgroup scheduling from &docker;
  </para>
  </listitem>
  <listitem>
  <para>
  The <command>docker run</command> command must be issued with the <literal>--privileged=true</literal> parameter
  </para>
  </listitem>
  <listitem>
  <para>
  The container is equipped with the <literal>chrt</literal> system tool
  </para>
  </listitem>
  </itemizedlist>
  </section>
  <section xml:id="rt-docker-running-starting">
  <title>Starting real time application containers</title>
  <para>
  To start a &docker; container without isolation, run the command <command>docker run</command> using <literal>chrt</literal> to set the RT class/priority of the program you execute when starting of the container. For example:
  </para>
  <screen>&prompt.sudo;<command>docker run --privileged=true ... /usr/bin/chrt -f 1 /usr/sbin/sshd -D</command></screen>
  <para>
  The above starts <literal>sshd</literal> within the container as a <literal>SCHED_FIFO</literal> task of priority 1. If you SSH into it, and any command/application run in the container inherits the scheduler's RT class/priority.
  </para>
  </section>
</topic>
