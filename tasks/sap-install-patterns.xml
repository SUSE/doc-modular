<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<topic xml:id="sap-install-patterns"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Installing &sap; software patterns</title>
    <meta name="maintainer" content="eliska.romanova@suse.com" its:translate="no"/>
    <abstract>
      <para>
        Learn about &sap; software patterns, installation options and pattern verification steps.
      </para>
    </abstract>
  </info>
  <para>
    &suse; packages software and dependencies into patterns tailored to specific &sap; scenarios.
    When you install the operating system using &agama;, you can select the patterns during the
    installation.
  </para>
  <para>
    To list which &sap; patterns are already in place on your system, run the following command:
  </para>
<screen>&prompt.user;<command>zypper search --installed-only --type pattern sap</command></screen>
  <para>
    If you need to apply patterns after the installation, follow these steps:
  </para>
  <procedure>
    <step>
      <para>
        List available &sap;-related patterns on your system:
      </para>
<screen>&prompt.user;<command>zypper search --type pattern sap</command></screen>
      <tip>
        <title>Review complete list of available patterns</title>
        <para>
          For a complete list of patterns available for &productname; and your target architecture,
          see <link xlink:href="https://scc.suse.com/packages"></link> in the &scc;: Select your
          product, version and architecture, and type <emphasis>patterns-sap</emphasis> in the
          <emphasis role="bold">Matching name</emphasis> field.
        </para>
      </tip>
    </step>
    <step>
      <para>
        Optionally, list the information and contents of a specific pattern:
      </para>
<screen>&prompt.user;<command>zypper info <replaceable>PATTERN_NAME</replaceable></command></screen>
    </step>
    <step>
      <para>
        Identify the pattern that matches your workload and install it. For example, to install the
        pattern for &hana;, run:
      </para>
<screen>&prompt.sudo;<command>zypper install --type pattern sles_sap_DB</command></screen>
      <note>
        <title>Other common patterns</title>
        <para>
          Other common patterns include <package>sles_sap_APP</package> for &netweaver; and &s4h;,
          as well as <package>sles_sap_HADB</package> and <package>sles_sap_HAAPP</package> for
          &ha; setups.
        </para>
      </note>
    </step>
  </procedure>
</topic>
