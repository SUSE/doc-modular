<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<topic xml:id="saptune-configure"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Configuring &saptune;</title>
    <!-- can be changed via merge
      in the assembly -->
    <!-- add author's e-mail -->
    <meta name="maintainer" content="" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        Learn how to configure &saptune; after it has been installed.
      </para>
    </abstract>
  </info>
  <section xml:id="task-saptune-configure-requirements">
    <title>Requirements</title>
    <itemizedlist>
      <listitem>
        <para>
          A system with &saptune; installed.
        </para>
      </listitem>
    </itemizedlist>
  </section>

  <section xml:id="task-saptune-configure-perform">
    <title>Configuring &saptune;</title>
    <para>
      Keep in mind that the <command>configure</command> command manages  the &saptune; configuration and not the tuning itself. The command exposes only user-defined changes,
      and it helps avoid misconfiguration. Configurable options include the color scheme, skipped
      <command>sysctl</command> files, etc. (refer to <command>man 8 </command> for more info). The command can be used as
      follows:
    </para>
<screen>saptune configure <replaceable>PARAMETER</replaceable> <replaceable>VALUE</replaceable></screen>
    <para>
      For example:
    </para>
<screen>saptune configure COLOR_SCHEME red-noncmpl</screen>
    <para>
      To view the &saptune; configuration file, use the <command>saptune configure show</command>
      command.
    </para>
    <para>
      The following command reverts the &saptune; configuration to its defaults:
    </para>
<screen>saptune configure reset</screen>
    <important>
      <para>
        Do not edit the <filename>/etc/sysconfig/saptune</filename> directly, and use
        <command>saptune configure</command> instead.
      </para>
    </important>
  </section>
</topic>
