<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<topic xml:id="saptune-configure-tuning"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Configuring &saptune; tuning</title>
    <!-- can be changed via merge
      in the assembly -->
    <!-- add author's e-mail -->
    <meta name="maintainer" content="" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        Learn how to configure &saptune; tuning.
      </para>
    </abstract>
  </info>
  <section xml:id="task-saptune-configure-tuning-requirements">
    <title>Requirements</title>
    <itemizedlist>
      <listitem>
        <para>
          A system with &saptune; installed, configured, and enabled.
        </para>
      </listitem>
    </itemizedlist>
  </section>

  <section xml:id="task-saptune-tuning-perform">
    <title>Configuring &saptune; tuning</title>
    <para>
      The easiest way to tune a system is to apply an &sapsol; that matches your
      installed &sap; software. &sapsol;s are a group of &sapnote;s that perform
      the actual tuning. To display all available Solutions and their Notes, run
      the following command:
    </para>
<screen>&prompt.root;<command>saptune solution list</command></screen>
    <para>
      &saptune; recognizes the following tuning &sapsol;s:
    </para>
    <itemizedlist>
      <listitem>
        <para>
          BOBJ Solution for running &bo;
        </para>
      </listitem>
      <listitem>
        <para>
          HANA Solution for running an &hana; database
        </para>
      </listitem>
      <listitem>
        <para>
          MAXDB Solution for running an &sap; MaxDB database
        </para>
      </listitem>
      <listitem>
        <para>
          NETWEAVER Solution for running &netweaver; application servers
        </para>
      </listitem>
      <listitem>
        <para>
          S4HANA-APPSERVER Solution for running &s4ha;s
        </para>
      </listitem>
      <listitem>
        <para>
          S4HANA-APP+DB Solution for running both &s4ha;s and &hana; on the same host
        </para>
      </listitem>
      <listitem>
        <para>
          S4HANA-DBSERVER Solution for running the &hana; database of an &s4h; installation
        </para>
      </listitem>
      <listitem>
        <para>
          SAP-ASE Solution for running an &ase; database. Note that the SAP-ASE
          Solution and the associated &sapnote;s 1805750 and 1680803 are
          deprecated and removed from &saptune; 3.2 on &slea; 16. The default
          settings are sufficient for ASE, so no additional tuning is required.
        </para>
      </listitem>
      <listitem>
        <para>
          NETWEAVER+HANA Solution for running both &sap; application servers and
          &hana; on the same host
        </para>
      </listitem>
      <listitem>
        <para>
          NETWEAVER+MAXDB Solution for running both &sap; application servers
          and MAXDB on the same host
        </para>
      </listitem>
    </itemizedlist>
    <para>
      To apply a Solution, run the following command:
    </para>
<screen>&prompt.root;<command>saptune solution apply <replaceable>SOLUTION</replaceable></command></screen>
    <para>
      Keep in mind that only one Solution can be applied at the time.
    </para>
    <para>
      To disable a Solution, use the command below:
    </para>
<screen>&prompt.root;<command>saptune solution revert<replaceable>SOLUTION</replaceable></command></screen>
    <para>
      To switch to a different Solution, use the following command:
    </para>
<screen>&prompt.root;<command>saptune solution change <replaceable>SOLUTION</replaceable></command></screen>
    <para>
      Alternatively, you can tune the computer according to recommendations from
      specific &sapnote;s. Use the <command>saptune note list</command> to view
      a list of notes that you can tune for.
    </para>
    <para>
      To apply a Note, run the following command:
    </para>
<screen>&prompt.root;<command>saptune note apply <replaceable>NOTE</replaceable></command></screen>
    <para>
      Reverting a Note can be done as follows:
    </para>
<screen>&prompt.root;<command>saptune note revert <replaceable>NOTE</replaceable></command></screen>
    <note>
      <title>Combining optimizations</title>
      <para>
        It is possible to combine Solutions and Notes by reverting Notes from an
        applied Solution or applying additional ones. However, only one solution
        can be active at a time. The <literal>saptune</literal> service restores
        the combination of Solution and Notes after a service restart or reboot.
      </para>
      <para>
        In rare cases, Notes can have conflicting options or parameters. Arrange
        your Notes carefully to avoid conflicts. The last Note always takes
        priority over conflicting options or parameters of previous notes. In
        this situation, create your own Solution (see
        <xref linkend="sec-saptune-create-solution"/>) or customize the applied Solution (see
        <xref linkend="sec-saptune-customize-solution"/>).
      </para>
    </note>
  </section>
</topic>
