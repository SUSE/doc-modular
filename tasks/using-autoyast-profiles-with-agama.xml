<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="using-autoyast-profiles-with-agama"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Using &ay; profiles with &agama;</title>
    <!-- can be changed via merge in the assembly -->
    <meta name="maintainer" content="souvik.sarkar@suse.com" its:translate="no"/>
    <abstract>
      <!-- can be changed via merge in the assembly -->
      <para>
        &agama; can reuse an existing &ay; profile for automated installation by converting it
        into its own JSON format, instead of requiring you to rewrite it from scratch. Support is
        not complete: some &ay; sections have no equivalent in &agama;, or behave differently.
        This topic shows how to convert an &ay; profile using the &agama; CLI and what to check
        afterward.
      </para>
    </abstract>
  </info>
  <section xml:id="using-autoyast-profiles-with-agama-introduction">
    <title>What &agama; does with an &ay; profile</title>
    <para>
      The <command>agama config generate</command> command accepts a path or URL and converts its
      content into a native &agama; JSON configuration. It recognizes an &ay; profile
      automatically: if the given path or URL ends in <literal>.xml</literal>,
      <literal>.erb</literal>, or a trailing slash (for a <literal>rules</literal> or
      <literal>classes</literal> directory), &agama; downloads and processes it as &ay;
      content, evaluating any Embedded Ruby (ERB) templating or rules/classes selection along the
      way, before converting the result to JSON. Any other input is treated as an already
      &agama;-native JSON or Jsonnet profile and passed through unchanged.
    </para>
    <para>
      &agama; is not a drop-in replacement for &ay;, and the conversion does not cover every
      &ay; section. For the exact list of which &ay; elements are supported, and which
      &agama; field they map to, refer to
      <xref linkend="autoyast-agama-compatibility-reference"/>.
    </para>
  </section>
  <section xml:id="using-autoyast-profiles-with-agama-requirements">
    <title>Requirements</title>
    <itemizedlist>
      <listitem>
        <para>
          An existing &ay; profile, reachable as a local path or a URL from the machine running
          the &agama; CLI. The profile can use ERB templating or rules/classes selection.
        </para>
      </listitem>
      <listitem>
        <para>
          Familiarity with which &ay; sections &agama; supports, to review after conversion. See
          <xref linkend="autoyast-agama-compatibility-reference"/>.
        </para>
      </listitem>
    </itemizedlist>
  </section>
  <section xml:id="using-autoyast-profiles-with-agama-executing">
    <title>Converting and using an &ay; profile</title>
    <procedure xml:id="proc-using-autoyast-profiles-with-agama">
      <step>
        <para>
          Convert the &ay; profile to a native &agama; JSON profile, writing the result to a
          file:
        </para>
<screen>&prompt.root;<command>agama config generate <replaceable>http://example.net/autoyast/profile.xml</replaceable> &gt; profile.json</command></screen>
      </step>
      <step>
        <para>
          Validate the converted profile against the &agama; profile schema:
        </para>
<screen>&prompt.root;<command>agama config validate profile.json</command></screen>
      </step>
      <step>
        <para>
          Open <filename>profile.json</filename> in a text editor and review it against
          <xref linkend="autoyast-agama-compatibility-reference"/>. The converter simply omits
          any &ay; element it does not support from the generated profile, so review it for
          missing configuration rather than relying on <command>agama config validate</command>
          to catch it: validation only rejects a profile that is itself malformed, not one that is
          silently missing settings the source &ay; profile had.
        </para>
      </step>
      <step>
        <para>
          Load the reviewed profile into the running &agama; installation session:
        </para>
<screen>&prompt.root;<command>agama config load profile.json</command></screen>
      </step>
    </procedure>
  </section>
  <section xml:id="using-autoyast-profiles-with-agama-summary">
    <title>Summary</title>
    <para>
      <command>agama config generate</command> lets you carry over an existing &ay; profile
      instead of writing an &agama; profile from scratch, but the two formats are not fully
      compatible. Always validate and review the converted profile before loading it, so
      unsupported &ay; sections are caught before installation rather than during it.
    </para>
  </section>
  <section xml:id="using-autoyast-profiles-with-agama-troubleshooting">
    <title>Troubleshooting</title>
    <itemizedlist>
      <listitem>
        <para>
          If the installed system is missing configuration that was present in the original
          &ay; profile, check <xref linkend="autoyast-agama-compatibility-reference"/> for
          that section. &agama; reports an unsupported field in the web interface when the
          profile is loaded, though a command started from the CLI may otherwise appear to make
          no progress; check the web interface if that happens.
        </para>
      </listitem>
      <listitem>
        <para>
          If your &ay; profile relies heavily on the <literal>partitioning</literal> section and
          converting it to the native &agama; <literal>storage</literal> format is impractical,
          consider using the <literal>legacyAutoyastStorage</literal> section instead, described
          in <link xlink:href="https://documentation.suse.com/sles/16.0/html/SLES-x86-64-agama-automated-installation/index.html#agama-advanced-storage-configuration">Advanced storage configuration using &agama; profiles</link>.
        </para>
      </listitem>
    </itemizedlist>
  </section>
</topic>
