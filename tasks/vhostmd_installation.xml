<?xml version="1.0" encoding="UTF-8"?>
<!-- This file originates from the project https://github.com/openSUSE/doc-kit -->
<!-- This file can be edited downstream. -->
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="vhostmd-installation"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>Getting <literal>vhostmd</literal></title><!-- can be changed via merge
in the assembly -->
    <!-- add author's e-mail -->
    <meta name="maintainer" content="jsindelarova@suse.com" its:translate="no"/>
    <abstract><!-- can be changed via merge in the assembly -->
      <para>
       The topic covers installation of <literal>vmhostmd</literal> on &productname;.
      </para>
    </abstract>
  </info>
  <para>
    By default, <literal>vhostmd</literal> is not present on the delivered images. So to use it,
    you need to install it first:
  </para>
  <procedure>
    <step>
      <para>
        Install the daemon by running:
      </para>
      <screen>
        &prompt.sudo;<command>transactional-update pkg install vhostmd</command>
          </screen>
    </step>
    <step>
      <para>
        Reboot your system to switch to the new snapshot.
      </para>
    </step>
    <step>
      <para>
        Start the daemon by running:
      </para>
      <screen>
&prompt.sudo;<command>vhostmd</command> <replaceable>OPTIONS</replaceable>
      </screen>
      <para>
        You can use the following options to modify the command behavior:
      </para>
      <itemizedlist>
        <listitem>
          <para>
            <literal>-v</literal> - for a verbose output
          </para>
        </listitem>
        <listitem>
          <para><literal>--connect <replaceable>URI</replaceable></literal> - to pass URI of the
          libvirt daemon to establish connections to the daemon</para>
        </listitem>
        <listitem>
          <para>
            <literal>-f <replaceable>FILE_PATH</replaceable></literal> - to pass an alternative
            configuration file. For details regarding configuration, refer to <xref linkend="vhostmd-configuration"/>. 
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>-p <replaceable>PID_FILE</replaceable></literal> - to create an alternative
            PID file. The default is <filename>/var/run/vhostmd.pid</filename>
          </para>
        </listitem>
        <listitem>
          <para>
            <literal>-u <replaceable>USERNAME</replaceable></literal> - to use a non-privileged user.
          </para>
        </listitem>
      </itemizedlist>
    </step>
  </procedure>
 
  
</topic>
