<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
  <!ENTITY % entities SYSTEM "../common/generic-entities.ent">
    %entities;
]>
<!-- refers to legacy doc: <add github link to legacy doc piece, if applicable> -->
<!-- point back to this document with a similar comment added to your legacy doc piece -->
<!-- refer to README.md for file and id naming conventions -->
<!-- metadata is dealt with on the assembly level -->
<topic xml:id="task-creating-systemd-timers"
 role="task" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.2"
 xmlns:its="http://www.w3.org/2005/11/its"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink"
 xmlns:trans="http://docbook.org/ns/transclusion">
  <info>
    <title>VM Host Setup</title>
    <meta name="maintainer" content="shalaka.harne@suse.com" its:translate="no"/>
    <abstract>
      <para>
        &systemd; timers help you to automate recurring tasks. A &systemd;
        timer can control triggering of &systemd; services and handling of
        events.
      </para>
    </abstract>
  </info> <para>VM Host Setup</para>
<para>The VM Host has four bridges involved in the setup.</para>
<para>Optional: When the VM Host is on a productive network with independent IP connectivity to the test networks, thus there no IP setup on any of these bridges including <literal>net1</literal>.
 But you can use the same network as the Host such as dhcp on <literal>net1</literal></para>
<para>The <literal>net1</literal> bridge contains an uplink port <literal>eth1</literal> providing access to an
  external <literal>test/lan</literal> network for the <literal>vm7</literal> router VM.
</para>
  <procedure>
<step>
<para><screen>$ cat /etc/sysconfig/network/ifcfg-net1`
  ```
  STARTMODE='auto'
  BOOTPROTO='none'
  LINK_REQUIRED=no
  BRIDGE='yes'
  BRIDGE_STP='off'
  BRIDGE_FORWARDDELAY='0'
  BRIDGE_PORTS='eth1'
  ```</screen>
</para></step>
<step>
<para>The <literal>net7</literal>, <literal>net8</literal> and <literal>net8</literal> are host-only bridges with dynamic VM ports
  (added by libvirt) only, specific and relevant for this setup. To view the <literal>cat /etc/sysconfig/network/ifcfg-net7</literal> network settings, run:</para>
  <screen>$ cat /etc/sysconfig/network/ifcfg-net7`
  ```
  STARTMODE='auto'
  BOOTPROTO='none'
  LINK_REQUIRED=no
  LLADDR=66:00:00:00:00:07
  BRIDGE='yes'
  BRIDGE_STP='off'
  BRIDGE_FORWARDDELAY='0'
  BRIDGE_PORTS=''
  ```</screen></step>
  <step><para>To view the <literal>ifcfg-net8</literal> network settings, run the following command:</para>
<screen>$ cat /etc/sysconfig/network/ifcfg-net8
  ```
  STARTMODE='auto'
  BOOTPROTO='none'
  LINK_REQUIRED=no
  LLADDR=66:00:00:00:00:08
  BRIDGE='yes'
  BRIDGE_STP='off'
  BRIDGE_FORWARDDELAY='0'
  BRIDGE_PORTS=''
  ```</screen>
</step>
  <step><para>To view the <literal>ifcfg-net9</literal> network settings, run the following command:</para>
<screen>$ cat /etc/sysconfig/network/ifcfg-net9`
  ```
  STARTMODE='auto'
  BOOTPROTO='none'
  LINK_REQUIRED=no
  LLADDR=66:00:00:00:00:09
  BRIDGE='yes'
  BRIDGE_STP='off'
  BRIDGE_FORWARDDELAY='0'
  BRIDGE_PORTS=''
  ```</screen>
    </step>
  </procedure>
</topic>
